# 修改记录

## 2026-09-03 修复标签编辑器旋转字段移动/缩放错乱（版本 2.2.14）

### 问题描述
标签预览编辑器中，字段旋转竖版（90°/270°）后左右只能移动一点点；旋转后缩放也存在错乱（拖动边角时宽高互换、位置在边界来回振荡）。

### 根因分析
- **移动**：边界限制用字段「本地」宽高，而旋转 90°/270° 后「视觉」宽高与本地互换（竖版字段本地宽很大、对应视觉高），水平可移动范围被算成 `标签宽 − 本地宽`，几乎为 0。
- **缩放**：
  1. 拖 e/w、s/n 边分支用本地宽/高当视觉基准，仅「不改宽高」分支做了 swapped 处理，旋转后缩放方向错乱；
  2. 位置补偿把本地宽和视觉宽混算，旋转后锚点调整错误；
  3. Auto 尺寸（条码 WidthMm=0）字段缩放时每帧读取 `ActualWidth`（随写回变化），形成反馈振荡（日志实测 W/H 交替、Y 在 0/12.21 间跳）。

### 修复方案（统一改为「视觉包围盒模型」）
1. 移动/缩放均以旋转后的**视觉包围盒**为基准；`offX/offY`（视觉框左上角 − 本地锚点）由本地尺寸 + 旋转角推导（90°/270° 时 = (W−H)/2），拖动中恒定，不再按 `VisualBox` 测量值回推（选中 1px 边框会使 ActualWidth/Height 变化，导致 offX/offY 漂移抖动）。
2. 缩放：拖 e/w 边改视觉宽、拖 s/n 边改视觉高，视觉框四边跟随鼠标，按视觉尺寸限制不超出标签，最后用新尺寸反推本地锚点与本地宽高。
3. `El_MouseDown` 缓存有效本地宽/高基准（Auto 字段用当前渲染尺寸），缩放全程使用，消除振荡。

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/LabelPreviewEditor.xaml.cs` | 移动/缩放改用视觉包围盒模型；MouseDown 缓存稳定基准；移除调试日志 |
| `src/QP11.Wpf/QP11.Wpf.csproj` | 版本号 2.2.13 → 2.2.14 |
| `QP11_Setup.iss` | MyAppVersion 同步为 2.2.14 |

### 编译结果
`dotnet build QP11.Wpf.csproj -c Debug`：**0 错误 0 警告**。

### 待用户验证
旋转字段竖版后拖动、缩放四个角，确认移动范围正常、无振荡（提交前已用调试日志实测验证）。

## 2026-08-29 修复作废单据后销售历史显示"配件报损"（作废改为物理删除单据）

### 问题描述
作废销售单后，在配件的销售历史中该记录变成了"卖给配件报损"，正确应为删除该销售历史记录。

### 根因分析（数据库实证，qipei@192.168.1.86,1433）
- `bill_sell.flag` 全库分布仅 1（正常销售 265157）/ 2（退货 32543）/ 3（2578）。
- **flag=3 是历史约定的「报损单」标记**（2576 张，client 均为配件报损客户 02288/03136/03346）。
- 新 WPF 代码把 `BusinessConstants.BillFlag.Voided` 定义为 3，作废单据时 `UPDATE bill_sell SET flag=3`，与报损标记撞车：
  - 销售历史查询 `CASE WHEN flag=3 THEN '配件报损'` 将作废单显示成"配件报损"；
  - 作废单混入报损管理列表（`BaosunRepository.GetListAsync WHERE flag=3`）。
- 已实证产生 2 张误作废单（2026-08-29 上午）00304010（客户 02130）/00304011（客户 00054），被当成报损单。
- 连带问题：`SellExchangeWindow`（换货窗）硬编码写 `bill_sell.flag=3`，换货单同样会被误认报损（库中 0 张，未实际使用）；采购作废 `BuyQueryViewModel` 同样写 `BillFlag.Voided=3`，而 `bill_buy.flag=3/4` 已被借货业务占用（在借/还货单）。

### 修复方案（与旧系统"作废=直接删除数据"一致）
作废单据 = **物理删除该单**（同一事务内：回补库存 + 删除 detail_sell 明细 + 删除 bill_sell 单据头 + 删除 arrearage 欠款）。作废后销售历史/报表/单据列表自然不再出现该记录，无需再写 flag，彻底避开 flag=3 与报损的冲突。

1. **仓储层**：`SellRepository`/`BuyRepository` 新增 `DeleteBillAsync(sn, txn)`（物理删除单据头）。
2. **作废逻辑改造**（4+2 处）：
   - `SellViewModel.VoidBillAsync`（销售开单页）
   - `SellQueryViewModel.VoidBillAsync`（销售查询页，注入 `IUnitOfWorkFactory` + `IArrearageRepository`）
   - `SellService.VoidSellOrderAsync`（WebApi）
   - `SellEditWindow.BtnVoid_Click`（销售编辑窗，注入 `IArrearageRepository`，删除成功后关闭窗口）
   - `BuyQueryViewModel.VoidBillAsync`（采购查询页，同样改为物理删除）
3. **UI 文案**：作废确认框/成功提示改为"删除"语义（删除后不可恢复）。`SellControl`/`SellQueryControl`/`BuyQueryControl`/`SellEditWindow`/WebApi `SellController`。
4. **删除换货窗**：`SellExchangeWindow.xaml/.xaml.cs` 物理删除（无任何入口引用；旧系统无换货功能，换货均人工备注记录；其 flag=3 硬编码会误认报损）。
5. **存量数据清理**（已执行）：DELETE 00304010/00304011 的 bill_sell + detail_sell + arrearage（各 1 明细 + 1 欠款；库存当日作废时已回补，不再动）。

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Core/Interfaces/ISellRepository.cs` | 新增 `DeleteBillAsync(sn, txn)` 签名 |
| `src/QP11.Core/Interfaces/IBuyRepository.cs` | 新增 `DeleteBillAsync(sn, txn)` 签名 |
| `src/QP11.Data/Repositories/SellRepository.cs` | 实现 `DeleteBillAsync`（DELETE bill_sell WHERE sn） |
| `src/QP11.Data/Repositories/BuyRepository.cs` | 实现 `DeleteBillAsync`（DELETE bill_buy WHERE sn） |
| `src/QP11.Wpf/ViewModels/SellViewModel.cs` | `VoidBillAsync` 改为物理删除（明细+头+欠款） |
| `src/QP11.Wpf/ViewModels/SellQueryViewModel.cs` | `VoidBillAsync` 改为物理删除，注入 uow/arrearRepo |
| `src/QP11.Wpf/ViewModels/BuyQueryViewModel.cs` | `VoidBillAsync` 改为物理删除，注入 uow/arrearRepo |
| `src/QP11.Services/SellService.cs` | `VoidSellOrderAsync` 改为物理删除（明细+头+欠款） |
| `src/QP11.WebApi/Controllers/SellController.cs` | 注释与返回消息更新 |
| `src/QP11.Wpf/Views/SellEditWindow.xaml.cs` | 作废改删除（注入 arrearRepo）；删除成功关闭窗口 |
| `src/QP11.Wpf/Views/SellControl.xaml.cs` | 删除确认框/成功提示文案 |
| `src/QP11.Wpf/Views/SellQueryControl.xaml.cs` | 删除确认框/错误提示文案 |
| `src/QP11.Wpf/Views/BuyQueryControl.xaml.cs` | 删除确认框/错误提示文案 |
| `src/QP11.Wpf/Views/SellExchangeWindow.xaml/.xaml.cs` | **删除**（换货窗，flag=3 隐患，无入口） |

### 编译结果
`dotnet build QP11.Wpf.csproj / QP11.WebApi.csproj --no-incremental`：**0 错误 0 警告**。

### 待用户验证
1. 重启软件，作废一张销售单 → 确认弹窗为"删除"，确认后单据从销售历史/单据列表消失、库存回补。
2. 报损管理列表不再出现作废单；配件销售历史中报损单仍显示"配件报损"。

## 2026-08-29 优化应收款/应付款两个页面查询速度（数据库实测驱动）

### 问题描述
应收款/应付款页面（`ArrearageControl`）打开时列表加载约 **2 秒**，体验差。

### 数据库实测（qipei@192.168.1.86,1433，ODBC 计时）

| 查询 | 优化前 | 优化后 | 提速 |
|------|--------|--------|------|
| 应付款列表（type=1） | 2022ms | **约 65ms** | ~30 倍 |
| 应收款列表（type=2） | 2128ms | **约 140~650ms**（base+adj 两步，当前 adj 约 0 行） | ~4-15 倍 |
| 客户明细-应付（type=1） | 18ms | 无 join，更快 | - |
| 客户明细-应收（type=2） | 110ms | 仅 1 个 join | - |
| 应收应付管理窗列表（type=1） | 1070ms | 无 join，显著更快 | - |

### 根因分析
1. **应付款（type=1）**：原 SQL 无条件 LEFT JOIN `bill_sell`（30 万行），join 条件 `arrearage.sn=bs.sn AND arrearage.type=2` 在 type=1 下**恒不匹配**，却让 SQL Server 2000 对 30 万×27.7 万做 hash join（同 2026-08-28 报表案例的同类问题）。
2. **应收款（type=2）**：取反判定（`bs.flag=2 OR bs.total<0`）需要 join bill_sell；实测 join 30 万行 bill_sell 需约 2 秒（SQL2000 优化器对 OR 条件/UNION/IN 子查询均退化为全表扫描，多种改写方案实测 1.2s~12s 均不理想）。
3. **关键数据事实**：真正需要取反的 arrearage 行仅 286 行（type=2 共 18.7 万行），且当前**全部已结清**（未结清取反行=0）→ 取反修正对现值无影响，但为防未来出现未结清退货，保留修正逻辑。

### 优化方案（ArrearageRepository.cs）
1. **`GetClientArrearageListAsync`**：
   - type=1（应付）：去掉 LEFT JOIN bill_sell（恒不匹配 → 结果等价），CASE 收敛为 `SUM(total-charge)`。
   - type=2（应收）：两段式——① base：`SUM(total-charge)` 按客户聚合（无 join，约 140ms）；② adj 取反修正：`INNER LOOP JOIN` 仅对 `flag=2 OR total<0` 的退货单 sn 集合（3.2 万行）与未结清行匹配，结果合并 `total_je = base - adj`（当前 adj 为 0，将来自动修正）；C# 内存合并 + 排序。
2. **`GetArrearageDetailByBidAsync`**：按 type 裁剪 join——应付无 join（原 bill_sell 恒 NULL，CASE 收敛）；应收/全部仅 LEFT JOIN bill_sell（去掉恒不匹配且从未参与判定的 bill_buy 3.7 万行）。
3. **`GetListWithCalcAsync`**（应收应付管理窗）：同上按 type 裁剪（应付无 join；其它仅 bill_sell join）。

### 语义一致性验证（已执行）
对 7 个代表客户（含真实欠款 02707=16551.74、负欠款 00307=-7791.00、有取反行的 00011=705.00）逐客户对比新旧 SQL 结果：**全部一致**。

### 后续修复：RuntimeBinderException（'object' does not contain a definition for 'bid'）
优化上线后应收款/应付款页面报 `Microsoft.CSharp.RuntimeBinder.RuntimeBinderException: 'object' does not contain a definition for 'bid'`。

**根因**（临时控制台复现确认）：
- 旧实现 `GetClientArrearageListAsync` 返回 `IEnumerable<dynamic>`（DapperRow）。
- 新应收(type=2)分支混合返回**匿名类型**（`new { bid, name, total_je }`），与应付款分支的 DapperRow 在同一 UI 方法内混用，WPF/DynamicBinder 的 CallSite 缓存按类型切换时失效 → 运行时找不到 'bid'（现象：type=2 列表访问 bid 抛异常；后续复现还出现 type=1 行元素为 null 并导致进程崩溃）。
- 另外 Dapper 强类型映射**不做下划线归一化**：属性 `TotalJe` 无法匹配列别名 `total_je`（合计显示全 0）。

**修复方案（全面强类型，弃用 dynamic）**：
1. 新增 DTO `QP11.Core/Models/ArrearageSummaryItem.cs`（Bid/Name/TotalJe）。
2. `IArrearageRepository.GetClientArrearageListAsync` 返回类型改为 `Task<IEnumerable<ArrearageSummaryItem>>`；实现中两条分支均 `QueryAsync<ArrearageSummaryItem>`。
3. SQL 列别名 `total_je` → `TotalJe`（对齐 Dapper 属性匹配）；XAML 绑定三列全部改为 PascalCase：`{Binding bid}`→`{Binding Bid}`、`{Binding name}`→`{Binding Name}`、`{Binding total_je}`→`{Binding TotalJe}`（WPF Binding 属性名**区分大小写**，小写绑定强类型 DTO 会导致编号/名称列空白）。
4. `ArrearageControl.xaml.cs`：`DgClients_SelectionChanged`/`BtnConfirm_Click`/`BtnPrint_Click` 中 dynamic 访问 `row.bid` 改为强类型 `(ArrearageSummaryItem)`。

**验证**：临时控制台复测 type=1（35 行，合计真实）+ type=2（341 行，合计真实）均正常；`dotnet build` Wpf/WebApi **0 错误 0 警告**。

### 编译结果
`dotnet build QP11.Wpf.csproj / QP11.WebApi.csproj --no-incremental`：**0 错误 0 警告**。

### 待用户验证
重启软件，打开应收款/应付款页面：列表秒开、金额正确显示，选中客户明细正常，应收应付管理窗查询正常。

## 2026-08-28 打包 v2.2.10：Web 开单写入对齐 + 排行榜改造 + 更新源迁移 GitHub

### 一、Web 端销售开单数据写入与桌面端对齐

**问题**：Web 端开单数据写入与桌面端存在差异——`bill_sell.total_payment` 落库为 NULL（对账报表 `sell_settled=SUM(total_payment)` 会漏计 Web 端单子）；明细缺失 `unit`/`place`/`datetime`；`operator` 固定 "WebAPI"；无开单日期概念。

**修复**：
- 单头补齐 `Total`/`TotalPayment`（折后金额）/`BillPayment`，`SellService.CreateSellOrderAsync` 增加 `TotalPayment ??= BillPayment` 兜底（覆盖所有调用方）
- 明细补齐 `unit`（取 part_data）、`place`（`d.Place ?? part_data.Place` 兜底）、`datetime`（跟随单头）
- `CreateSellOrderRequest` 支持 `Datetime`，前端新增「开单日期」输入框以补录历史日期（所选日期 + 当前时刻，对齐桌面端 billDate）
- `operator` 最终策略：固定 `"WebAPI"` 标识来源（先尝试从登录 Token 取用户名，按需求改回固定值）

**修改文件**：
- [SellController.cs](file:///f:/qp11/src/QP11.WebApi/Controllers/SellController.cs) — 单头/明细字段补齐，请求体加 Datetime
- [SellService.cs](file:///f:/qp11/src/QP11.Services/SellService.cs#L61-L68) — TotalPayment 兜底
- [index.html](file:///f:/qp11/src/QP11.WebApi/wwwroot/index.html#L105-L108) / [app.js](file:///f:/qp11/src/QP11.WebApi/wwwroot/app.js) — 开单日期控件与提交传参

### 二、业务查询排行榜全面改造

**问题**：销售排行/Top客户仅显示前 50；客户排行欠款子查询引用了 `arrearage` 表不存在的 `cid` 列（查询必失败）；三入口（销售/客户/职员排行）进入同一页面；无导出。

**修复**：
- 销售排行、客户排行去掉 TOP 50 限制，显示全部；业务员业绩保留 TOP 50
- 客户排行欠款改为 `bid` 列一次预聚合 + LEFT JOIN（消除相关子查询），口径 `SUM(total - charge)` 与系统欠款统计一致
- 排序键：销售排行按**销售数量**、客户排行按**订单数**降序（SQL + 客户端兜底双重保证）
- 客户排行**包含退货单据**：净订单数 = 销售单 +1 / 退货单 -1，净订单数为负的整行红色（#C62828）
- 业务查询菜单三个入口合并为单个「排行榜」（RankingWindow 内含三 Tab）
- 新增导出：SaveFileDialog 自选保存位置，文件名含起止日期（如 `销售排行_20260801-20260828.xlsx`），客户排行导出保留红色行标记

**修改文件**：
- [RankingWindow.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/RankingWindow.xaml.cs) / [RankingWindow.xaml](file:///f:/qp11/src/QP11.Wpf/Views/RankingWindow.xaml) — 查询、排序、红色行、导出、按钮
- [MainWindow.xaml](file:///f:/qp11/src/QP11.Wpf/Views/MainWindow.xaml#L52-L53) / [MainWindow.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/MainWindow.xaml.cs) — 入口合并为「排行榜」
- [ExportService.cs](file:///f:/qp11/src/QP11.Services/ExportService.cs#L92-L103) — 新增 `ExportMultiSheetToPathAsync` 指定路径导出重载

### 三、在线更新源迁移 GitHub + 下载加速代理

**需求**：更新源从 Gitee 切换为 GitHub，并增加代理源提高国内下载速度。

**改动**：
- `GiteeReleaseClient` 重命名为 `GitHubReleaseClient`：API 改为 `api.github.com`，认证改请求头 `Bearer`，强制 User-Agent，去掉 URL 拼接 token 逻辑
- 配置键 `GiteeOwner/Repo/AccessToken` → `GitHubOwner/GitHubRepo/GitHubAccessToken`（兼容回退读取旧键），指向 `zhong5829/QP11-AutoParts-Manager`
- 新增 `GitHubDownloadProxy` 下载加速代理前缀（默认 `https://ghfast.top/`），安装包与 md5 文件下载时自动拼接；留空则直连
- `UpdateService` 下载认证头 `token` → `Bearer`，提示文案同步

**修改文件**：
- [GitHubReleaseClient.cs](file:///f:/qp11/src/QP11.Services/Update/GitHubReleaseClient.cs)（新增，删除 GiteeReleaseClient.cs）
- [UpdateService.cs](file:///f:/qp11/src/QP11.Services/Update/UpdateService.cs)
- [App.xaml.cs](file:///f:/qp11/src/QP11.Wpf/App.xaml.cs#L346-L363) / [appsettings.json](file:///f:/qp11/src/QP11.Wpf/appsettings.json#L19-L24)

### 四、版本与发布

- 版本号 2.2.9 → **2.2.10**（[QP11.Wpf.csproj](file:///f:/qp11/src/QP11.Wpf/QP11.Wpf.csproj#L28)）
- 推送 tag `v2.2.10` 触发 [release.yml](file:///f:/qp11/.github/workflows/release.yml) 打包（发布 WPF → Inno Setup → GitHub Release，状态 in_progress）

### 编译验证

`dotnet build src/QP11.Wpf/QP11.Wpf.csproj -c Debug`：0 错误，0 警告（同时修复了 2 个 CS0618 过时警告 + 1 个 robocopy MSB3073 警告）

### 提交记录

1. `1316ff3` fix: Web 端销售开单写入与桌面端对齐
2. `047b664` feat: 排行榜显示全部数据并支持导出，修复排序与欠款查询
3. `2e0bfe2` feat: 在线更新源从 Gitee 切换为 GitHub，并支持下载加速代理
4. `aa8e254` chore: 版本号升至 2.2.10

---
## 2026-08-26 打包 v2.2.9 安装程序（版本不变重打包）

### 操作内容
基于 v2.2.9 重新打包（版本号不变），包含 2026-08-26 修复的「销售退货页面选择客户后拿货明细加载慢10秒」改动。

### 打包步骤
1. 清理旧 `F:\qp11\Publish\` 目录
2. `dotnet publish F:\qp11\src\QP11.Wpf\QP11.Wpf.csproj -c Release -o F:\qp11\Publish`
   - 自包含发布（SelfContained=true，RuntimeIdentifier=win-x64）
   - QP11.Wpf.exe FileVersion=2.2.9.0
   - 编译警告：2 个 SqlConnection 过时警告（既有，与本打包无关）
   - robocopy 退出码 1（MSB3073 警告）= wwwroot 复制成功，属正常
3. `ISCC.exe F:\qp11\QP11_Setup.iss` 编译安装包（64.6 秒）

### 输出结果
- 安装包路径：`F:\qp11\InstallPackage\QP11Setup_v2.2.9.exe`（75.92 MB）
- Inno Setup 编译警告（3 个，均为既有问题，未引入）：
  - OnlyBelowVersion 6.1 的 quicklaunchicon 任务在新系统不处理
  - PrivilegesRequired=admin 与 per-user 区域（userappdata）共存提示
  - [Code] 段 ResultCode 变量未使用

---
## 2026-08-26 修复销售退货页面选择客户后拿货明细加载慢10秒

### 问题描述
销售退货页面（SellReturnControl）选择客户后，客户拿货明细加载要等约10秒（客户「沪青平邢振6-月结 清单发群」实测）。该客户有 6978 行正数（未退完）销售明细。

### 根因分析
`SellReturnViewModel.LoadSourceDetailsAsync` 查询中，`LEFT JOIN` 的聚合子查询对整张 `detail_sell` 表（54.5 万行）做 `GROUP BY tsn, partid` 全表聚合，用于计算「已退数量」。SQL Server 2000 下该子查询执行极慢：
- `detail_sell` 无 tsn 索引，全表聚合需扫描 54.5 万行
- 连接数据库实测（客户 02707）：原 SQL（20列+全表聚合+ORDER BY）7403ms；去掉聚合子查询仅 162ms；聚合子查询限定客户后 247ms

### 修复
聚合子查询增加 `AND bs.client = @ClientId`，把全表聚合缩小为「该客户自己的退货明细」聚合（该客户仅 300 余条退货记录）。业务语义不变：已退数量只需统计当前客户对其所购配件的退货。

`@ClientId` 在 SQL 中出现两次，`SqlParamConverter` 支持按出现顺序逐一绑定重复参数，已用应用同款连接路径（OleDbCompatConnection + Dapper）实测：6978 行耗时 393ms，已退数量/可退数量计算正确。

同时修复同一方法中 `cartype` 筛选分支重复添加 `CarTypePy` 参数（原会导致车型筛选时报「An item with the same key has already been added」）。

### 修改文件
- [SellReturnViewModel.cs](file:///f:/qp11/src/QP11.Wpf/ViewModels/SellReturnViewModel.cs#L87) — 聚合子查询限定客户；删除重复 CarTypePy 参数

### 编译验证
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj -c Debug`：0 错误，2 个既有警告（CS0618）

### 待用户验证
运行软件打开销售退货，选择客户后拿货明细应在 1 秒内加载（原约 10 秒）。

---
## 2026-08-13 修复工作台加载阻塞页面渲染（启动/切换卡顿10秒）

### 问题描述
增加工作台后，软件启动和切换到桌面导航页面时要等10秒左右页面才渲染出来。第一次修复（fire-and-forget）无效，仍然卡顿。

### 根因
ODBC 驱动的 `DbConnection.OpenAsync()` 实际是同步阻塞的（SQL Server 2000 ODBC 驱动不支持真正的异步 I/O）。即使 `_ = LoadDashboardAsync()` 是 fire-and-forget，`LoadDashboardAsync` 的同步部分（5 个仓储方法各自调用 `CreateConnectionAsync` → `db.OpenAsync()`）仍在 UI 线程上执行，依次阻塞 UI 线程，导致页面无法渲染。

### 修复
用 `Task.Run` 将所有数据库查询放到线程池线程上执行，UI 线程完全不被阻塞：
1. **DesktopControl.xaml.cs** `LoadDashboardAsync`：
   - 所有数据库查询（5 个仓储方法 + `Task.WhenAll` + 数据聚合）包在 `await Task.Run(async () => { ... })` 中
   - `Task.Run` 立即返回，UI 线程继续渲染页面
   - 数据加载完成后，`await` 之后的代码自动回到 UI 线程更新控件
   - catch 中 UI 操作用 `Dispatcher.Invoke` 保护
2. **DesktopControl.xaml.cs** `UserControl_Loaded`：`await LoadDashboardAsync()` 改为 `_ = LoadDashboardAsync()`（fire-and-forget）
3. **DesktopControl.xaml**：4 张 KPI 卡片默认文本改为「加载中...」

### 修改文件
- [DesktopControl.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml.cs#L50-L160) — `Task.Run` 后台线程加载 + fire-and-forget
- [DesktopControl.xaml](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml#L147) — KPI 卡片默认「加载中...」

### 编译验证
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj -c Debug`：0 错误，0 警告

---

## 2026-08-13 修复工作台应收/应付合计 RuntimeBinderException

### 问题描述
工作台加载时抛出 `Microsoft.CSharp.RuntimeBinder.RuntimeBinderException`，日志记录"加载工作台数据失败"，KPI 卡片数据不显示。

### 根因
`IArrearageRepository.GetClientArrearageListAsync` 返回 `IEnumerable<dynamic>`（Dapper 的 `DapperRow`）。在 `LoadDashboardAsync` 中通过 `r.total_je` 动态访问属性时，C# 运行时绑定器（RuntimeBinder）对 `DapperRow` 的 dynamic 分发失败，抛出异常。

### 修复
将 dynamic 属性访问改为 `IDictionary<string, object>` 字典访问（`DapperRow` 实现了该接口）：

```csharp
// 修复前（抛 RuntimeBinderException）
var receivableTotal = receivables.Sum(r => Convert.ToDecimal(r.total_je));

// 修复后
var receivableTotal = receivables.Sum(r =>
    Convert.ToDecimal(((IDictionary<string, object>)r)["total_je"]));
```

### 修改文件
- [DesktopControl.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml.cs#L139-L143) — 应收/应付合计改用字典访问

### 编译验证
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj -c Debug`：0 错误，0 警告

---

## 2026-08-13 移除桌面导航「我的快捷方式」区域

### 需求
移除桌面导航页面（DesktopControl）右侧工作台底部的「我的快捷方式」区域（原 desktop 表磁贴卡片 + 右键菜单）。

### 操作内容
1. **XAML**（[DesktopControl.xaml](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml)）：
   - 删除「我的快捷方式」GroupBox 整个区域（含 ItemsControl/WrapPanel/DataTemplate/右键菜单）
   - 删除 `ShortcutTileTemplate` ControlTemplate（不再使用）
2. **Code-Behind**（[DesktopControl.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml.cs)）：
   - 移除字段：`_repo`(IDesktopRepository)、`_desktopItems`、`_selectedShortcut`
   - 移除方法：`LoadDataAsync`、`DeleteSelectedAsync`、`ShortcutTile_Click`、`ShortcutTile_PreviewMouseRightButtonDown`、`MenuEdit_Click`、`MenuDelete_Click`、`MenuRefresh_Click`、`MenuSortByName_Click`、`MenuSortByTime_Click`
   - 移除构造函数参数 `IDesktopRepository repo`
   - `ITabContent.OnEdit`/`OnDelete` 改为空方法；`OnQuery` 仅刷新仪表板
   - 移除 `using System.Windows.Data`（CollectionViewSource 不再使用）
3. **MainWindow**（[MainWindow.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/MainWindow.xaml.cs#L83-L87)）：
   - 两处 `new DesktopControl(...)` 调用移除 `IDesktopRepository` 参数

### 编译验证
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj -c Debug`：0 错误，0 警告

---

## 2026-08-13 桌面导航右侧改造成工作台仪表板

### 需求
将桌面导航页面（DesktopControl）右侧导航区域从原来的 DataGrid（仅展示用户自定义快捷方式列表）改造成业务数据仪表板（工作台形式），保持现有 35:65 左右布局比例不变。

### 操作内容
1. **XAML 改造**（[DesktopControl.xaml](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml)）：
   - 左侧导航按钮区域完全不动
   - 右侧原 `dgDesktop` DataGrid 替换为四区域工作台布局：
     - **标题栏**：「工作台 · 今日数据快照」+ 最后刷新时间 + 手动刷新按钮
     - **KPI 卡片行**（4 张可点击卡片，UniformGrid 4 列）：
       - 今日销售（蓝色条）：汇总当日 `bill_sell.total` + 笔数，点击跳转销售查询(tag 133)
       - 今日采购（绿色条）：汇总当日 `bill_buy.total` + 笔数，点击跳转采购明细(tag 31)
       - 库存预警（红色条）：低库存配件数量，点击跳转库存预警(tag 156)
       - 应收款（橙色条）：应收合计 + 应付副行，点击跳转应收款(tag 22)
     - **双栏列表**：左侧 60% 今日销售单据（最近 10 笔，双击跳转销售查询），右侧 40% 库存预警前 10 条（双击跳转库存预警）
     - **我的快捷方式**：原 desktop 表数据改为 WrapPanel 横向磁贴卡片，单击打开对应功能；保留原右键菜单全部功能（编辑/删除/刷新/按名称排序/按时间排序）
   - 新增两个 ControlTemplate：`KpiCardTemplate`（白底圆角卡片+悬停高亮）、`ShortcutTileTemplate`（灰底磁贴+悬停高亮）
   - 新增 `Unloaded` 事件用于停止自动刷新定时器

2. **Code-Behind 改造**（[DesktopControl.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml.cs)）：
   - 构造函数扩展：注入 `IDesktopRepository` + `ISellRepository` + `IBuyRepository` + `IPartRepository` + `IArrearageRepository`
   - 新增 `LoadDashboardAsync()`：使用 `Task.WhenAll` 并行加载 5 个数据源（今日销售/采购/库存预警/应收/应付），填充 KPI 文本和列表
   - 60 秒自动刷新：`DispatcherTimer`，`Unloaded` 时停止
   - KPI 卡片点击跳转：`KpiCard_Click` → `OnDesktopButtonClick(tag)` → `MainWindow.OpenFunctionTab`
   - 快捷方式磁贴单击打开：`ShortcutTile_Click`
   - 右键选中记录：`ShortcutTile_PreviewMouseRightButtonDown` 记录到 `_selectedShortcut` 字段（替代原 `dgDesktop.SelectedItem`）
   - 原 `dgDesktop` 引用全部替换：`icShortcuts.ItemsSource`、`_selectedShortcut`
   - `ITabContent.OnQuery` 同步刷新工作台数据
   - BuyRepository.GetListAsync 未过滤 flag=-1，在代码中手动过滤

3. **MainWindow 改造**（[MainWindow.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/MainWindow.xaml.cs)）：
   - 第 83 行和第 461 行两处 `new DesktopControl(...)` 调用更新，传入 5 个仓储参数

### 涉及文件
- [DesktopControl.xaml](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml) — 右侧工作台布局
- [DesktopControl.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml.cs) — 数据加载/刷新/跳转逻辑
- [MainWindow.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/MainWindow.xaml.cs#L83-L88) — DesktopControl 构造参数（两处）

### 数据来源（全部复用现有仓储，无新增接口）
| KPI | 来源表 | 仓储方法 |
|------|--------|---------|
| 今日销售 | bill_sell | ISellRepository.GetListAsync(today, tomorrow) |
| 今日采购 | bill_buy | IBuyRepository.GetListAsync(today, tomorrow) |
| 库存预警 | part_stock | IPartRepository.GetStockAlertItemsAsync()（IsLowStock 过滤） |
| 应收款 | arrearage | IArrearageRepository.GetClientArrearageListAsync(2) |
| 应付款 | arrearage | IArrearageRepository.GetClientArrearageListAsync(1) |

### 行为约定
- 60 秒自动刷新工作台数据（DispatcherTimer，页面 Unloaded 时停止）
- KPI 卡片点击跳转对应功能页（复用 OpenFunctionTab）
- 快捷方式磁贴单击打开（原 DataGrid 是双击）
- 快捷方式右键菜单全部功能保留（编辑/删除/刷新/排序）
- 左侧导航按钮区域和底部状态条完全不变

### 编译验证
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj -c Debug`：0 错误，0 警告

---

## 2026-08-12 打包 v2.2.8 安装程序

### 操作内容
基于 v2.2.7 之后新增的「多条件查询」功能及此前多次 bug 修复（负单求和、现金账加载、销售条件查询跨字段、PinyinHelper 二级汉字等），重新打包生成 v2.2.8 安装程序。

### 版本变更
- 版本号：v2.2.7 → v2.2.8
- 同步更新位置：
  - [QP11_Setup.iss](file:///f:/qp11/QP11_Setup.iss#L7) 第 7 行 `#define MyAppVersion "2.2.8"`
  - [QP11.Wpf.csproj](file:///f:/qp11/src/QP11.Wpf/QP11.Wpf.csproj#L28) 第 28 行 `<Version>2.2.8</Version>`
- 注：`UpdateService.GetCurrentVersion()` 通过反射读取程序集版本，无需硬编码改动

### 打包步骤
1. 清理旧 `F:\qp11\Publish\` 目录
2. `dotnet publish F:\qp11\src\QP11.Wpf\QP11.Wpf.csproj -c Release -o F:\qp11\Publish`
   - 自包含发布（SelfContained=true，RuntimeIdentifier=win-x64，已在 csproj 配置）
   - 编译输出：680 个文件，QP11.Wpf.exe FileVersion=2.2.8.0
   - 编译警告：2 个 `SqlConnection` 过时警告（既有，与本打包无关）
   - robocopy 退出码 1（MSB3073 警告）= wwwroot 复制成功，属正常
3. `ISCC.exe F:\qp11\QP11_Setup.iss` 编译安装包（65.4 秒）

### 输出结果
- 安装包路径：`F:\qp11\InstallPackage\QP11Setup_v2.2.8.exe`
- 大小：75.89 MB
- FileVersion / ProductVersion：2.2.8
- Inno Setup 编译警告（3 个，均为既有问题，未引入）：
  - OnlyBelowVersion 6.1 的 quicklaunchicon 任务在新系统不处理（Vista/2008 已不支持）
  - PrivilegesRequired=admin 与 per-user 区域（userappdata）共存提示
  - [Code] 段 `ResultCode` 变量未使用（卸载询问删除 Data/logs 时未取返回值）

### 备注
- 旧安装包 `QP11Setup_v2.2.7.exe` 仍保留在 InstallPackage 目录，可手动删除
- 安装包内置 .NET 8.0 运行时，目标机器无需预装 .NET
- 安装时保留用户配置（appsettings.json、printsettings.json、Data 目录）不覆盖

---

## 2026-08-12 销售开单页新增「多条件查询」按钮

### 需求
在销售开单页（Mode A 开单模式）查询条件栏增加「多条件查询」按钮，放在「切换查询模式(F3)」按钮前面。点击弹窗让用户逐行输入多个配件编号，确定后一次性精确查询这些编号对应的配件并显示到配件列表。

### 操作内容
1. **新增弹窗** `MultiCodeQueryDialog.xaml(.cs)`：多行 TextBox（每行一个编码），确定时解析（去空行/Trim/去重），空则提示。仿 `InputBoxDialog` 模式。
2. **仓库层** `IPartRepository` / `PartRepository` 新增 `GetStockListByCodesAsync(IEnumerable<string> partNos)`：
   - 复用 `GetStockListAdvancedAsync` 的子查询+外层JOIN优化骨架（规避 SQL Server 2000 多列+ORDER BY 慢执行计划）
   - 子查询 `TOP 999999999` 满足 SQL Server 2000 ORDER BY 必须 TOP（坑 #5）
   - 只搜 `part_data.partno`，不跨字段（坑 #18）
   - 每个编码按 `LIKE '%code%'` 包含匹配，多个编码间 OR 关系；动态构建参数化 LIKE 条件，避免 SQL 注入
   - 按 500 个/批分片查询合并，规避 SQL Server 单条语句 2100 参数上限
3. **ViewModel** `SellViewModel` 新增 `LoadPartListByCodesAsync(codes, hideScrapPlace)`，复用现有「隐藏废品仓」过滤逻辑。
4. **UI** `SellControl.xaml` 查询条件栏在「匹配」下拉框与 `*` 列之间新增一个 Auto 列，将「多条件查询」按钮放在「匹配模式」下拉框后面（第 10 列），「切换查询模式(F3)」移到 `*` 列（第 11 列）右对齐。
5. **事件** `SellControl.xaml.cs` 新增 `BtnMultiCodeQuery_Click`：弹窗 → 查询 → 绑定 `dgParts`。

### 涉及文件
- [IPartRepository.cs](file:///f:/qp11/src/QP11.Core/Interfaces/IPartRepository.cs#L22-L23) 新增接口方法
- [PartRepository.cs](file:///f:/qp11/src/QP11.Data/Repositories/PartRepository.cs#L332-L376) 新增实现
- [MultiCodeQueryDialog.xaml](file:///f:/qp11/src/QP11.Wpf/Views/MultiCodeQueryDialog.xaml) 新增弹窗
- [MultiCodeQueryDialog.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/MultiCodeQueryDialog.xaml.cs) 新增弹窗代码
- [SellViewModel.cs](file:///f:/qp11/src/QP11.Wpf/ViewModels/SellViewModel.cs#L113-L122) 新增查询方法
- [SellControl.xaml](file:///f:/qp11/src/QP11.Wpf/Views/SellControl.xaml#L90-L93) 按钮插入
- [SellControl.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/SellControl.xaml.cs#L596-L614) 新增点击事件

### 行为约定
- 匹配方式：包含匹配（每个编码 `partno LIKE '%code%'`，多个编码 OR），不跨字段
- 输入容错：空行/空格忽略，重复编码去重，查不到的编码静默跳过
- 废品仓：沿用「隐藏废品仓」勾选状态过滤
- 查询后查询栏输入框保持原样，互不干扰
- 仅展示有库存记录的配件（INNER JOIN part_stock，与现有查询一致）
- 按钮仅在开单模式显示（查询栏整体属 Mode A）

### 编译验证
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj -c Debug`：0 错误，2 警告（均为既有 `MigrationService.cs` SqlConnection 过时警告，与本次改动无关）

---

## 2026-08-12 打包 v2.2.7 安装程序

### 操作内容
基于最近多次 bug 修复（负单求和、现金账加载、销售条件查询跨字段、PinyinHelper 二级汉字等），重新打包生成 v2.2.7 安装程序。

### 版本变更
- 版本号：v2.2.6 → v2.2.7
- 同步更新位置：
  - [QP11_Setup.iss](file:///f:/qp11/QP11_Setup.iss#L7) 第 7 行 `#define MyAppVersion "2.2.7"`
  - [QP11.Wpf.csproj](file:///f:/qp11/src/QP11.Wpf/QP11.Wpf.csproj#L28) 第 28 行 `<Version>2.2.7</Version>`
- 注：`UpdateService.GetCurrentVersion()` 通过反射读取程序集版本，无需硬编码改动

### 打包步骤
1. 清理旧 `F:\qp11\Publish\` 目录
2. `dotnet publish F:\qp11\src\QP11.Wpf\QP11.Wpf.csproj -c Release -o F:\qp11\Publish`
   - 自包含发布（SelfContained=true，RuntimeIdentifier=win-x64，已在 csproj 配置）
   - 编译输出：680 个文件，QP11.Wpf.exe FileVersion=2.2.7.0
   - 编译警告：2 个 `SqlConnection` 过时警告（既有，与本打包无关）
   - robocopy 退出码 1（MSB3073 警告）= wwwroot 复制成功，属正常
3. `ISCC.exe F:\qp11\QP11_Setup.iss` 编译安装包（63.8 秒）

### 输出结果
- 安装包路径：`F:\qp11\InstallPackage\QP11Setup_v2.2.7.exe`
- 大小：75.88 MB
- FileVersion / ProductVersion：2.2.7
- Inno Setup 编译警告（3 个，均为既有问题，未引入）：
  - OnlyBelowVersion 6.1 的 quicklaunchicon 任务在新系统不处理（Vista/2008 已不支持）
  - PrivilegesRequired=admin 与 per-user 区域（userappdata）共存提示
  - [Code] 段 `ResultCode` 变量未使用（卸载询问删除 Data/logs 时未取返回值）

### 备注
- 旧安装包 `QP11Setup_v2.2.6.exe` 仍保留在 InstallPackage 目录，可手动删除
- 安装包内置 .NET 8.0 运行时，目标机器无需预装 .NET
- 安装时保留用户配置（appsettings.json、printsettings.json、Data 目录）不覆盖

---

## 2026-08-12 修复应收款页面负数销售单据被当成正数相加

### 问题描述
应收款页面（`ArrearageControl` 模式`ls_flag=2`）汇总销售单据时，负数金额销售单（红字/负单）被当成正数相加，导致客户欠款合计虚高。经查历史记录可定位到具体客户，例如老东方会计合计应为 `-380`，页面显示为 `380`。

### 根因分析
`ArrearageRepository` 汇总 SQL 对退货/负额单据统一取反：
```sql
CASE WHEN bs.flag=2 OR bs.total<0 THEN -(arrearage.total - ISNULL(arrearage.charge,0)) ELSE ... END
```
但真实库（`qipei@192.168.1.86`）中存在两类负数数据：

- **`arrearage.total` 本身为负的记录共 92 条**（91 条关联 `flag=1` 但 `bill_sell.total<0` 的负金额正单 + 1 条未匹配单据）。它们的存储值已带负号，正确贡献就是 `total - charge`；原逻辑对其**再次取反**，把 `-230` 加成了 `+230`；
- `flag=2` 退货单对应的正数 `total` 记录，经查 `total > charge`（未结清退货）的记录为 **0 条**，全部已结清，取反不影响。

结论：取反规则只在"存储值为正、单据为退货"时才成立；当存储值已为负时不得再取反。

### 操作内容
在 `ArrearageRepository.cs` 全部 4 处查询的 `CASE` 最前面增加 `arrearage.total < 0`（或 `a.total < 0`）分支：**存储值已为负时按 `total - charge` 原样取值，不再取反**；其余分支（退货单取反、正常单原样）保持不变：

```sql
CASE WHEN arrearage.total < 0
        THEN arrearage.total - ISNULL(arrearage.charge,0)              -- 负数单：已带负号，不取反
     WHEN bs.flag=2 OR bs.total<0
        THEN -(arrearage.total - ISNULL(arrearage.charge,0))           -- 退货单：存储值为正，取反
     ELSE arrearage.total - ISNULL(arrearage.charge,0) END
```
明细查询（`je`/`charge`/`owe` 三个 CASE 与 `WHERE ... <> 0` 过滤）同步改造，保证**客户合计 = 明细合计**一致。

### 修改文件
- `src/QP11.Data/Repositories/ArrearageRepository.cs`
  - `GetClientArrearTotalAsync`（客户欠款合计，供校验/收款窗口显示）
  - `GetClientArrearageListAsync`（应收款/应付款客户列表 `total_je` 与 `HAVING` 过滤）
  - `GetArrearageDetailByBidAsync`（应收款页面明细 `je`/`charge`/`owe` 与过滤）
  - `GetListWithCalcAsync`（欠款历史窗口 `je`/`charge`/`owe`）

### 验证
- 已用新 SQL 在真实库复算对比：受影响客户由旧值 `+8945 → -3855`、`+1298 → -1298` 等（负号正确），明细 `old_owe=+230 → new_owe=-230`；
- 客户列表合计与明细合计按新公式抽样抽查全部一致；
- 全库仅下列客户合计将变为负数（净欠款为负，表示客户有净预存款/退货抵减余额），属正确结果：老东方 -380、金兴汽车修理厂、伟鹏汽修厂、城北汽修厂等。

### 编译结果
- `dotnet build QP11.Data\QP11.Data.csproj`：**0 错误 0 警告**

### 说明
- 修复原则：存储的 `arrearage.total` 已带符号时以其为准；仅当存储值为正且单据为退货/负额时才取反，防止旧数据约定下出现双重取反。
- `SellRepository`（`ConfirmArrearagePaymentAsync` 对负单确认收款用 `ABS(arrear)`、`GetArrearBillsAsync` 一键做账）语义为"结清负数欠款"，与本次修复不冲突，未改动。
- 若未来新代码将退货单欠款记录为负数（部分负单），本修复同样适用；建议后续新单据统一约定：`arrearage.total` 直接存储带符号金额。

## 2026-08-12 删除遗留死代码 AccountWindow（账户管理旧窗口）

### 问题描述
`src/QP11.Wpf/Views/AccountWindow.xaml` 与 `AccountWindow.xaml.cs` 为"账户管理"功能的早期独立窗口版（`Window`，标题"账户管理"），已被 `AccountControl`（主窗口内 6 个账户标签页：现金账/银行账/支付宝账/微信账/运费账/日结账）完全替代。

### 根因分析
全代码库无任何 `new AccountWindow()`、DI 注册或 XAML 引用，且未实现 `ITabContent` 接口，无法进入主窗口标签页体系，属于纯死代码。

### 操作内容
- 删除 `src/QP11.Wpf/Views/AccountWindow.xaml`
- 删除 `src/QP11.Wpf/Views/AccountWindow.xaml.cs`

### 修改文件
- 删除 `src/QP11.Wpf/Views/AccountWindow.xaml`
- 删除 `src/QP11.Wpf/Views/AccountWindow.xaml.cs`

### 编译结果
- `dotnet build QP11.sln -c Debug`：**0 错误 0 警告**

### 说明
- csproj 为 SDK 风格自动包含，无需改动；删除前已确认无任何源代码引用（`.trae/specs` 下的历史规划文档与 `_tmp` 编译缓存不涉及源码，未改动）。

## 2026-08-12 修复现金账/银行账页面"加载账目失败"（dynamic 的 Sum 误选 int 重载）

### 问题描述
现金账、银行账两个页面（均为 `AccountControl`）打开时报错：`加载账目失败: Cannot implicitly convert type 'decimal' to 'int'. An explicit conversion exists (are you missing a cast?)`。

### 根因分析
`AccountControl.LoadAccountsAsync` 中统计收支合计使用了动态 LINQ：
```csharp
var sumIncome = data.Sum(r => r.income == null ? 0m : Convert.ToDecimal(r.income));
var sumExpense = data.Sum(r => r.expense == null ? 0m : Convert.ToDecimal(r.expense));
```
`data` 是 `List<dynamic>`（Dapper `QueryAsync<dynamic>` 返回）。lambda 内 `r.income` 为 dynamic，导致三元表达式整体为 dynamic、lambda 返回类型为 `dynamic`。运行时绑定器解析 `Enumerable.Sum` 重载时误选了返回 `int` 的重载，实际执行返回 `decimal` 时因"decimal 无法隐式转 int"抛出 `RuntimeBinderException`，被 catch 捕获显示"加载账目失败"。

已通过连接生产库（qipei@192.168.1.86）复现验证：
- `account.flag` 为 `Int32`，`income`/`expense`（SQL `CASE flag WHEN 1 THEN charge ELSE 0 END`）为 `Decimal`；
- 原 `Sum` 写法抛 `RuntimeBinderException`，报错消息与用户完全一致；
- `(int?)row.flag` 强转本身正常（flag 列类型无误），报错点仅在 `Sum` 两行。

### 操作内容
将两处动态 `Sum` 改为与文件内既有遍历风格一致的 `foreach` 显式累加（`decimal` 静态类型，规避 dynamic 重载绑定）：
```csharp
decimal sumIncome = 0m;
decimal sumExpense = 0m;
foreach (var r in data)
{
    if (r.income != null) sumIncome += Convert.ToDecimal(r.income);
    if (r.expense != null) sumExpense += Convert.ToDecimal(r.expense);
}
```

### 修改文件
- `src/QP11.Wpf/Views/AccountControl.xaml.cs`（`LoadAccountsAsync` 第55-56行）

### 编译结果
- `dotnet build QP11.sln -c Debug`：**0 错误 0 警告**

### 说明
- 实测修复后遍历 32 万余条记录正常合计（sumIncome=69358168.40，sumExpense=56951947.92），性能可接受（与 DataGrid 分页绑定 `ItemsSource` 为全部行）。
- `AccountWindow.xaml.cs` 原存在完全相同的 `Sum` 写法，已于同日作为遗留死代码删除（见上方"删除遗留死代码 AccountWindow"记录）。

## 2026-08-12 移除 DataGrid 方向键/选择调试日志（DEBUG-ETT/DG/↓）

### 操作内容
移除销售开单页（SellControl）方向键导航、DataGrid 选择/按键调试期间临时添加的 `Debug.WriteLine` 日志，共 7 处：

| 文件 | 移除内容 |
|------|---------|
| `src/QP11.Wpf/Helpers/EnterToTabHelper.cs` | `OnPreviewKeyDown` 中 `[DEBUG-ETT] 方向键穿透!` 日志。该 `else if (e.Key == Key.Down \|\| e.Key == Key.Up)` 分支仅含此日志，整支删除 |
| `src/QP11.Wpf/Views/SellControl.xaml.cs` | 查询框↓键进表格流程中 2 处 `[DEBUG-↓]` 日志（`cell=...` / `FocusedElement=...`），保留 `cell?.Focus()` 功能代码 |
| `src/QP11.Wpf/Views/SellControl.xaml.cs` | `DgParts_PreviewKeyDown` 中 `[DEBUG-DG隧道]` 日志 |
| `src/QP11.Wpf/Views/SellControl.xaml.cs` | 空方法 `DgParts_KeyDown`（原 `[DEBUG-DG冒泡]` 日志移除后无功能代码，连同 XAML `KeyDown="DgParts_KeyDown"` 绑定一并删除） |
| `src/QP11.Wpf/Views/SellControl.xaml.cs` | `DgParts_SelectionChanged` 中 `[DEBUG-DG选择]` 日志 |
| `src/QP11.Wpf/Views/SellControl.xaml.cs` | "添加配件到明细"流程中 `[DEBUG] 新增明细 Memo` 日志（经用户确认一并移除） |

### 修改文件
- `src/QP11.Wpf/Helpers/EnterToTabHelper.cs`
- `src/QP11.Wpf/Views/SellControl.xaml.cs`
- `src/QP11.Wpf/Views/SellControl.xaml`（移除 `DgParts_KeyDown` 事件绑定）

### 编译结果
- `dotnet build QP11.sln -c Debug`：**0 错误**；2 个既有警告（MigrationService CS0618 SqlConnection 过时），与本次改动无关

### 说明
功能代码保持不变（↓键进入表格、F4/Enter 双击、左右键切查询框、选中行加载图片等逻辑均未改动）。全库搜索确认 `[DEBUG-` 前缀日志已无残留。`UpdateService.cs` 中的 `[Update]` 日志与 `BuyReturnWindow.xaml.cs` 的 `刷新单据列表失败` 日志不属于本次范围，保留。

## 2026-08-12 移除页面切换/查询性能调试日志（PERF/QUERY）

### 操作内容
按此前约定（卡顿修复验证后清理），移除 2026-08-09 "添加页面切换/查询性能调试日志" 中埋设的全部 `[PERF]` 性能耗时日志与 `[QUERY]` 数据库查询日志（含关联的 Stopwatch 计时代码）。卡顿问题已确认修复（9.5秒→9毫秒），日志已完成使命。

> 范围确认：经用户确认，仅移除 `[PERF]`/`[QUERY]` 调试日志；保留 `App.xaml.cs` 中 `[ERR] 后台任务未处理异常` 全局错误日志，保留 Serilog 的 `.WriteTo.Debug` 输出与 `MinimumLevel.Information()` 配置。

### 修改文件

| 文件 | 移除内容 |
|------|---------|
| `src/QP11.Data/Infrastructure/DbConnectionFactory.cs` | `CreateAsync` 中 `OpenAsync` 耗时 `[PERF]` 日志 + Stopwatch |
| `src/QP11.Data/Repositories/BaseRepository.cs` | `CreateConnectionAsync` 中 `OpenAsync` 耗时 `[PERF]` 日志（含仓储类名）+ Stopwatch |
| `src/QP11.Data/Repositories/PartRepository.cs` | `CreateConnectionAsync` 耗时 `[PERF]` 日志；`GetStockListAsync`/`GetStockListAdvancedAsync` 的 `[QUERY]` 信息日志、`[QUERY-EMPTY]` 警告日志（空结果打印SQL用于SSMS验证）+ Stopwatch |
| `src/QP11.Wpf/ViewModels/SellViewModel.cs` | `LoadPartListAsync` 简单/高级查询分支、废品仓过滤前后行数 `[PERF]` 日志（`beforeFilter` 变量一并清除） |
| `src/QP11.Wpf/Views/MainWindow.xaml.cs` | `OpenTab` 新建/切换已存在Tab `[PERF]` 日志；`OpenFunctionTab` 创建控件/总耗时 `[PERF]` 日志（swTotal/swCreate 一并清除） |
| `src/QP11.Wpf/Views/SellControl.xaml.cs` | `LoadDropdowns` 3个下拉并行查询 `[PERF]` 日志；`LoadPartList` 查询参数/返回行数/耗时 `[PERF]` 日志 |

### 例外保留
- `SellControl.LoadPartList` 的 catch 块保留异常日志 `Serilog.Log.Warning(ex, "加载配件列表失败")`，仅去掉其中的耗时参数（属错误日志，非调试日志）
- `SellControl.LoadDropdowns` 的 catch 块 `Serilog.Log.Warning(ex, "LoadDropdowns 失败")` 原样保留

### 编译结果
- `dotnet build QP11.sln -c Debug`：**0 错误**；2 个既有警告（MigrationService CS0618 SqlConnection 过时），与本次改动无关

### 说明
功能代码保持不变，仅删除调试日志埋点与计时变量，未引入任何新依赖。VS 输出窗口中的 `已加载 "xxxx.dll"` 行属于 Visual Studio 调试器模块加载提示（"工具→选项→调试→输出窗口→模块加载消息"），非应用代码，无法通过改代码移除。

## 2026-08-12 移除 SellEditDialog 拼音搜索调试日志

### 操作内容
按此前约定（方案B验证前清理），移除 `src/QP11.Wpf/Views/SellEditDialog.xaml.cs` 中标记 `调试日志-排查拼音搜索Bug` 的 3 处调试日志（Serilog，Information 级别）：
1. `SellEditDialog_Loaded`：GetAllAsync 加载完成后的客户数/含振东鑫凯日志
2. `ResolveClientNames` 入口：输入与 `_allClients.Count` 记录、振东鑫凯8 探测块（Cid/Name/NamePy/CachePy）
3. `ResolveClientNames` 拼音匹配后：匹配结果列表日志

### 修改文件
- `src/QP11.Wpf/Views/SellEditDialog.xaml.cs`：删除上述 3 处调试日志；顺带修正一处 try 块闭合大括号的缩进

### 编译结果
- 0 错误；2 个既有警告（MigrationService CS0618 SqlConnection 过时），与本次改动无关

### 说明
调试日志已完成使命（成功定位根因为 PinyinHelper 二级汉字区未覆盖），现按约定删除。功能代码保持不变，未额外引入任何修改。

## 2026-08-12 全局修复 PinyinHelper 二级汉字拼音首字母（方案B，根因修复）

### 问题描述
SellEditDialog 输入"zdxk"查不到客户"振东鑫凯8"（cid=01376，name_py=`ZDXK8`）。调试日志确认：`_allClients` 已加载该客户，但 `CachePy=[zdk8]`（漏掉"鑫"的 X），导致 `ResolveClientNames` 的 `py.Contains("zdxk")` 不命中。

### 根因分析
`PinyinHelper.ComputePinyin` 仅用 GB2312 码点区间表覆盖**一级汉字区**(0xB0A1-0xD7F9，按拼音排序)。**二级汉字区**(0xD8A1-0xF7FE，按部首排序，共 3008 字)完全未处理，被静默丢弃。
- "鑫"的 GB2312 码点为 0xF6CE，属二级区，被丢弃 → "振东鑫凯8" 缓存为 `zdk8` 而非 `zdxk8`
- SellEditDialog 用 `_pinyinCache.GetValueOrDefault(c.Name) ?? c.NamePy ?? ""`，缓存优先且非空时不会回退到正确的数据库 name_py
- ClientSearchBox 用 `缓存.StartsWith || NamePy.StartsWith` 双通道回退，故不受影响
- 影响面评估（pypinyin 模拟）：3276 客户中 421 个(12.8%)含二级汉字，缓存均与 name_py 不一致

### 修复方案（方案B：直接全局修 PinyinHelper）
1. 用 pypinyin（Python 权威 Unicode 汉字注音库）遍历 GB2312 二级区 3008 个码点，生成首字母查找表 `Level2Initials`（3008 字符，按 区号(high-0xD8)*94+位号(low-0xA1) 索引）
2. 生成后程序校验：长度 3008、全 A-Z、关键字 鑫→X/闫→Y/窦→D/圳→Z 全通过（闫/窦/圳 正是旧算法无法识别的字）
3. `ComputePinyin` 抽取 `GetChinesePinyinInitial(char)` 方法：一级汉字沿用码点区间，二级汉字查 `Level2Initials` 表
4. 同步修复 `MigrationService.GetChineseInitial`（同源算法，避免未来迁移生成错误的 name_py）

### 修改文件
- `src/QP11.Wpf/Helpers/PinyinHelper.cs`：嵌入 `Level2Initials`(3008字)、抽取 `GetChinesePinyinInitial` 方法、`ComputePinyin` 改为调用该方法
- `src/QP11.Services/MigrationService.cs`：嵌入同源 `Level2Initials` 字段、`GetChineseInitial` 末尾加二级区查表

> 注：两处 `Level2Initials` 表内容完全一致（同源），因架构限制(WPF 依赖 Services，不可反向引用)各自维护一份，注释已互相标注需同步。

### 编译结果
- 0 错误；4 个既有警告（PartRepository CS8603 ×2、MigrationService CS0618 SqlConnection 过时 ×2），均与本次改动无关

### 待用户验证
1. 打开销售编辑窗口，输入"zdxk" → 应能查到"振东鑫凯8"的销售历史
2. 输入"zdxk8" → 同样应能查到
3. 其他含二级汉字的客户（如含"闫/窦/圳/钛/镁"等字）拼音搜索应恢复正常
4. 验证通过后，可删除 SellEditDialog.xaml.cs 中标记为 `调试日志-排查拼音搜索Bug` 的 3 处调试日志

### 临时文件
已清理：生成表用的 Python 脚本和中间 txt 文件（tmp_verify_pinyin_table.py 等 5 个），表来源已在代码注释中记录（pypinyin 生成）

## 2026-08-12 为 SellEditDialog 拼音搜索问题添加调试日志（定位用，暂未修复）

### 问题描述
SellEditDialog 输入"zdxk"查不到客户（此前已确认数据库实际客户为"振东鑫凯8"，name_py=`ZDXK8`，有 341 条销售历史），需加日志复现，定位是"客户列表没加载"还是"匹配但无该配件历史"。

### 根因分析
数据库验证结论：
- `client_infor` 中不存在"振动鑫凯8"，实际为 **"振东鑫凯8"**（cid=01376，name_py=`ZDXK8`）
- `PinyinHelper.GetPinyinInitials("振东鑫凯8")` = `ZDXK8`，与 name_py 一致，拼音匹配逻辑本身没问题
- 该客户有 341 条 detail_sell 明细、290 张 bill_sell（2014-12-25 ~ 2026-07-31），排除"无历史"的大前提

### 本次改动（调试日志）
在 `src/QP11.Wpf/Views/SellEditDialog.xaml.cs` 添加 3 处日志（Serilog，Information 级别）：
1. `SellEditDialog_Loaded`：GetAllAsync 加载完成后记录客户总数、是否含"振东鑫凯"
2. `ResolveClientNames` 入口：记录输入文本、`_allClients.Count`、振东鑫凯8 的 Cid/Name/NamePy/缓存拼音
3. `ResolveClientNames` 拼音匹配后：记录匹配到的客户名列表数量及内容

日志位置：程序运行目录下 `logs/qp11-YYYYMMDD.log`（滚动日切）

### 编译结果
- 0 错误；2 个 CS8603 警告为既有警告，与本次改动无关

### 下一步
运行程序复现"输入 zdxk 查不到"，查看当日日志判断：
- 若 `_allClients.Count` 很大但"振东鑫凯 不在 _allClients 中" → GetAllAsync 数据问题
- 若 `_allClients.Count=0` → 加载失败（静默 catch 吞掉异常，需修加载）
- 若匹配结果含"振东鑫凯8"但历史网格仍空 → 该配件对该客户确实无历史（业务上正确），需与用户确认预期

## 2026-08-12 修复销售编辑窗口客户拼音搜索找不到（振动鑫凯8/ZDXK）

### 问题描述
销售编辑窗口（SellEditDialog）的客户查询输入框中，输入拼音"ZDXK"查不到客户"振动鑫凯8"。

### 根因分析
`ResolveClientNames` 方法的拼音匹配逻辑仅依赖 `_pinyinCache`（通过 `PinyinHelper.GetPinyinInitials` 在内存中计算拼音首字母）。如果 `ComputePinyin` 对某些汉字的 GB2312 编码范围判断不准确导致缓存缺失，或者 `_pinyinCache` 因其他原因未命中，则拼音匹配会失败。

`client_infor` 表本身有 `name_py` 字段（数据库存储的拼音码），但原代码未使用此字段作为后备。

### 修复方案
在 `ResolveClientNames` 的拼音匹配循环中，增加数据库 `name_py` 字段作为后备：
- 优先用 `_pinyinCache` 的缓存值
- 如果缓存没有该客户的拼音，回退到 `ClientInfor.NamePy`（数据库 `name_py` 字段）

### 修改文件
- `src/QP11.Wpf/Views/SellEditDialog.xaml.cs`：`ResolveClientNames` 方法中拼音匹配逻辑改为 `_pinyinCache.GetValueOrDefault(c.Name) ?? c.NamePy ?? ""`

### 编译结果
- 0 错误，0 新增警告

### 待用户验证
1. 打开销售编辑窗口，输入拼音"ZDXK" → 应能查到客户"振动鑫凯8"的销售历史
2. 其他客户拼音搜索不受影响

## 2026-08-12 修复销售开单条件查询区字段跨字段搜索

### 问题描述
销售开单页面的条件查询区（配件编号、名称、车型、分类）存在跨字段搜索问题：
1. **配件编号**输入框能搜到**名称**字段的数据
2. **名称**输入框能搜到**车型**（carname）字段的数据

### 根因分析
`PartRepository.GetStockListAdvancedAsync` 方法中 SQL 条件构建时，每个字段的 WHERE 条件包含了其他字段的列：
- 配件编号条件：`part_data.partno OR part_data.name`（中文）/ `part_data.partno OR part_data.name_py`（ASCII）
- 名称条件：`part_data.name OR part_data.carname OR part_data.memo OR part_data.[class]`

### 修复方案
每个查询字段只搜索自己的数据库列，不跨字段搜索：
- 配件编号：只搜 `part_data.partno`
- 名称：只搜 `part_data.name`（+ `name_py` 支持拼音缩写）
- 车型/分类：原有逻辑不变（只搜自己的列）

### 修改文件
- `src/QP11.Data/Repositories/PartRepository.cs`：精简 `GetStockListAdvancedAsync` 中 partNo 和 partName 的 SQL 条件，去掉跨字段 OR 条件

### 编译结果
- 0 错误，0 警告

### 待用户验证
1. 配件编号输入"大灯" → 不应返回名称含"大灯"的结果，只返回编号含"大灯"的结果
2. 名称输入"凯越" → 不应返回车型含"凯越"的结果，只返回名称含"凯越"的结果
3. 拼音缩写搜索（如名称输入"DG"）仍应能搜到"大灯"

## 2026-08-11 打包 exe 安装程序 v2.2.6

### 操作内容
将项目打包为 exe 安装程序，包含本次数据迁移模块修复（detail_sell/detail_buy 车型列不显示、采购明细品牌名遗漏、库存多仓位信息丢失、配件仓位未填充）。

### 版本号变更
- `src/QP11.Wpf/QP11.Wpf.csproj`：`<Version>` 2.2.5 → 2.2.6
- `QP11_Setup.iss`：`#define MyAppVersion` 2.2.5 → 2.2.6

### 打包流程
1. **dotnet publish**：`dotnet publish src\QP11.Wpf\QP11.Wpf.csproj -c Release -o Publish --self-contained true -r win-x64`
   - 自包含发布（内置 .NET 8 运行时，目标机器无需预装环境）
   - 输出目录：`f:\qp11\Publish\`
   - 结果：成功，0 错误（仅既有 CS8603/CS0618 警告）
2. **Inno Setup 编译**：`& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "f:\qp11\QP11_Setup.iss"`
   - 编译耗时：46.3 秒
   - 压缩方式：lzma2/ultra64 + SolidCompression

### 产物
- 安装包路径：`f:\qp11\InstallPackage\QP11Setup_v2.2.6.exe`
- 大小：75.89 MB
- 生成时间：2026-08-11 14:25

### 本次包含的修改
1. 修复 detail_sell/detail_buy 迁移遗漏 cartype 字段（车型列不显示）
2. 修复 detail_buy 迁移遗漏 carname 字段（品牌名不显示）
3. 修复 part_stock 多仓位信息丢失（place 字段拼接为 "仓A,仓B"）
4. 修复 part_data.place 未填充（从 tbisto.posi 填充仓位）

### 注意事项
- 安装脚本排除 `appsettings.json`、`printsettings.json`、`Data\*`，重装时不覆盖用户配置和业务数据
- 卸载时询问是否删除 `Data` 和 `logs` 目录，默认保留用户业务数据

---

## 2026-08-11 修复库存多仓位信息丢失及配件仓位未填充

### 问题描述
数据库迁移字段对比报告发现两个仓位相关问题：
- **#6**：`MigrateStock` 按 nno 分组时只取首个仓位（`g.First().posi`），源库 `tbisto` 中 36 个配件（0.14%）有多仓位多行，迁移后多仓位信息丢失
- **#7**：`MigrateParts` 未关联 `tbisto` 获取仓位，`part_data.place` 字段未填充

### 根因分析
通过查询源库确认：
- 源库 `tbisto` 中有 36/25504 个配件有多仓位（如 `C-1529/492-95` 有 3 个仓位：空、空、店）
- 目标库 `part_stock` 主键为单列 `(partid)`，应用代码约 10 个文件使用 `WHERE partid=@partid` 查询期望单行返回
- 目标库 `part_data.place` 为 varchar(60)，`part_stock.place` 为 varchar(20)

### 方案选型过程
1. **改主键为 (partid, place) 多行方案** — 否决：会破坏约 10 个应用文件的单行查询假设
2. **拆分成多个配件方案** — 否决：破坏 partno 业务唯一性，且进销明细 posi 为空时无法分配
3. **拼接仓位到 place 字段方案** — 采用：无 schema 变更，无应用代码风险

### 修复方案

**#6 MigrateStock 多仓位拼接**（[MigrationService.cs#L636-L654](file:///f:/qp11/src/QP11.Services/MigrationService.cs#L636)）：
- 分组逻辑修改：`posi` 从 `g.First().posi` 改为 `string.Join(",", g.Select(...).Where(非空).Distinct())`
- 多仓位拼接为 "仓A,仓B,仓C" 格式（去空去重，按首次出现顺序）
- INSERT 参数增加长度截断防护：`place = row.posi.Length > 20 ? row.posi[..20] : row.posi`
- 数量仍为各仓位求和（原逻辑保持）

**#7 MigrateParts 填充 part_data.place**（[MigrationService.cs#L315-L326](file:///f:/qp11/src/QP11.Services/MigrationService.cs#L315)）：
- MigrateParts 开头预加载 `tbisto` 仓位映射（按 nno 聚合，多仓位拼接，与 #6 逻辑一致）
- INSERT 语句列列表和 VALUES 增加 `place` 列/参数
- 长度截断防护：`place = place.Length > 60 ? place[..60] : place`（part_data.place varchar(60)）
- 仓位来源：`tbisto.posi`（实际仓位数据，`tbposi` 仅 5 行测试数据不可用）

### 字段映射
| 目标表.字段 | 源库字段 | 说明 |
|------------|---------|------|
| part_stock.place | tbisto.posi (多行拼接) | varchar(20)，截断到 20 |
| part_data.place | tbisto.posi (多行拼接) | varchar(60)，截断到 60 |

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | `MigrateStock()`：分组 posi 改为多仓位拼接 + INSERT place 长度防护 |
| `src/QP11.Services/MigrationService.cs` | `MigrateParts()`：预加载 tbisto 仓位映射 + INSERT 添加 place 列 |

### 编译结果
`dotnet build f:\qp11\src\QP11.Services\QP11.Services.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS0618/CS8603，非本次引入）

### 影响范围
- **part_stock**：36 个多仓位配件的 place 字段从单一仓位变为 "仓A,仓B" 拼接，其余 25468 个配件不受影响
- **part_data**：所有配件的 place 字段从 NULL 变为实际仓位值
- **应用代码**：无影响（partno 唯一性保持，partid 单行查询保持）
- **进销历史**：不受影响（detail_buy/detail_sell 各自保留原始 posi，与 part_stock 无关）

### 待用户验证
重新运行数据迁移后：
1. part_stock 中 36 个多仓位配件的 place 字段应显示 "仓A,仓B" 拼接格式
2. part_data 的 place 字段应显示实际仓位（非 NULL）
3. 配件列表/库存查询等界面功能正常

---

## 2026-08-11 修复采购明细迁移遗漏 carname 字段（品牌名）

### 问题描述
数据库迁移字段对比报告发现：`detail_buy.carname`（品牌名）字段在迁移时未填充，源库 `tbistoed.cxnno`（char(18)）有数据但未被读取写入目标库。

### 根因分析
通过查询源库 `opt` 数据库 schema 确认目标表 `detail_buy` 有 `carname varchar(200)` 列，源表 `tbistoed` 有 `cxnno char(18)` 字段（与配件主表 `tbprnoty.cxnno` 同字段，已正确迁移至 `part_data.carname`）。

但 `MigrationService.cs` 的 `MigratePurchases()` 方法中 `detail_buy` INSERT 语句**遗漏了 `carname` 字段**（与之前修复的 `cartype` 遗漏属同类问题）。

### 修复方案
在 `MigratePurchases()` 的明细循环中：
1. 读取源行 `cxnno` 字段：`var cxnno = ((string?)row.cxnno ?? "").Trim();`
2. `detail_buy` INSERT 语句列列表和 VALUES 中增加 `carname` 参数（截断到 200 字符以匹配目标列长度）

字段映射：`tbistoed.cxnno` (char 18) → `detail_buy.carname` (varchar 200)，与 `MigrateParts()` 中 `tbprnoty.cxnno → part_data.carname` 的映射保持一致。

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | `MigratePurchases()`（detail_buy INSERT）：新增 `cxnno` 读取 + `carname` 列/参数 |

### 编译结果
`dotnet build f:\qp11\src\QP11.Services\QP11.Services.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS0618/CS8603，非本次引入）

### 采购退货 tsn 处理（本次对比发现的另一潜在问题，经用户确认不修复）
对比报告第2项指出 `MigratePurchases` 采购退货未处理 tsn（销售退货通过 `tbsada.ypaper` 关联原单）。经源库数据查询：
- 源库 `tbistoed` 中**无字段存储原采购单号**：`seano` 字段始终等于 `cno` 自身（6013 条退货明细 100% 自匹配），`pno` 全部为空
- 采购退货在源库中是独立单据，不通过字段关联原采购单
- 用户确认：保持 `detail_buy.tsn` 为空（当前代码已符合，`detail_buy` INSERT 未包含 tsn 列，tsn 为 NULL）

### 待用户验证
重新运行数据迁移后，打开任一迁移来的采购单详情窗，明细列表的「品牌」列应显示对应品牌名（与「车型」列同时正常显示）。

---

## 2026-08-11 修复数据迁移后单据详情窗口车型列不显示

### 问题描述
数据迁移后，在单据详情窗口（`OrderDetailWindow`）中，销售单/采购单明细列表的「车型」列全部不显示（空白）。

### 根因分析
`OrderDetailWindow` 通过 SQL `SELECT partno, name, cartype, ... FROM detail_sell/detail_buy` 查询车型字段并绑定到 DataGrid 的「车型」列（此列于 2026-08-11 新增，见下方「单据详情窗口新增车型列」记录）。

通过查询目标库 schema 确认 `detail_buy`（cartype varchar(200)）和 `detail_sell`（cartype varchar(200)）两表均有 `cartype` 列。但连接源库查询第三方 schema（`Tools/SchemaQuery/third_party_schema.txt`）确认源表 `tbistoed`（采购明细）和 `tbsada`（销售明细）均有 `ty` 字段（char(14)，车型，与配件主表 `tbprnoty.ty` 同字段）。

排查 `MigrationService.cs` 的 `MigratePurchases()` 和 `MigrateSales()` 方法，发现两处明细 INSERT 语句均**遗漏了 `cartype` 字段**：
- `detail_buy` INSERT 列表：`sn, partid, partno, name, amount, unit, inprice, ...`（无 cartype）
- `detail_sell` INSERT 列表：`sn, partid, partno, name, amount, unit, price, ...`（无 cartype）

迁移时源行的 `ty` 字段未被读取，目标表 `cartype` 列保持默认 NULL，导致详情窗口车型列查询结果为空。

对比 `MigrateParts()`（配件主表迁移）已正确读取 `ty` 并写入 `part_data.cartype`，明细表迁移遗漏了同一字段。

### 修复方案
在 `MigratePurchases()` 和 `MigrateSales()` 的明细循环中：
1. 读取源行 `ty` 字段：`var ty = ((string?)row.ty ?? "").Trim();`
2. `detail_buy`/`detail_sell` INSERT 语句列列表和 VALUES 中增加 `cartype` 参数（截断到 200 字符以匹配目标列长度）

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | `MigratePurchases()`（detail_buy INSERT）：新增 `ty` 读取 + `cartype` 列/参数；`MigrateSales()`（detail_sell INSERT）：同上 |

### 编译结果
`dotnet build f:\qp11\src\QP11.Services\QP11.Services.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS0618/CS8603，非本次引入）

### 待用户验证
重新运行数据迁移后，打开任一迁移来的销售单/采购单详情窗，明细列表「车型」列应显示对应车型（有值的显示车型，无值的显示空白）。

### 补迁移说明
已迁移过的库重复运行迁移时，`bill_buy`/`bill_sell` 的 `existSn > 0` 跳过逻辑（第751-753行/第889-891行）会跳过已存在的单据，**不会补插已存在单据的明细车型**。如需修复存量迁移数据的车型列，需手动执行 UPDATE 或先删除目标库的 detail_buy/detail_sell 后重新迁移。

---

## 2026-08-11 打包 exe 安装程序 v2.2.5

### 操作内容
将项目打包为 exe 安装程序，包含本次代码修改（单据详情窗口新增车型列 + 日期精确到秒、报损单客户列空白修复）。

### 版本号变更
- `src/QP11.Wpf/QP11.Wpf.csproj`：`<Version>` 2.2.4 → 2.2.5
- `QP11_Setup.iss`：`#define MyAppVersion` 2.2.4 → 2.2.5

### 打包流程
1. **dotnet publish**：`dotnet publish src\QP11.Wpf\QP11.Wpf.csproj -c Release -o Publish --self-contained true -r win-x64`
   - 自包含发布（内置 .NET 8 运行时，目标机器无需预装环境）
   - 输出目录：`f:\qp11\Publish\`
   - 结果：成功，0 错误（仅既有 CS8603/CS0618 警告）
2. **Inno Setup 编译**：`"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" QP11_Setup.iss`
   - 编译耗时：51.1 秒
   - 压缩方式：lzma2/ultra64 + SolidCompression

### 产物
- 安装包路径：`f:\qp11\InstallPackage\QP11Setup_v2.2.5.exe`
- 大小：75.9 MB
- 生成时间：2026-08-11 13:38

### 注意事项
- 安装脚本排除 `appsettings.json`、`printsettings.json`、`Data\*`，重装时不覆盖用户配置和业务数据
- 卸载时询问是否删除 `Data` 和 `logs` 目录，默认保留用户业务数据

---

## 2026-08-11 单据详情窗口新增车型列 + 日期精确到秒

### 需求
单据详情窗口（`OrderDetailWindow`）：
1. 明细列表新增「车型」显示列
2. 单据头日期显示改为精确到秒

### 修改内容
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/OrderDetailWindow.xaml` | 明细 DataGrid 在「配件名称」后新增「车型」列（Width=90，绑定 `Cartype`） |
| `src/QP11.Wpf/Views/OrderDetailWindow.xaml.cs` | `OrderDetailItem` 新增 `Cartype` 属性；销售明细 SQL 与采购明细 SQL 均增加 `cartype` 字段；`LoadSellOrder`/`LoadBuyOrder` 日期格式由 `yyyy-MM-dd` 改为 `yyyy-MM-dd HH:mm:ss` |

### 说明
- 已连库确认 `detail_sell`、`detail_buy` 两表均有 `cartype` 列（varchar(200)），销售/采购详情通用
- 销售单和采购单共用同一窗口与 DataGrid，车型列对两种单据同时生效

### 编译结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS8603/CS0618，与本次改动无关）

### 待用户验证
打开任一销售单/采购单详情窗：
1. 明细列表应显示「车型」列（有值的显示车型，无值的显示空白）
2. 单据信息区的日期应显示到秒（如 `2026-08-11 09:30:45`）

---

## 2026-08-11 修复报损单客户列空白（写入端根治 + 存量数据修复）

### 问题描述
报损管理报损配件后，在销售开单的查询模式下客户列为空白，正确应该显示为"配件报损"。

### 数据库实证分析（连接 qipei 库，192.168.1.86,1433）
分析 `bill_sell WHERE flag=3` 全部 2575 张报损单的写入字段，发现历史惯例与新 WPF 写入不一致：

| 时间段 | client 写入值 | 单数 | 说明 |
|--------|-------------|------|------|
| 2018-03 ~ 2023-07-19 | `02288` | 1653 | `client_infor` 中 cid=02288，name=**配件报损** |
| 2023-07-19 ~ 2026-08-04 | `03136` | 916 | `client_infor` 中 cid=03136，name=**配件报损**（最新惯例） |
| 2026-08-10 新 WPF 报损模块 | `''` / NULL | 5 | **写入错误**：00303997~00304002 |

`client_infor` 表中存在 3 个特殊客户（cid=02288/03136/03346），name 均为"配件报损"，name_py 均为 PJBS。

### 根因分析
**历史 PB 系统的报损单 `client` 字段写入的是特殊客户"配件报损"的真实 cid**（早期 02288，2023-07 后 03136），查询时 `LEFT JOIN client_infor` 自然显示"配件报损"。

当前 `BaosunRepository.InsertBillAsync` 的 INSERT SQL 写死 `client=''`（此前"修复配件报捷新单数据格式与旧单不一致"时误将 client 置为空），导致新报损单匹配不到客户 → 各页面客户列空白。此前在查询端到处加 `CASE WHEN flag=3 THEN '配件报损'`（PartQueryService / SellEditDialog / OrderDetailWindow）只是打补丁，**未从数据源头根治**。

### 修复方案（数据驱动，写入端根治）
1. **写入端修复**：报损单保存时 `client` 写入固定特殊客户 cid `03136`（2023-07-19 至今的最新惯例），与历史数据一致。
   - `BusinessConstants` 新增常量 `BaosunClientId = "03136"`
   - `BillBaosun` 实体新增 `Client` 属性映射
   - `BaosunRepository.InsertBillAsync` SQL：`VALUES (@Sn, '', ...)` → `VALUES (@Sn, @Client, ...)`
   - `BaosunViewModel.SaveBillAsync`：保存前设置 `bill.Client = BusinessConstants.BaosunClientId`
2. **存量数据修复**：UPDATE 5 张 client 为 NULL/空的报损单（00303997/00303998/00303999/00304001/00304002）的 client 为 `03136`，修复后 JOIN `client_infor` 验证 ClientName 均显示"配件报损"。
3. **查询端不再新增补丁**：数据正确后，销售开单查询模式（`SellViewModel.SearchBillsAsync`、`DgBills_SelectionChanged` 头部客户栏）及销售明细页经 `LEFT JOIN client_infor` 自然显示"配件报损"；此前已加的 CASE WHEN 补丁保留作防御性代码，不影响本次修复。

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Core/Constants/BusinessConstants.cs` | 新增常量 `BaosunClientId = "03136"`（含注释说明历史惯例 02288→03136） |
| `src/QP11.Core/Entities/BillBaosun.cs` | `BillBaosun` 新增 `[Column("client")] public string? Client` 属性 |
| `src/QP11.Data/Repositories/BaosunRepository.cs` | `InsertBillAsync` SQL：client 由空字符串 `''` 改为参数 `@Client`；补充注释说明历史惯例 |
| `src/QP11.Wpf/ViewModels/BaosunViewModel.cs` | `SaveBillAsync` 保存前设置 `bill.Client = BusinessConstants.BaosunClientId` |

### 数据修复（已执行）
```sql
UPDATE bill_sell SET client='03136'
WHERE sn IN ('00303997','00303998','00303999','00304001','00304002')
  AND (client IS NULL OR client='')
```
受影响 5 行，执行后 JOIN `client_infor` 验证 5 张单 ClientName 均显示"配件报损"。

### 编译结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS8603/CS0618，与本次改动无关）

### 待用户验证
1. 重启软件，在销售开单查询模式下查看已报损的配件历史，客户列应显示"配件报损"（存量 5 单已修，新单写入即正确）
2. 报损管理新建一张报损单保存后，直接查 `bill_sell` 确认 `client='03136'`，销售开单查询模式下客户列显示"配件报损"

---

## 2026-08-11 修复在线更新下载进度条不走动

### 问题描述
在线更新功能点击「立即更新」后，下载进度条一直停在 0% 不动，直到下载完成才瞬间跳满。

### 根因分析
`UpdateService.DownloadAndInstallAsync`（`src/QP11.Services/Update/UpdateService.cs`）的下载实现是一次性读入：
1. `http.GetAsync(downloadUrl, HttpCompletionOption.ResponseContentRead)` 等待整个响应体缓冲完成才返回
2. `response.Content.ReadAsByteArrayAsync()` 一次性把整个安装包（75MB+）读入内存，过程中没有任何中间回调
3. `progress?.Report((data.Length, total))` **只在下载全部完成后调用一次**，进度从 0% 直接跳到 100%

因此下载全程进度条不动作，与「进度条不走动」的现象完全吻合。

### 修复方案
改为**流式下载**：
- `GetAsync` 改用 `HttpCompletionOption.ResponseHeadersRead`，收到响应头即返回
- `response.Content.ReadAsStreamAsync()` 获取内容流，以 256KB 分块循环 `ReadAsync` → `WriteAsync` 写入文件
- 每写完一块即 `progress?.Report((downloaded, total))` 上报一次进度，进度条随下载实时走动
- 完整性校验（文件非空、大小匹配、MZ 魔数、MD5）逻辑保持不变，仅把基于内存 byte[] 的判断改为基于文件读取

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/Update/UpdateService.cs` | `DownloadAndInstallAsync` 下载段：`ResponseContentRead`+`ReadAsByteArrayAsync` 一次性读入改为 `ResponseHeadersRead`+`ReadAsStreamAsync` 分块流式下载，循环上报进度；EXE 校验改为从已写入的文件读取头部字节 |

### 编译结果
- `dotnet build src/QP11.Services/QP11.Services.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS0618/CS8603，与本次改动无关）
- `dotnet build src/QP11.Wpf/QP11.Wpf.csproj --no-incremental`：**0 错误**（同上 4 个既有警告）

### 待用户验证
重启软件，触发在线更新下载，观察：
1. 点击「立即更新」后进度条应从 0% 开始随下载实时走动
2. 进度文字（已下载/总大小 + 百分比）同步更新
3. 下载完成后仍能正常弹出安装程序并退出当前应用

---

## 2026-08-11 打包 exe 安装程序 v2.2.4

### 操作内容
将项目打包为 exe 安装程序，包含本次 Web 服务改为手动启动的代码修改。

### 版本号变更
- `src/QP11.Wpf/QP11.Wpf.csproj`：`<Version>` 2.2.3 → 2.2.4
- `QP11_Setup.iss`：`#define MyAppVersion` 2.2.3 → 2.2.4

### 打包流程
1. **dotnet publish**：`dotnet publish src\QP11.Wpf\QP11.Wpf.csproj -c Release -o Publish --self-contained true -r win-x64`
   - 自包含发布（内置 .NET 8 运行时，目标机器无需预装环境）
   - 输出目录：`f:\qp11\Publish\`
   - 结果：成功，0 错误（仅既有 CS0618 警告）
2. **Inno Setup 编译**：`"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" QP11_Setup.iss`
   - 编译耗时：68.8 秒
   - 压缩方式：lzma2/ultra64 + SolidCompression

### 产物
- 安装包路径：`f:\qp11\InstallPackage\QP11Setup_v2.2.4.exe`
- 大小：75.90 MB
- 生成时间：2026-08-11 11:24

### 注意事项
- 安装脚本排除 `appsettings.json`、`printsettings.json`、`Data\*`，重装时不覆盖用户配置和业务数据
- 卸载时询问是否删除 `Data` 和 `logs` 目录，默认保留用户业务数据

---

## 2026-08-11 Web 服务改为手动启动（移除自动启动）

### 需求
Web 服务改成手动启动，移除自动启动功能。

### 背景
登录成功、主窗口显示后，`App.xaml.cs` 会自动启动内嵌 Web 服务（Kestrel，`http://0.0.0.0:5000`）。桌面导航页（`DesktopControl`）已有完整的「Web 服务管理」区域（▶ 启动服务 / ■ 停止服务 / 🔄 刷新状态 按钮 + 在线连接数 + 状态文字），手动启动能力已具备，本次仅移除自动启动逻辑。

### 修改内容
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/App.xaml.cs` | 删除主窗口显示后 `Dispatcher.BeginInvoke` 延迟启动 `StartWebServer()` 的代码块（原第269-273行）；异步检查更新逻辑保留 |
| `src/QP11.Wpf/Views/DesktopControl.xaml.cs` | 删除 `UserControl_Loaded` 中"延迟2秒再刷新（等待异步启动的 Web 服务就绪）"的 `DispatcherTimer` 逻辑（自动启动已移除，该定时器无意义）；`BtnWebStop_Click` 中停止后1秒延迟刷新逻辑保留（用于等异步停止完成） |

### 变更后行为
- 软件启动**不再自动启动** Web 服务
- 需要网页版/手机平板访问时，进入「桌面导航」页，点击「▶ 启动服务」手动启动；「■ 停止服务」可手动停止
- 退出软件时仍会调用 `App.OnExit` 中的 `StopWebServer()` 停止已启动的 Web 服务（逻辑保留不变）

### 编译结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS8603/CS0618，与本次改动无关）

### 待用户验证
1. 重启软件，确认登录后不会自动启动 Web 服务（桌面导航页状态显示"○ 未运行"）
2. 点「▶ 启动服务」可正常启动，浏览器访问 `http://本机IP:5000` 正常
3. 点「■ 停止服务」可正常停止

---

## 2026-08-11 修复配件管理查询区两个问题（方向键切换 + 车型拼音搜索）

### 问题描述
配件管理页面（`BasicDataControl`）两个 bug：
1. 三个条件查询框（编码/名称/车型）不支持左右方向键切换输入框
2. 车型输入框不支持拼音码搜索（如输入 "xjw" 搜不出 "新君威" 相关车型）

### 根因分析
1. 三个搜索框只绑定了 `TextChanged`，没有焦点全选和方向键切换逻辑（报损管理页 `BaosunControl` 已有相同功能，本次对齐）
2. 页面为客户端内存过滤：`LoadData` 的 SQL 只查了 `cartype`、`name_py`，**未查询 `cartype_py`**，内存过滤时车型条件只匹配 `Cartype` 字段，拼音输入无法命中

### 修复方案
**问题 1（方向键切换）**：复用 `BaosunControl` 的既有模式
- 三个搜索框添加 `GotFocus="TxtSearch_GotFocus"`、`PreviewKeyDown="TxtSearch_PreviewKeyDown"`
- 新增 `TxtSearch_GotFocus`（焦点自动全选）、`TxtSearch_PreviewKeyDown`（光标在最左/最右时按左右键切换焦点）、`MoveFocusTo` 辅助方法
- 切换顺序：编码 → 名称 → 车型；`_searchBoxes` 用**实例字段**（构造函数中初始化），避免 `BaosunControl` 静态数组在页面多次实例化时可能持有旧控件引用的隐患

**问题 2（车型拼音）**：
- `LoadData` SQL 增加 `cartype_py` 字段
- `BasicDataItem` 新增 `CartypePy` 属性（非编辑列，不触发 IsChanged）
- `ApplyFilter` 车型条件：输入为纯 ASCII（拼音）时同时匹配 `CartypePy`（新增 `IsPureAscii` 检测，与 `PartRepository`/`PartsController` 中同名方法逻辑一致）

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/BasicDataControl.xaml` | 三个搜索框添加 `GotFocus`/`PreviewKeyDown` 事件绑定 |
| `src/QP11.Wpf/Views/BasicDataControl.xaml.cs` | 新增 `CartypePy` 属性、`_searchBoxes` 实例字段；`LoadData` SQL 加 `cartype_py`；`ApplyFilter` 车型条件支持拼音码；新增 `TxtSearch_GotFocus`/`TxtSearch_PreviewKeyDown`/`MoveFocusTo`/`IsPureAscii` 方法 |

### 编译结果
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS0618/CS8603，与本次改动无关）

### 待用户验证
重启软件，打开「配件管理」页面：
1. 三个搜索框间用左右方向键可循环切换，获得焦点自动全选文本
2. 车型框输入拼音缩写（如 "xjw"）能过滤出对应车型

---

## 2026-08-11 修复配件管理页面底部出现一行空白行

### 问题描述
配件管理页面（基础数据 Tab，`BasicDataControl`）的配件列表最下面显示一行空白行。

### 根因分析
`dgData` DataGrid 设置了 `CanUserAddRows="True"`，WPF DataGrid 会在列表底部渲染一条空白的"新增行"，用于直接在表格内新增记录。此前页面没有独立的新增入口，靠该空白行 + F6 保存；在新增了「添加配件」按钮（弹窗方式新增）后，该内联新增行成为多余并造成视觉上的空白行。

### 修复方案
`BasicDataControl.xaml` 中 `dgData` 的 `CanUserAddRows="True"` 改为 `CanUserAddRows="False"`，不再显示底部空白新增行。新增配件统一走「添加配件」按钮弹窗；已有行的单元格编辑（修改后 F6 保存）不受影响。

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/BasicDataControl.xaml` | `dgData` 的 `CanUserAddRows` 由 `True` 改为 `False` |

### 编译结果
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS0618/CS8603，与本次改动无关）

### 待用户验证
重启软件，打开「配件管理」页面，列表底部不应再出现空白行；「添加配件」按钮新增功能正常。

---

## 2026-08-11 配件编辑窗口文案调整（仓位/分类）

### 需求
对配件编辑窗口（`PartEditWindow`，新增/编辑配件共用）的表单做文字和布局调整：
1. 「仓位」行不要（删除底部独立的 txtPlace 输入行）
2. 「产地」改名为「仓位」
3. 「属性」改名为「分类」

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/PartEditWindow.xaml` | 「属性:」→「分类:」（Row 5，仍绑定 cboClass）；「产地:」→「仓位:」（Row 6，仍绑定 cboPlace）；删除 Row 12 的「仓位:」+ `txtPlace` 输入框；「备注」上移至 Row 12、按钮行上移至 Row 13；RowDefinitions 从 15 行减为 14 行 |
| `src/QP11.Wpf/Views/PartEditWindow.xaml.cs` | 删除编辑模式回填 `txtPlace.Text = entity.Place;`（txtPlace 控件已移除）；`Place` 仍取 `cboPlace.Text`，保存逻辑不变 |

### 说明
- 仅改文案和删除冗余输入行，字段绑定与保存逻辑不变：分类→`ClassName`（CLASSES 表），仓位→`Place`
- 该窗口被配件管理页（`BasicDataControl` 添加配件按钮）和库存管理页（`InventoryWindow`）共用，改动同时作用于两处

### 编译结果
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS0618/CS8603，与本次改动无关）

---

## 2026-08-11 配件管理页面新增「添加配件」按钮

### 需求
配件管理页面（基础数据 Tab，对应 `BasicDataControl`）增加「添加配件」按钮，点击按钮弹出窗口设置新增配件信息。

### 方案
复用现有的 `PartEditWindow`（配件编辑窗口）新增模式（无参构造 → `_isEdit=false` → 保存走 `IPartRepository.InsertAsync`，自动 `MAX(partid)+1` 分配主键）。该窗口已包含完整新增字段：件号、名称、适用车名、适用车型、单位、属性、产地、区域、进货价、零售价、批发价、拼音码、仓位、备注，保存时自动生成拼音码、校验件号/名称非空。

该模式与库存管理页（`InventoryWindow.BtnAddPart_Click`）的既有实现完全一致。

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/BasicDataControl.xaml` | 工具栏（搜索区右侧）新增「添加配件」按钮，绑定 `BtnAddPart_Click` |
| `src/QP11.Wpf/Views/BasicDataControl.xaml.cs` | 新增 `BtnAddPart_Click`：`new PartEditWindow { Owner = Window.GetWindow(this) }`，`ShowDialog()==true` 后刷新配件列表 |

### 编译结果
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj --no-incremental`：**0 错误**（4 个警告均为既有 CS0618/CS8603，与本次改动无关）

### 待用户验证
重启软件，打开「配件管理」页面，点「添加配件」按钮，弹出配件编辑窗口（件号、名称等字段为空），填写后保存，列表应刷新并出现新配件。

---

## 2026-08-11 借还管理增加配件选择窗口与归还按钮

### 需求
借货管理（借还管理 Tab）增加：
1. 配件选择窗口（三条件查询区 + 配件列表）
2. 双击配件弹出销售开单的配件历史记录窗口（SellEditDialog），添加到明细后不关闭窗口，可继续添加
3. 新增「归还」按钮，选中在借(flag=3)状态的订单点击归还

### 数据库实证分析
连接 `qipei` 库实测，借/还是"两张单"模型，不是"改状态"：
- **借货单** `bill_buy.flag=3`，明细 `detail_buy.amount` 为正（借入入库）
- **还货单** `bill_buy.flag=4`，`total` 取负、`memo='还货单(原sn)'`，明细 `amount` 取负、`tsn` 指向原单（归还出库）
- 该结构与采购退货（flag=2 + tsn）完全同构；归还是"新建负向还货单"，而非把原单 flag 改掉
- 实测：49 张 flag=3 借货单、41 张 flag=4 还货单一一对应（如 00012080 借→00012127 还）
- 数据库无触发器/存储过程接管库存，库存增减完全由应用层代码控制

### 方案与实现
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/PartSelectorWindow.xaml.cs` | 新增 `BorrowMode` 属性；`DgParts_MouseDoubleClick`/`BtnConfirm_Click` 增加借货分支；新增 `OpenBorrowEdit` 复用 `SellEditDialog` + 通过 `ItemAdded` 事件实时写入借货明细（窗口保持打开可连续添加） |
| `src/QP11.Wpf/Views/BorrowControl.xaml` | 工具栏新增「归还」按钮；列定义加一列；"已还" RadioButton 文案从"归还"改为"已还"避免与按钮重名 |
| `src/QP11.Wpf/Views/BorrowControl.xaml.cs` | `BtnAdd_Click` 复用 `PartSelectorWindow`（借货模式）+ `ItemAdded` 实时加明细；新增 `BtnReturn_Click` 调用 `SaveReturnAsync`；`SaveBill` 拦截已还模式直接保存；`SettleBill` 旧逻辑（误把 flag 设为 Confirmed=1）改为走归还流程 |
| `src/QP11.Wpf/ViewModels/BorrowViewModel.cs` | 注入 `IUnitOfWorkFactory`/`IDbConnectionFactory`；新增 `SaveReturnAsync(origBill)` 事务内建 flag=4 负向还货单（amount/total 取负，tsn 指向原单，memo 记原单号）+ `IncreaseStockAsync` 回补库存；移除错误的 `SettleBillAsync` |

### 归还流程（事务内原子完成）
1. 校验选中单据 `flag==3`（在借）
2. 生成新还货单号（`GenerateBuySN`）
3. 事务内：`InsertBillAsync`（flag=4, total 取负, memo='还货单(原sn)'）→ `InsertDetailsAsync`（amount 取负, tsn=原sn）→ 逐条 `DecreaseStockAsync` 扣减库存
4. 提交事务，刷新列表

### 库存方向修正（重要）
用户确认业务语义：**借=入库（+库存），还=出库（-库存）**，与数据库符号一致：
- 借货单明细 `amount` 为正 → `IncreaseStockAsync`（货从供应商借入，进仓库）
- 还货单明细 `amount` 为负 → `DecreaseStockAsync`（货还回给供应商，出仓库）
- 与全系统惯例一致：销售出库 Decrease、客户退货入库 Increase、采购退货给供应商 Decrease
- 修改：`SaveNewBillAsync` 原 `DecreaseStockAsync` 改为 `IncreaseStockAsync`；`SaveReturnAsync` 原 `IncreaseStockAsync` 改为 `DecreaseStockAsync`

### 借入保存补包事务
`SaveNewBillAsync`（借入保存）原为"先插单→再逐条加库存"，中途失败会产生单据已存但库存未加/未加全的脏状态。现改为与归还对称：事务内原子完成"插主表 + 插明细 + 加库存"，失败整体回滚。

### 编译结果
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj`：**0 错误**（4 个警告均为既有，与本次改动无关）

### 备注
- `BorrowViewModel` 构造函数新增 `IUnitOfWorkFactory`、`IDbConnectionFactory` 两个参数，已由 DI 容器自动注入（App.xaml.cs 注册），无需额外配置
- 借入保存与归还均已包裹事务，原子完成（插单+明细+库存变更），中途失败整体回滚，不会产生脏状态

---

## 2026-08-11 修复数据迁移 OdbcTransaction 已完成异常

### 问题描述
数据迁移在迁移销售出库数据时报错：
```
This OdbcTransaction has completed; it is no longer usable
```
实际根本原因是某张销售单的明细数据存在 SQL 执行错误（如数据截断、约束冲突等），但异常信息被掩盖。

### 根因分析
`MigrateSales()` 和 `MigratePurchases()` 的 catch 块使用裸 `catch { transaction.Rollback(); throw; }` 模式：

1. ODBC 事务内的 SQL 执行出错时，SQL Server 2000 的 `SET XACT_ABORT ON`（ODBC 默认行为）**自动回滚**了事务
2. 随后 catch 块调用 `transaction.Rollback()` 时，底层事务已由服务器端完成
3. `Rollback()` 抛出 `"This OdbcTransaction has completed; it is no longer usable"`
4. 此异常传播到 `RunMigration` 的外层 catch，原始 SQL 错误被丢失

### 修复方案
- catch 块改为 `catch (Exception ex)`，捕获并记录原始异常（含具体单据号 sn）
- `Rollback()` 调用加上 try-catch 保护，事务已自动回滚时静默忽略
- 仍 `throw` 重新抛出原异常，让外层 `RunMigration` 继续处理

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | `MigratePurchases()` catch 块（第839-845行）和 `MigrateSales()` catch 块（第986-992行）：改为捕获 `Exception ex` 并记录日志，`Rollback()` 加 try-catch 保护 |

### 编译结果
`dotnet build f:\qp11\src\QP11.Services\QP11.Services.csproj --no-incremental`：**0 错误**（4个警告均为既有）

---

## 2026-08-11 借货管理与归还管理合并，改名为借还管理

### 变更内容
将"借货管理"和"归还管理"两个导航入口合并为一个模块，统一命名为"借还管理"。

由于 BorrowControl 本身已支持"在借"（flag=3）和"归还"（flag=4）两种模式的切换，合并后统一通过 BorrowControl 进入，不再需要独立的"归还管理"入口。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/BorrowControl.xaml.cs` | `TabTitle` 从"借货管理"改为"借还管理" |
| `src/QP11.Wpf/Views/SellControl.xaml` | "借货管理"按钮改为"借还管理"，删除"归还管理"按钮 |
| `src/QP11.Wpf/Views/SellReturnControl.xaml` | 同上 |
| `src/QP11.Wpf/Views/MainWindow.xaml` | 菜单项"借货出库"改为"借还管理"，删除"还货入库"菜单项 |
| `src/QP11.Wpf/Views/MainWindow.xaml.cs` | 删除 Tag 19 的窗口路由和标题映射；Tag 18 标题从"借货出库"改为"借还管理" |

### 编译结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj --no-incremental`：**0 错误**（4个警告均为既有，非本次引入）

---

## 2026-08-11 修复借货管理供应商显示数字ID、采购员不显示

### 问题描述
借货管理（BorrowControl）中：
- 右侧单据列表的"供应商"列显示的是数字/字母ID（如 S001），而非供应商名称
- 右侧单据列表的"采购员"列显示空白或用户名，而非人员姓名
- 选中单据编辑时，供应商输入框显示的是ID而非名称

### 根因分析
`BillBuy.Supplier` 字段存储的是供应商编号（sid），`BillBuy.Worker` 字段存储的是用户登录名（username），直接绑定显示没有做 ID→名称 的映射。

### 修改内容
**文件：** `src\QP11.Wpf\Views\BorrowControl.xaml.cs`

1. 添加 `_supplierNameMap`、`_workerNameMap` 两个字典，以及 `_allSuppliers`、`_allUsers` 两个缓存列表
2. `LoadDropdowns()` 中加载供应商和用户数据时，同时构建字典和缓存列表
3. `LoadBillList()` 中列表显示时，通过字典将供应商ID和用户登录名映射为显示名称
4. `DgBills_SelectionChanged()` 中选中单据时，使用 `SetSupplier()` 方法设置供应商（显示名称而非ID），采购员也通过姓名查找显示

### 验证
- 编译通过，0 错误 0 警告
- 运行时需验证：打开借货管理页面，检查右侧单据列表供应商列显示名称、采购员列显示姓名

## 2026-08-10 修复销售历史双击报损记录详情窗客户显示空白

### 问题描述
销售开单的配件历史记录窗口（SellEditDialog）中，双击"配件报损"记录打开详情窗（OrderDetailWindow）时：
- 旧报损单（如 00303947）：客户/供应商栏能正常显示
- 新报损单（如 00304001、00303999）：客户/供应商栏空白

### 根因分析
通过 ODBC 直连数据库查询 `bill_sell WHERE flag=3` 对比新旧报损单数据：

| 单号 | client | worker | 时间 |
|------|--------|--------|------|
| 00304001（新） | `''`（空） | 邓鹏 | 08/10 15:04 |
| 00303999（新） | `''`（空） | 邓鹏 | 08/10 14:00 |
| 00303947（旧） | `03136`（真实cid） | 00084 | 08/04 15:41 |
| 00303853（旧） | `03136`（真实cid） | 00065 | 08/03 09:33 |

**旧报损单**的 `client` 字段存有真实客户 cid（如 `03136`），`LEFT JOIN client_infor` 能匹配到客户名称 → 详情窗正常显示。

**新报损单**的 `client` 字段为空字符串（见此前修复"配件报捷新单数据格式与旧单不一致"中 `client=''`），`LEFT JOIN client_infor` 匹配不到 → `client_infor.name` 为 NULL → 详情窗显示空白。

**遗漏点**：此前修复"报损后配件销售历史客户名称为空"时，已在销售历史列表的 SQL 中加入 `CASE WHEN ISNULL(bill_sell.flag, 0) = 3 THEN '配件报损' ELSE client_infor.name END`（`PartQueryService.GetSellHistoryAsync` 和 `SellEditDialog.LoadSellHistoryAsync`），但 `OrderDetailWindow.LoadSellOrder` 的单据头 SQL 被遗漏，仍使用 `client_infor.name AS ClientName`，导致 flag=3 且 client 为空的报损单在详情窗中客户栏为空。

### 修复方案
在 `OrderDetailWindow.LoadSellOrder` 的单据头 SQL 中加入与销售历史列表一致的 `CASE WHEN`：
```sql
CASE WHEN ISNULL(bill_sell.flag, 0) = 3 THEN '配件报损' ELSE client_infor.name END AS ClientName
```

修复后所有 flag=3 的报损单在详情窗客户栏均显示"配件报损"，与销售历史列表显示一致。

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/OrderDetailWindow.xaml.cs` | `LoadSellOrder` 单据头 SQL：`client_infor.name AS ClientName` 改为 `CASE WHEN ISNULL(bill_sell.flag, 0) = 3 THEN '配件报损' ELSE client_infor.name END AS ClientName` |

### 编译结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj --no-incremental`：**0 错误**（4 个既有警告）

### 待用户验证
重启软件，在销售开单的配件历史中双击新报损单（如 00303999），详情窗客户/供应商栏应显示"配件报损"而非空白。

---

## 2026-08-10 修复配件报损保存报错"必须声明变量 '@Worker'"

### 问题描述
配件报损页面（BaosunControl）保存报损单时报错：
```
System.Data.OleDb.OleDbException (0x80040E14)
必须声明变量 '@Worker'。
   在 QP11.Data.Infrastructure.OleDbCompatCommand.ExecuteNonQuery() 在 OleDbCompatConnection.cs:第 160 行
```

### 根因分析
报损单 `InsertBillAsync` 的 INSERT SQL 在 VALUES 中使用了空字符串字面量 `''`：
```sql
VALUES (@Sn, '', @Worker, @Operator, @Total, 0, 0, @Total, 0, @Total, 0, 0, 0, 3, 0, GETDATE(), '', @Memo)
```

`SqlParamConverter.GetPositionsInsideQuotes`（`src/QP11.Data/Infrastructure/SqlParamConverter.cs`）负责判断 SQL 中哪些 `@参数` 位于单引号字符串内（应跳过转换）。旧实现用 `if (sql[i] == '\'' && (i == 0 || sql[i - 1] != '\'')) inSingle = !inSingle` 切换字符串状态：

- 对独立的 `''`（空字符串）：第一个 `'` 切换为“字符串内”（inSingle=true），第二个 `'` 因前一字符是 `'` 被当作转义引号跳过 → **字符串状态停留在“内”不关闭**。
- 此后 `@Worker`、`@Operator`、`@Total`(×3) 全部被误判为“在字符串内”而跳过 `@param → ?` 转换，原样发给 SQL Server，触发“必须声明变量 '@Worker'”（第一个未转换的参数）。
- `@Sn`（在第一个 `''` 之前）和 `@Memo`（在第二个 `''` 之后，状态被再次切换回 false）转换正常。

对比销售开单 `SellRepository.InsertBillAsync` 能正常工作：其 SQL VALUES 中没有 `''` 字面量，全用参数/函数，故不受此 bug 影响。

### 修复方案
重写 `GetPositionsInsideQuotes`，按正确的 SQL 字符串字面量规则解析：
- 不在字符串内遇到 `'` → 开启字符串；
- 在字符串内遇到 `'` 且下一字符也是 `'` → 转义引号（跳过两个字符，字符串不关闭）；
- 在字符串内遇到 `'` 且下一字符不是 `'` → 关闭字符串。

先预计算每个字符位置的“是否在字符串内”状态，再判断各 `@参数` 起始位置。独立 `''` 现被正确识别为“开-关”，其后的参数不再被误判。

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Infrastructure/SqlParamConverter.cs` | 重写 `GetPositionsInsideQuotes`：用正确的 `''` 转义/空字符串解析逻辑替换旧的 `sql[i-1]!='\''` 切换逻辑 |

### 影响范围
该修复对所有走 OleDbCompatCommand / OdbcCompatCommand（SQLOLEDB / ODBC 驱动，不支持 `@命名参数`）的 SQL 生效。此前任何含 `''` 字面量且其后跟 `@参数` 的 SQL 都会触发同样错误，现已一并修复。

### 编译结果
- `dotnet build f:\qp11\src\QP11.Data\QP11.Data.csproj --no-incremental`：**0 错误**（2 个 CS8603 既有警告）
- `dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj --no-incremental`：**0 错误**（4 个既有警告）

### 待用户验证
重启软件，在配件报损页面添加明细后点“保存”，确认不再报“必须声明变量 '@Worker'”，报损单正常写入。

---

## 2026-08-10 修复配件报捷新单数据格式与旧单不一致

### 问题描述
新创建的报损单（如00303999）与旧报损单（如00303268）对比，抬头和明细多个字段缺失或值不正确。

### 旧单与新单格式差异及修复

**bill_sell 抬头：**

| 字段 | 旧单(00303268) | 新单(00303999)修复前 | 修复后 |
|------|:-:|:-:|:-:|
| type | 0 | 3 | 0 |
| client | '' | None | '' |
| operator | qp110 | None | App.CurrentUser.Username |
| total_payment | =total | 0 | =total |
| cash | =total | 0 | =total |
| checkno | '' | None | '' |
| discount_rate | 0 | None | 0 |
| bill_total/bill_payment/collection/checks/arrear | 0 | 0/None | 0 |

**detail_sell 明细：**

| 字段 | 旧单(00303268) | 新单(00303999)修复前 | 修复后 |
|------|:-:|:-:|:-:|
| flag | 3 | 0 | 3 |
| type | 0 | 0 | 0 |
| unit | 个 | None | 从配件数据带入 |
| cartype | 吉利帝豪GS/GL | None | 从配件数据带入 |
| cb(成本) | 90.00 | None | 从配件InPrice带入 |
| btotal | 0 | None | 0 |
| bill_price | 0 | 0 | 0 |
| car_mark | '' | None | '' |

**worker 字段保持不变**：旧单存工号，新单存姓名（已修复的格式）。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Repositories/BaosunRepository.cs` | `InsertBillAsync` SQL 添加 client/operator/total_payment/cash/checkno 等字段，type 改为 0；`InsertDetailAsync` SQL 添加 flag=3/cartype/cb/btotal/car_mark 等字段 |
| `src/QP11.Core/Entities/BillBaosun.cs` | DetailBaosun 添加 Cartype、Cb 属性映射 |
| `src/QP11.Wpf/Views/BaosunControl.xaml.cs` | BaosunDetailItem 添加 Unit/Cartype/Cb 属性；双击添加配件时从 PartStockDisplay 带入这些值；SaveBill 设置 Operator |
| `src/QP11.Wpf/ViewModels/BaosunViewModel.cs` | SaveBillAsync 传递 Unit/Cartype/Cb 到 DetailBaosun |

### 编译结果
QP11.Wpf：0 错误，5 警告（既有）

## 2026-08-10 报损查询模式新增查询按钮

### 改动内容
报损查询模式（Mode B）的日期条件行新增"查询"按钮，点击后立即执行查询（取消当前防抖并直接加载单据列表）。

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/BaosunControl.xaml` | 查询模式 Grid 列定义拆为6列，第4列新增"查询"按钮 |
| `src/QP11.Wpf/Views/BaosunControl.xaml.cs` | 新增 `BtnQuery_Click` 事件处理 |

### 编译结果
QP11.Wpf：0 错误，4 警告（既有）

## 2026-08-10 报损管理页面搜索框支持左右方向键切换+经手人自动填充

### 需求1：左右方向键切换搜索框 + 自动全选

#### 改动内容
- 三个搜索框（配件编号、名称、车型）添加 `GotFocus="TxtSearch_GotFocus"` 和 `PreviewKeyDown="TxtSearch_PreviewKeyDown"` 事件
- 输入框获得焦点时自动全选文本（`SelectAll()`），方便用户直接输入新内容覆盖
- 光标在文本最左时按左方向键 → 跳到上一个输入框
- 光标在文本最右时按右方向键 → 跳到下一个输入框
- 搜索框顺序：配件编号 → 名称 → 车型，循环切换

### 需求2：经手人自动获取当前登录账号姓名

#### 改动内容
- `LoadDropdowns()` 加载用户列表后，根据 `App.CurrentUser.Username` 匹配并自动设置 `cboWorker.SelectedItem`

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/BaosunControl.xaml` | 三个搜索框添加 GotFocus 和 PreviewKeyDown 事件 |
| `src/QP11.Wpf/Views/BaosunControl.xaml.cs` | 新增 `TxtSearch_GotFocus`（自动全选）、`TxtSearch_PreviewKeyDown`（方向键切换）、`MoveFocusTo`（焦点移动辅助方法）；`LoadDropdowns` 添加当前用户自动匹配 |

### 编译结果
QP11.Wpf：0 错误，4 警告（既有）

## 2026-08-10 修复报损查询配件拼音缩写搜不到的问题

### 问题描述
报损管理页面搜索配件时，输入拼音缩写"qjzc"加上车型"新君威"返回0行，但实际数据库中有对应配件。

日志：
```
[QUERY] GetStockListAdvancedAsync partNo=null partName=qjzc partNamePy=null cartype=新君威 ... 返回0行 耗时40ms
```

### 根因分析
`GetStockListAdvancedAsync()` 方法中，`partNamePy` 参数为 null 时代码走中文模式分支，只在 `name`/`carname`/`memo`/`class` 字段中搜索 `%qjzc%`。但拼音缩写"qjzc"存储在 `name_py` 字段中，不在 `name` 等中文文本字段中，导致搜不到结果。

同样的问题在 `cartype` 字段也存在：如果用户输入拼音缩写车型，`cartypePy` 为 null 时也不会搜索 `cartype_py` 字段。

### 修复方案
在 `partName` 和 `cartype` 的处理分支中，增加 `IsPureAscii()` 自动检测：
- 当输入为纯ASCII（拼音缩写）时，自动转为拼音搜索，同时搜索 `name_py`/`cartype_py` 字段
- 保留中文 `name`/`cartype` 字段的 LIKE 搜索，确保不会漏掉数据

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Repositories/PartRepository.cs` | `GetStockListAdvancedAsync` 中 partName 和 cartype 分支增加 `else if (IsPureAscii(...))` 拼音自动检测 |

### 编译结果
QP11.Data：0 错误，2 警告（既有）

## 2026-08-10 修复报损出库两个Bug

### Bug 1：报损单经手人被写为用户名而非姓名

#### 问题描述
报损出库保存报损单时，经手人（worker）字段被写入了用户名（如 "admin"），而不是姓名（如 "管理员"）。

#### 根因分析
`BaosunControl.xaml.cs` 的 `SaveBill()` 方法中：
```csharp
// 此前代码
Worker = cboWorker.SelectedValue?.ToString() ?? cboWorker.Text.Trim(),
```
`cboWorker` 设置了 `SelectedValuePath = "Username"`，所以 `SelectedValue` 返回的是用户名。而 `DisplayMemberPath = "Name"` 显示的是姓名，两者不一致。

#### 修复方案
将 `SaveBill()` 中的经手人赋值改为从 `SelectedItem` 直接获取 `Name` 属性：
```csharp
Worker = (cboWorker.SelectedItem as UserInfor)?.Name ?? cboWorker.Text.Trim(),
```

#### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/BaosunControl.xaml.cs` | SaveBill 方法中 Worker 赋值改为从 SelectedItem 获取 Name |

### Bug 2：报损后配件销售历史客户名称为空

#### 问题描述
配件报损后，该配件的销售历史中客户名称显示为空白，正确应该显示"配件报损"。

#### 根因分析
报损记录写入 `bill_sell` 表时 `flag=3`，没有客户信息（`client` 字段为空）。销售历史查询时 `LEFT JOIN client_infor` 找不到匹配记录，导致 `ClientName` 为 NULL。

涉及查询位置：
1. `PartQueryService.GetSellHistoryAsync()` - 配件详情窗口的销售历史
2. `SellEditDialog.LoadSellHistoryAsync()` - 销售开单弹窗的销售历史

#### 修复方案
在两个查询的 SQL 中添加 `CASE WHEN` 判断，当 `bill_sell.flag = 3` 时显示"配件报损"：
```sql
CASE WHEN ISNULL(bill_sell.flag, 0) = 3 THEN '配件报损' ELSE client_infor.name END AS ClientName
```

#### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/PartQueryService.cs` | 两个 SQL 查询的 ClientName 字段改为 CASE WHEN 表达式 |
| `src/QP11.Wpf/Views/SellEditDialog.xaml.cs` | 两个 SQL 查询的 ClientName 字段改为 CASE WHEN 表达式 |

### 编译结果
- QP11.Services：0 错误
- QP11.Wpf：0 错误

## 2026-08-10 修复销售退货迁移已退数量为0的问题

### 问题描述
目标库退货窗口有"已退数量"和"可退数量"字段，迁移后的数据"已退数量"全部为0。

### 根因分析
通过连接源数据库（frp-bar.com:47017, 备用数据）分析发现：

1. **目标库退货逻辑**：`BuyReturnWindow` 和 `SellReturnViewModel` 通过 `detail.tsn = 原单sn` 查询 `amount < 0` 的退货明细来计算已退数量。迁移时未设置 `tsn` 字段，导致查询不到退货记录。

2. **销售退货**：源库 `tbsada.ypaper` 字段存储了原销售单号，匹配率100%（100431/100373条能匹配到原销售单）。但迁移代码的 `detail_sell` INSERT 语句中没有 `tsn` 字段。

3. **采购退货**：源库 `tbistoed` 中没有字段关联到原采购单：
   - `seano` = `cno`（退货单自己的编号，不是原采购单号），匹配率0%
   - `pno` 也不是原采购单号，匹配率0%
   - 通过 `gyno + nno`（供应商+配件编号）模糊匹配有97.2%匹配率，但无法精确定位到具体原单

### 修复方案
**销售退货**：在 `MigrateSales()` 方法中，将源库 `tbsada.ypaper`（原销售单号）通过 `GenerateSellSn()` 转换后设置到 `detail_sell.tsn` 字段。每条退货明细单独读取 `ypaper`，因为同一退货单下的不同明细可能来自不同的原销售单。

**采购退货**：源库中没有直接关联原采购单的字段，暂不处理（需进一步确认方案）。

### 修改文件列表

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | `MigrateSales()` 方法：detail_sell INSERT 添加 tsn 字段，退货明细通过 ypaper 映射 tsn（第913-975行） |

### 验证方式
重新运行迁移后，在销售退货窗口查询客户销售单，已退数量应能正确显示（非0值）。

---

## 2026-08-10 修复数据迁移窗口进度条被按钮遮挡的问题

### 问题描述
数据迁移窗口中，"开始迁移"按钮遮挡了进度条后面的进度文字（txtProgress）。

### 根因分析
`MigrationWindow.xaml` 操作按钮区域的 Grid 列定义中，第一列（txtStatus）宽度为 `*`，占满了所有剩余空间，将进度条、进度文字、按钮全部挤到最右边紧挨在一起；同时进度文字（txtProgress）固定 `Width="80"`，导致文字被按钮遮挡或截断。

### 修复方案
调整 Grid 列布局，将进度条向左移动，并消除按钮对进度文字的遮挡：
1. 将第1列（txtStatus）从 `*` 改为 `Auto`，使其只占所需宽度，进度条紧跟其后向左移动
2. 新增一个 `*` 空白列作为间距，将按钮推到最右侧
3. 按钮的 Grid.Column 从 `3` 改为 `4`
4. 去掉 txtProgress 的固定 `Width="80"`，改为自适应内容宽度，避免文字被截断
5. 给 txtStatus 增加右 Margin 与进度条保持间距

### 修改文件列表

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/MigrationWindow.xaml` | 操作按钮区域 Grid 列布局调整（第89-104行） |

---

## 2026-08-10 销售退货页面业务员自动填充

### 变更内容
销售退货页面（SellReturnControl）加载后，业务员 ComboBox 自动填充为当前登录账号。

### 修改文件
1. `src\QP11.Wpf\Views\SellReturnControl.xaml`
   - 将 `cboWorker` 的 `SelectedValuePath` 从 `"Workid"` 改为 `"Username"`，与 `UserInfor` 实体匹配（同销售开单 SellControl 一致）

2. `src\QP11.Wpf\Views\SellReturnControl.xaml.cs`
   - 在 `LoadDropdownsAsync()` 中加载用户列表后，查找 `App.CurrentUser` 对应的用户，自动设置 `cboWorker.SelectedItem`

## 2026-08-08 数据库连接地址变更

### 变更内容
将数据库连接地址从 `192.168.83.128,8829` 改为 `192.168.1.86,1433`

### 修改文件列表

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.WebApi/appsettings.json` | 3处连接串中的地址 |
| `src/QP11.Wpf/appsettings.json` | 3处连接串中的地址 |
| `Publish/appsettings.json` | 3处连接串中的地址 |
| `src/QP11.Data/Infrastructure/DatabaseFactory.cs` | 第112行默认回退地址 |
| `.trae/rules/sql配置.md` | 参考文档中的连接信息 |

### 旧连接
`192.168.83.128,8829`

### 新连接
`192.168.1.86,1433`

---

## 2026-08-08 修复销售订单编辑时库存计算错误的Bug

### 问题描述
入库一个库存为0的配件（库存变为1），销售开单1个（库存变为0），之后修改该订单数量从1改成2，保存成功后配件库存错误地变回了1。

### 根因分析
`PartRepository.DecreaseStockAsync` 的SQL中有守卫条件 `WHERE ISNULL(amount,0) >= @Qty`，当库存不足时UPDATE静默失败（影响0行），而调用方 `SellViewModel.SaveBillAsync` 不检查返回值。

编辑订单时的库存变化流程：
1. 回补旧库存：IncreaseStock(1)，0→1（无守卫，必成功）
2. 扣减新数量2：DecreaseStock(2)，`1>=2` 为假，静默失败，库存保持1

### 修复方案
在 `SellViewModel.SaveBillAsync` 扣减新明细库存时，检查 `DecreaseStockAsync` 返回值，返回0（库存不足）则抛出异常阻止保存，事务自动回滚。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/ViewModels/SellViewModel.cs` | 第291-303行：扣减库存时检查返回值，不足则抛异常 |

---

## 2026-08-08 修复编辑订单时DataGrid单元格未提交导致库存不足误报

### 问题描述
原订单数量为3，配件实际库存为0。修改订单数量改成4个，提示库存不足（正确）。再把数量改成2个，仍提示库存不足（错误）。取消操作重新编辑，数量改成2个，正常保存。

### 根因分析
`SellControl.SaveBill()` 在调用 `SaveBillAsync()` 之前未调用 `dgDetails.CommitEdit()` 提交DataGrid中正在编辑的单元格。

数量列绑定 `{Binding Amount}` 默认使用 `UpdateSourceTrigger=LostFocus`，即TextBox失去焦点时才更新数据源。第一次保存失败后，DataGrid的编辑状态可能未正确重置，导致第二次编辑（4→2）的值未被提交到 `SellControlItem.Amount` 属性，保存时仍使用旧值4，触发库存不足误报。

### 修复方案
在 `SaveBill()` 方法开头、调用 `SaveBillAsync()` 之前，添加 `dgDetails.CommitEdit(DataGridEditingUnit.Row, true)` 强制提交DataGrid当前编辑。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/SellControl.xaml.cs` | 第1084-1085行：SaveBill方法中添加CommitEdit调用 |

---

## 2026-08-08 新增第三方数据迁移模块

### 功能描述
在系统管理分类下新增"数据迁移"模块，支持从第三方软件数据库迁移数据到新空库。用户输入源库和目标库的连接信息，可测试连接，执行迁移操作。

### 迁移范围
- 系统设置（tbsysset）
- 配件数据（tbpart + tbpart_fa）
- 客户数据（tbclient）
- 供应商数据（tbsupplier）
- 仓位数据（tbplace）
- 库存数据（part_stock）
- 采购入库记录（tbinput + tbinput_list）
- 销售出库记录（tboutput + tboutput_list）
- 应收应付（tbouder）
- 付款记录（tbpay）
- 账户流水（tbbank）

### 修改文件列表

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | 新建：核心迁移服务，实现数据库连接测试、各表数据迁移、ID映射、进度报告 |
| `src/QP11.Wpf/Views/MigrationWindow.xaml` | 新建：数据迁移UI界面，含源库/目标库连接输入、测试连接、开始迁移、日志输出 |
| `src/QP11.Wpf/Views/MigrationWindow.xaml.cs` | 新建：MigrationWindow代码后置，处理连接测试、迁移执行、进度更新、日志显示 |
| `src/QP11.Wpf/App.xaml.cs` | 注册MigrationService为Singleton |
| `src/QP11.Wpf/Views/MainWindow.xaml` | 系统管理菜单下新增"数据迁移"菜单项（Tag="7g"） |
| `src/QP11.Wpf/Views/MainWindow.xaml.cs` | 添加"7g"路由映射到MigrationWindow |

### 编译修复
- `MigrationService.cs` 第527/619/745行：`(long)(double?)` 改为 `(long?)(double?)` 修复CS0019编译错误（`??` 运算符不能应用于非空值类型）

---

## 2026-08-08 修复 QP11.Services 项目编译警告

### 问题描述
`dotnet build` 全量编译 QP11.Services 项目时，共产生 9 个编译警告（QP11.Data 项目的 CA1416/CS8619/CS8603 警告不在本次修复范围）：

| 警告码 | 文件 | 位置 | 说明 |
|--------|------|------|------|
| CS8634 | CompositeVinQueryService.cs | 146,187 | 泛型类型参数 `VinDecodeResult?`/`VinPartPageResult?` 与 `class` 约束不匹配 |
| CS8621 | CompositeVinQueryService.cs | 147,188 | lambda 返回类型为 Null 性与目标委托不匹配（由 CS8634 约束问题连带触发） |
| CS8604 | JhdhService.cs | 49,91 | `bill.Supplier`（string?）可能传入 null 给 `ValidateRequired(string value, ...)` |
| CS8604 | BuyService.cs | 34 | `bill.Supplier`（string?）可能传入 null 给 `ValidateRequired(string value, ...)` |
| CS8604 | SellService.cs | 36 | `bill.Client`（string?）可能传入 null 给 `ValidateRequired(string value, ...)` |
| CS0067 | MockVinQueryService.cs | 15 | 事件 `LoginStatusChanged` 从未使用 |

### 根因分析
- **CS8634/CS8621**：`SafeExecuteAsync<T>` 的泛型约束为 `where T : class`（非空引用类型），但调用时传入的 `s.DecodeVinAsync` / `s.GetPartCardsAsync` 返回 `Task<VinDecodeResult?>` / `Task<VinPartPageResult?>`，推断出的 T 为可空类型，不满足 `class` 约束。
- **CS8604**：实体属性 `Supplier`/`Client` 为 `string?`，而 `IValidationService.ValidateRequired(string value, string fieldName)` 形参为非空 `string`。`ValidateRequired` 本身就是做非空校验，传入 `!` 抑告即可。
- **CS0067**：`LoginStatusChanged` 是 `IVinDataSource` 接口要求实现的成员，不能删除，但 Mock 实现从不触发该事件。

### 修复方案
- **CS8634/CS8621**：将 `SafeExecuteAsync<T>` 的约束由 `where T : class` 改为 `where T : class?`，允许可空引用类型作为类型参数，连带消除 CS8621。
- **CS8604**：在传给 `ValidateRequired` 的可空实参后加 `!`（null-forgiving 运算符），符合任务要求且语义正确（校验方法内部会检查空值并抛异常）。
- **CS0067**：参照同文件 `SourceStatusChanged` 既有写法，将 `LoginStatusChanged` 改为空访问器 `{ add { } remove { } }`，既满足接口实现又消除告警，不引入 pragma。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/CompositeVinQueryService.cs` | 第330行：`where T : class` 改为 `where T : class?` |
| `src/QP11.Services/JhdhService.cs` | 第49、91行：`bill.Supplier` 改为 `bill.Supplier!` |
| `src/QP11.Services/BuyService.cs` | 第34行：`bill.Supplier` 改为 `bill.Supplier!` |
| `src/QP11.Services/SellService.cs` | 第36行：`bill.Client` 改为 `bill.Client!` |
| `src/QP11.Services/MockVinQueryService.cs` | 第15行：事件改为空访问器 `{ add { } remove { } }` |

### 验证结果
`dotnet build f:\qp11\src\QP11.Services\QP11.Services.csproj --no-incremental` 编译通过，0 错误，QP11.Services 项目 0 警告（剩余警告均位于 QP11.Data 项目，不在本次范围）。

---

## 2026-08-08 修复 QP11.Core 项目编译警告

### 问题描述
`dotnet build f:\qp11\src\QP11.Core\QP11.Core.csproj --no-incremental` 全量编译时产生 21 个编译警告（构建输出因重复显示为 42 条），0 错误。分两类：

- **CS0109（18 处）**：成员不会隐藏可访问成员，不需要 `new` 关键字。
- **CS0108（3 处）**：成员隐藏继承成员但未使用 `new` 关键字。

### 根因分析
基础接口 `IRepository<T>` 定义了通用仓储方法：
```csharp
Task<T> GetByIdAsync(object id);
Task<int> InsertAsync(T entity, IDbTransaction? transaction = null);
Task<int> UpdateAsync(T entity, IDbTransaction? transaction = null);
Task<int> DeleteAsync(object id, IDbTransaction? transaction = null);
```

各派生仓储接口在重写这些方法时存在 `new` 关键字误用：

- **CS0109（误加 `new`）**：派生接口方法签名与基类**不同**（参数个数或类型不同），实际并未隐藏任何基类成员，此时 `new` 多余。例如 `UpdateAsync(Account entity)`（1 个参数）相对于基类 `UpdateAsync(T entity, IDbTransaction? transaction = null)`（2 个参数）并不构成隐藏。
- **CS0108（漏加 `new`）**：派生接口方法签名与基类**相同**（仅默认值不同，默认值不属于方法签名），构成隐藏却未标注 `new`。经核对实现类（`BuyRepository`/`JhdhRepository`/`SellRepository`），基类 `IRepository<T>.UpdateAsync(...)` 被**显式实现为抛出 `NotImplementedException`**（如 `BuyRepository.cs:236`），而派生接口同名方法有真实实现（如 `BuyRepository.cs:123`），确属**有意隐藏**，故应添加 `new` 而非删除（删除会导致调用走到抛异常的基类实现，破坏行为）。

### 修复方案
- **CS0109**：移除 18 处多余的 `new` 关键字，方法声明本身保留（作为合法重载）。
- **CS0108**：为 3 处有意隐藏的同签名方法添加 `new` 关键字。
- 保留与基类签名完全一致且本就无警告的 `new`（如 `IAccountRepository.InsertAsync`、`IMemberCardRepository.InsertAsync`、`IPartRepository.InsertAsync`），不动它们。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Core/Interfaces/IAccountRepository.cs` | 第12行：`UpdateAsync(Account)` 移除 `new` |
| `src/QP11.Core/Interfaces/IBorrowRepository.cs` | 第10行：`InsertAsync(Borrow)` 移除 `new` |
| `src/QP11.Core/Interfaces/IBuyRepository.cs` | 第18行：`UpdateAsync(BillBuy)` 移除 `new`；第19行：`UpdateAsync(BillBuy, IDbTransaction?)` 添加 `new` |
| `src/QP11.Core/Interfaces/IClientRepository.cs` | 第9/11/12行：`GetByIdAsync`/`InsertAsync`/`UpdateAsync` 移除 `new` |
| `src/QP11.Core/Interfaces/IMemberCardRepository.cs` | 第10/13行：`GetByIdAsync`/`UpdateAsync` 移除 `new` |
| `src/QP11.Core/Interfaces/IJhdhRepository.cs` | 第18行：`UpdateAsync(BillJhdh)` 移除 `new`；第19行：`UpdateAsync(BillJhdh, IDbTransaction?)` 添加 `new` |
| `src/QP11.Core/Interfaces/ISysLogRepository.cs` | 第11行：`InsertAsync(SysLog)` 移除 `new` |
| `src/QP11.Core/Interfaces/IUserRepository.cs` | 第10/11行：`InsertAsync`/`UpdateAsync` 移除 `new` |
| `src/QP11.Core/Interfaces/ISupplierRepository.cs` | 第9/11/12行：`GetByIdAsync`/`InsertAsync`/`UpdateAsync` 移除 `new` |
| `src/QP11.Core/Interfaces/IPartRepository.cs` | 第11/15行：`GetByIdAsync`/`UpdateAsync` 移除 `new` |
| `src/QP11.Core/Interfaces/ISellRepository.cs` | 第18行：`UpdateAsync(BillSell)` 移除 `new`；第19行：`UpdateAsync(BillSell, IDbTransaction?)` 添加 `new` |

### 验证结果
`dotnet build f:\qp11\src\QP11.Core\QP11.Core.csproj --no-incremental` 编译通过：
```
已成功生成。
    0 个警告
    0 个错误
```

---

## 2026-08-08 修复 QP11.Data 项目编译警告

### 问题描述
QP11.Data 项目全量编译产生 79 个警告，分5类：
- CA1416（59处）：OleDbCompatConnection 类使用 Windows 专用 API
- CS8765（18处）：属性 setter 参数 nullability 与基类不匹配
- CS8619（12处）：仓储接口 GetByIdAsync 返回类型 nullability 不匹配
- CS8603（6处）：可能返回 null 引用
- CS0618（4处）：System.Data.SqlClient.SqlConnection 已过时

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Infrastructure/OleDbCompatConnection.cs` | 4个类添加 `[SupportedOSPlatform("windows")]`；属性 setter 添加 `#pragma warning disable CS8765`；DbConnection/DbTransaction 属性改为 `DbConnection?`/`DbTransaction?` |
| `src/QP11.Data/Infrastructure/OdbcCompatConnection.cs` | 属性 setter 添加 `#pragma warning disable CS8765`；DbConnection/DbTransaction 属性改为 `DbConnection?`/`DbTransaction?` |
| `src/QP11.Data/Infrastructure/DatabaseFactory.cs` | `System.Data.SqlClient.SqlConnection` 改为 `Microsoft.Data.SqlClient.SqlConnection`；ConnectionString setter 添加 `#pragma`；OleDb 调用处添加 `#pragma warning disable CA1416`；添加 `using System.Runtime.Versioning` |
| `src/QP11.Data/Repositories/BaseRepository.cs` | `GetByIdAsync` 返回类型改为 `Task<T?>` |
| `src/QP11.Core/Interfaces/IRepository.cs` | `GetByIdAsync` 返回类型改为 `Task<T?>` |
| `src/QP11.Data/Repositories/UserRepository.cs` | 显式实现返回类型改为 `Task<UserInfor?>` |
| `src/QP11.Data/Repositories/AccountRepository.cs` | 显式实现返回类型改为 `Task<Account?>` |
| `src/QP11.Data/Repositories/ClientRepository.cs` | 显式实现返回类型改为 `Task<ClientInfor?>` |
| `src/QP11.Data/Repositories/SupplierRepository.cs` | 显式实现返回类型改为 `Task<SupplierInfor?>` |
| `src/QP11.Data/Repositories/MemberCardRepository.cs` | 显式实现返回类型改为 `Task<MemberCard?>` |
| `src/QP11.Data/Repositories/PartRepository.cs` | 显式实现返回类型改为 `Task<PartData?>` |
| `src/QP11.Data/Repositories/BorrowRepository.cs` | 显式实现返回类型改为 `Task<Borrow?>` |
| `src/QP11.Data/Repositories/BuyRepository.cs` | 显式实现返回类型改为 `Task<BillBuy?>` |
| `src/QP11.Data/Repositories/SellRepository.cs` | 显式实现返回类型改为 `Task<BillSell?>` |
| `src/QP11.Data/Repositories/JhdhRepository.cs` | 显式实现返回类型改为 `Task<BillJhdh?>` |
| `src/QP11.Data/Repositories/ArrearageRepository.cs` | 显式实现返回类型改为 `Task<Arrearage?>` |
| `src/QP11.Data/Repositories/PaysRepository.cs` | 显式实现返回类型改为 `Task<Pays?>` |
| `src/QP11.Data/Repositories/SysLogRepository.cs` | 显式实现返回类型改为 `Task<SysLog?>` |

### 验证结果
`dotnet build f:\qp11\src\QP11.Data\QP11.Data.csproj --no-incremental`：0 警告 0 错误

---

## 2026-08-08 修复 QP11.WebApi 项目编译警告

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.WebApi/Program.cs` | `AddConsole` 改为 `AddSimpleConsole`，消除 CS0618（IncludeScopes 已过时） |
| `src/QP11.WebApi/Controllers/SellController.cs` | `kvp.Value` 后添加 `!` 消除 CS8602 |

### 验证结果
`dotnet build f:\qp11\src\QP11.WebApi\QP11.WebApi.csproj`：0 警告 0 错误

---

## 2026-08-08 修复 QP11.Wpf 项目编译警告

### 修改文件

| 文件 | 警告码 | 修改内容 |
|------|--------|---------|
| `Helpers/DatePickerHelper.cs` | CS8622 | `OnSelectedDateChanged` sender 参数改为 `object?` |
| `Views/MigrationWindow.xaml.cs` | CS8622 | `MigrationWindow_Closed` sender 参数改为 `object?` |
| `Views/SellQueryControl.xaml.cs` | CS8622 | `OnDateRangeChanged` sender 参数改为 `object?` |
| `Views/SellControl.xaml.cs` | CS8625/CS4014 | `DgParts_MouseDoubleClick` e 参数改为 `MouseButtonEventArgs?`；`null` 参数改为 `this, new RoutedEventArgs()`；`LoadPartList()` 改为 `_ = LoadPartList()` |
| `Views/PartSelectorWindow.xaml.cs` | CS8625/CS4014 | `DgParts_MouseDoubleClick` e 参数改为 `MouseButtonEventArgs?`；`Dispatcher.BeginInvoke` 前加 `_ =` |
| `Views/RegionWindow.xaml.cs` | CS8625 | `null` 改为 `null!` |
| `Views/ImagePreviewWindow.xaml.cs` | CS8604 | `bindings[i]` 后加 `!` |
| `Views/UserManagerWindow.xaml.cs` | CS8604 | `pwd`/`name` 后加 `!` |
| `Views/BuyReturnWindow.xaml.cs` | CS8604 | `sender` 后加 `!` |
| `Views/SellReturnControl.xaml.cs` | CS8604 | `App.CurrentUser.Name` 后加 `!` |
| `Views/MainWindow.xaml.cs` | CS8602 | 添加 `perm == null` 空检查 |
| `Views/DesktopControl.xaml.cs` | CS8602 | 添加 `perm == null` 空检查 |
| `Views/ReportCenterWindow.xaml.cs` | CS8600/CS8602 | `reader.Current` 后加 `!` |
| `Views/SellEditDialog.xaml.cs` | CS4014/CS8602 | `LoadSellHistoryAsync` 前加 `_ =`；`row.MaxPrice`/`row.MinPrice` 改为 `row!.MaxPrice`/`row!.MinPrice` |
| `Views/SellQueryControl.xaml.cs` | CS8600/CS8602/CS8604/CS4014 | `sourceItems[i]` 后加 `!`；`payMethod` 后加 `!`；`Dispatcher.BeginInvoke` 前加 `_ =` |
| `Views/SettingsWindow.xaml.cs` | CS8604/CS8602 | `.ToString()` 后加 `!`；`val` 后加 `!` |
| `Views/SupplierManagerWindow.xaml.cs` | CS0414 | 移除未使用的 `_isFiltering` 字段 |
| `Views/ClientSelectorWindow.xaml.cs` | CS0414 | 移除未使用的 `_isFiltering` 字段 |
| `Views/ClientManagerWindow.xaml.cs` | CS0414 | 移除未使用的 `_isFiltering` 字段 |

### 验证结果
全量编译 `dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj --no-incremental`：**0 警告 0 错误**

---

## 2026-08-09 修复数据迁移连接问题（ODBC→SqlClient→System.Data.SqlClient）

### 问题描述
1. 源数据库（SQL Server 2012）连接测试失败，ODBC 驱动不可用。
2. 改用 SqlClient 后，`@param` 参数转换失败，报"必须声明变量"。
3. 改用 `Microsoft.Data.SqlClient` 后，源库连接成功（SQL2012），但目标库（SQL 2000）报"内部连接致命错误"。

### 根因分析
**问题1（ODBC连接失败）**：`BuildConnStr` 写死了 `Driver={SQL Server}`，系统无此驱动。

**问题2（ODBC参数转换失败）**：Dapper 2.1.79 对 `OdbcConnection` 的 `@param`→`?` 转换未生效。

**问题3（SQL2000连接失败）**：`Microsoft.Data.SqlClient` v7.0.2 已放弃对 SQL Server 2000 的支持，TLS 握手失败（错误状态: 15）。

### 修复方案
**最终方案：使用 `System.Data.SqlClient` v4.9.0（兼容 SQL Server 2000~2019 全版本）。**
- `System.Data.SqlClient.SqlConnection` 原生支持 `@param` 命名参数
- 兼容 SQL Server 2000，无 TLS 兼容性问题
- `QP11.Data` 项目中已引用此包，直接复用

具体变更：
- `using Microsoft.Data.SqlClient;` → `using System.Data.SqlClient;`
- 连接串简化：去掉 `TrustServerCertificate=True;Encrypt=False;`（`System.Data.SqlClient` 不需要）

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | `using Microsoft.Data.SqlClient` 改为 `using System.Data.SqlClient`；`BuildConnStr` 去掉 `TrustServerCertificate=True;Encrypt=False;` |

### 验证结果
- `dotnet build src/QP11.Services/QP11.Services.csproj`：**0 错误**（3个CS0618警告：System.Data.SqlClient已过时，预期之内）
- `dotnet build src/QP11.Wpf/QP11.Wpf.csproj`：**0 错误**

---

## 2026-08-09 清空配件编码库（bs_code）数据

### 操作描述
格式化操作后，`bs_code` 表仍保留 20,117 条配件编码数据。应要求将此表数据全部删除。

### 操作内容
- 执行 `DELETE FROM bs_code` 清空全部数据
- 删除前：20,117 条记录
- 删除后：0 条记录

### 执行方式
- 通过 Python pymssql 直连数据库 `192.168.1.86,1433` 执行 DELETE 语句

---

## 2026-08-09 收缩数据库文件释放空间

### 操作描述
清空数据后，数据库文件仍占用 3.02 GB（数据文件 ~2GB，日志文件 ~1GB），需收缩释放空间。

### 操作内容
- 执行 `BACKUP LOG qipei WITH TRUNCATE_ONLY` 截断事务日志
- 执行 `DBCC SHRINKFILE (qipei, 20)` 收缩数据文件到 20 MB
- 执行 `DBCC SHRINKFILE (qipei_Log, 10)` 收缩日志文件到 10 MB

### 收缩前后对比

| 文件 | 收缩前 | 收缩后 | 实际已用 |
|------|--------|--------|----------|
| `qipei.mdf`（数据文件） | 2005.7 MB | 20.0 MB | 6.8 MB |
| `qipei_Log.ldf`（日志文件） | 1082.8 MB | 10.2 MB | 2.5 MB |
| **总计** | **3.02 GB** | **30.2 MB** | 9.3 MB |

### 执行方式
- 通过 Python pymssql 使用 autocommit 模式直连数据库执行 DBCC 命令

---

## 2026-08-09 清空剩余基础数据并收缩

### 操作内容
清空以下 7 张表的所有数据，并再次收缩数据库文件：

| 表名 | 说明 | 清空前行数 |
|------|------|-----------|
| `area` | 地区信息 | 3,062 |
| `CLASSES` | 分类 | 443 |
| `part_place` | 配件仓位 | 760 |
| `wuliu_infor` | 物流信息 | 27 |
| `part_sub` | 配件子类 | 47 |
| `business_infor` | 企业信息 | 1 |
| `sys_log` | 系统日志 | 7 |
| **合计** | | **4,347** |

### 收缩结果
| 文件 | 收缩前 | 收缩后 |
|------|--------|--------|
| `qipei.mdf` | 20.0 MB | 20.0 MB（已用 6.7 MB） |
| `qipei_Log.ldf` | 10.2 MB | 10.2 MB（已用 2.5 MB） |

---

## 2026-08-09 修复目标库（SQL 2000）连接失败问题

### 问题描述
测试目标库连接时，`System.Data.SqlClient` 报错：`内部连接致命错误。错误状态: 15，标记: 0`。源库（SQL Server 2012）连接正常。

### 根因分析
- **错误状态 15** = TLS/SSL 握手失败。
- SQL Server 2000 不支持 TLS 加密，而 `System.Data.SqlClient` 在 .NET 8 上默认会尝试协商 TLS 加密，导致 prelogin 阶段握手失败。
- 此前已验证 `Microsoft.Data.SqlClient` 也无法连接 SQL 2000，同属 TLS 兼容性问题。

### 修复方案
**源库和目标库使用不同的连接驱动：**

| 连接 | 驱动 | 说明 |
|------|------|------|
| 源库（SQL Server 2012） | `System.Data.SqlClient` | SqlClient 直连，兼容 SQL 2005+ |
| 目标库（SQL Server 2000） | `System.Data.Odbc` + `Driver={SQL Server}` | 经典 ODBC 驱动，使用旧版 TDS 协议，无需 TLS |

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/QP11.Services.csproj` | 添加 `System.Data.Odbc` v8.0.0 NuGet 引用 |
| `src/QP11.Services/MigrationService.cs` | 添加 `using System.Data.Odbc`；新增 `BuildOdbcConnStr()` 构建 ODBC 连接串；新增 `TestTargetConnection()` 使用 ODBC 测试目标库；`CreateTargetConnection()` 改为返回 `OdbcConnection`；`RunMigration()` 目标连接串改为 `BuildOdbcConnStr` |
| `src/QP11.Wpf/Views/MigrationWindow.xaml.cs` | `BtnTestTarget_Click` 中调用 `TestTargetConnection` 替代 `TestConnection` |

### 验证结果
- `dotnet build src/QP11.Services/QP11.Services.csproj`：**0 错误**（2个CS0618警告：System.Data.SqlClient已过时，预期之内）
- `dotnet build src/QP11.Wpf/QP11.Wpf.csproj`：**0 错误**

---

## 2026-08-09 修复ODBC目标库参数化查询失败（必须声明变量 '@xxx'）

### 问题描述
数据迁移时，目标库（SQL Server 2000，ODBC驱动）执行INSERT/SELECT报错：
```
系统设置迁移失败: ERROR [42000] [Microsoft][ODBC SQL Server Driver][SQL Server]必须声明变量 '@username'。
配件数据迁移失败: ERROR [42000] [Microsoft][ODBC SQL Server Driver][SQL Server]必须声明变量 '@partno'。
```
源库连接和目标库连接测试均成功，但实际迁移时所有带参数的SQL均失败。

### 根因分析
SQL Server 2000的ODBC驱动（`Driver={SQL Server}`）**不支持`@param`命名参数**，只支持`?`位置占位符。Dapper 2.1.79 不会自动将`@param`转换为`?`，导致ODBC驱动将`@username`等视为未声明的T-SQL变量。

此前（见2026-08-09 修复数据迁移连接问题）已发现此问题，当时尝试改用SqlClient解决，但SqlClient因TLS兼容性无法连接SQL Server 2000，最终目标库回退到ODBC，参数问题遗留至今。

### 修复方案
**在MigrationService中新增ODBC参数转换辅助方法，绕过Dapper直接使用原生OdbcCommand执行目标库操作：**

1. `ConvertToOdbcParameters(sql, param)` — 使用正则表达式`@(?!@)(\w+)`将SQL中的`@param`替换为`?`，通过反射从匿名参数对象中按出现顺序提取值，创建`OdbcParameter`数组（bool类型自动转为0/1）
2. `TargetExecuteAsync(conn, sql, param, transaction)` — 转换参数后使用原生`OdbcCommand.ExecuteNonQueryAsync()`执行INSERT/UPDATE
3. `TargetQueryFirstOrDefaultAsync<T>(conn, sql, param, transaction)` — 转换参数后使用原生`OdbcCommand.ExecuteScalarAsync()`查询首行首列值

然后将所有目标库的Dapper调用（`target.ExecuteAsync` / `target.QueryFirstOrDefaultAsync`）替换为上述辅助方法，共计替换31处。

**额外修复**：采购迁移和销售迁移中，事务内的`SELECT partid`查询原本未传递transaction参数，在ODBC中会导致"connection is busy"错误，现已补上transaction参数。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | 添加`using System.Text.RegularExpressions`；新增`#region ODBC参数转换辅助`区域（3个辅助方法）；将全部31处目标库Dapper调用替换为`TargetExecuteAsync`/`TargetQueryFirstOrDefaultAsync`；采购/销售迁移中2处事务内查询补传transaction参数 |

### 验证结果
- `dotnet build src/QP11.Services/QP11.Services.csproj --no-incremental`：**0 错误**（2个CS0618警告：System.Data.SqlClient已过时，预期之内）
- `dotnet build src/QP11.Wpf/QP11.Wpf.csproj`：**0 错误**

2026-08-09 修复数据迁移模块两个错误
背景
第三方数据迁移模块在执行迁移时报两个错误：

系统设置迁移失败：无法将 NULL 值插入列 'id'，表 'qipei.dbo.business_infor'；该列不允许空值。INSERT 失败。
配件数据迁移异常：ERROR [HY010] [Microsoft][ODBC SQL Server Driver]函数序列错误
原因分析
通过 PowerShell 连接目标库（SQL Server 2000，ODBC）查询 syscolumns.status & 128 确认各表主键的 IDENTITY 属性：

表.列	IsIdentity	代码是否提供值	结论
business_infor.id	0（否）	否	错误根因：非自增列未提供值导致 NULL 报错
part_data.partid	0（否）	否（依赖 SCOPE_IDENTITY()）	错误根因：非自增列 + INSERT...SELECT 批处理导致 ODBC HY010
detail_buy.id	1（是）	否	OK
detail_sell.id	1（是）	否	OK
arrearage.id	1（是）	否	OK
pays.id	1（是）	否	OK
account.id	1（是）	否	OK
错误1：business_infor.id 非自增列，原 INSERT 未提供 id 值，违反 NOT NULL 约束。
错误2：part_data.partid 非自增列，原代码使用 INSERT ...; SELECT CAST(SCOPE_IDENTITY() AS bigint) 批处理语句并通过 ExecuteScalarAsync 执行。由于 partid 非 IDENTITY，SCOPE_IDENTITY() 返回 NULL；同时 ODBC 驱动处理多语句批处理时 INSERT 行计数结果集未消费，触发 HY010 函数序列错误。
修复内容
1. business_infor INSERT 显式提供 id 值
文件：src/QP11.Services/MigrationService.cs（第 217 行）
问题：INSERT 语句未包含 id 列，而 id 非自增且 NOT NULL。
修复：在 INSERT 列表和 VALUES 中添加 id，值为 1（该处代码已通过 SELECT COUNT(1) FROM business_infor 检查 exists == 0，仅表为空时插入，故 id=1 安全）。
2. part_data INSERT 拆分为两步，显式生成 partid
文件：src/QP11.Services/MigrationService.cs（第 348-375 行）
问题：原代码用 INSERT ...; SELECT SCOPE_IDENTITY() 批处理获取自增 id，但 partid 非自增列，且批处理导致 ODBC HY010 错误。
修复：
先执行 SELECT ISNULL(MAX(partid), 0) + 1 FROM part_data 获取下一个 partid；
再执行单独的 INSERT 语句（不带 SELECT），显式提供 partid 值；
_partIdMapping[nno] 直接使用生成的 nextPartId，不再依赖 SCOPE_IDENTITY()。
验证
执行 dotnet build src/QP11.Services/QP11.Services.csproj，结果：


Plain Text

1
2
已成功生成。
    0 个错误
（2 个 CS0618 警告为既有的 SqlConnection 过时提示，非本次引入）

2026-08-09 财务三表迁移前清空避免重复
背景
财务三表（arrearage、pays、account）为流水表，id 为自增主键，无业务唯一键。原迁移代码无去重检查，重新迁移会追加重复记录。用户选择"清空三表后重迁"方案。

修复内容
文件：src/QP11.Services/MigrationService.cs（第 865-869 行）
修复：在 MigrateFinance() 方法开头、读取源数据前，执行三条 DELETE 清空目标库三表：

SQL

1
2
3
DELETE FROM arrearage
DELETE FROM pays
DELETE FROM account
仅清空这三张流水表，其他基础/业务表（配件、客户、供应商、单据等）有各自业务唯一键去重检查，不受影响。
验证
执行 dotnet build src/QP11.Services/QP11.Services.csproj，结果：


Plain Text

1
2
已成功生成。
    0 个错误
（2 个 CS0618 警告为既有的 SqlConnection 过时提示，非本次引入）

2026-08-09 修复供应商迁移"列名 'note' 无效"错误
背景
数据迁移在系统设置、配件、客户三步成功后，迁移供应商时报错： ERROR [42S22] [Microsoft][ODBC SQL Server Driver][SQL Server]列名 'note' 无效。

原因分析
对比 current_db_schema.txt 中两张表的实际 schema：

client_infor 有 note|varchar|200|YES 列（客户迁移成功的原因）
supplier_infor 实际列：sid, name, address, linkman, tel, fax, mobile, zip, level, credit, bank, tax, class, name_py ——没有 note 列
代码中供应商 INSERT 语句却包含了 note 列，导致 SQL Server 报列名无效。docs/数据库字段说明.md（第 411-430 行）将 supplier_infor 描述为有 note 列，与真实 schema 不一致；本次修复以实际 schema 为准（不修改文档，避免在未确认的情况下扩大改动范围）。

修复内容
文件：src/QP11.Services/MigrationService.cs（第 478-494 行）
修复：从 supplier_infor 的 INSERT 列表和 VALUES 中移除 note 列及对应的 note = ... 参数赋值，其他字段保持不变。
验证
执行 dotnet build src/QP11.Services/QP11.Services.csproj，结果：


Plain Text

1
2
已成功生成。
    0 个错误
（2 个 CS0618 警告为既有的 SqlConnection 过时提示，非本次引入）

2026-08-09 修复库存迁移"input string 'WLL N31291'"格式错误
背景
配件/客户/供应商/仓位迁移成功后，迁移库存时报错： The input string 'WLL N31291' was not in a correct format.

原因分析
错误发生在 MigrateStock() 的 long.Parse(partIdStr)（第 570 行）。partIdStr 来自 _partIdMapping[nno]，正常应为数字（part_data.partid），但实际值为 "WLL N31291"（配件业务编号）。

追查发现根因在 MigrateParts() 的"跳过已存在"分支（原第 329 行）：


C#

1
2
3
4
5
6
if (existCount > 0)
{
    skipped++;
    _partIdMapping[nno] = nno;  // 错误：把业务编号字符串当 partid 存入
    continue;
}
当配件已存在时，代码跳过了 INSERT，也跳过了 partid 查询，直接把 nno（业务编号字符串，如 "WLL N31291"）存入映射表。而 part_data.partid 是 bigint 数字主键，与 partno（业务编号 varchar）是不同字段。

对比 client_infor.cid 和 supplier_infor.sid（均为 varchar 字符串主键，值就是 gno/gyno），那两处 _clientIdMapping[gno] = gno 和 _supplierIdMapping[gyno] = gyno 是正确的；唯独 part_data 的主键 partid 是数字，不能用 nno 代替。

修复内容
文件：src/QP11.Services/MigrationService.cs（第 326-332 行）
修复：在"跳过已存在"分支中查询已存在配件的真实 partid 存入映射表：

C#

1
2
3
var existPartId = await TargetQueryFirstOrDefaultAsync<long>(target,
    "SELECT partid FROM part_data WHERE partno=@partno", new { partno = nno });
_partIdMapping[nno] = existPartId.ToString();
验证
执行 dotnet build src/QP11.Services/QP11.Services.csproj，结果：


Plain Text

1
2
已成功生成。
    0 个错误
（2 个 CS0618 警告为既有的 SqlConnection 过时提示，非本次引入）

2026-08-09 修复库存迁移"PRIMARY KEY 约束 'PK_part_stock'"冲突
背景
库存迁移报主键冲突： 违反了 PRIMARY KEY 约束 'PK_part_stock'。不能在对象 'part_stock' 中插入重复键。

原因分析
通过 ODBC 查询 INFORMATION_SCHEMA 确认 part_stock 的主键只有 partid 一列（单列主键，一配件一行）：

现有 1638 行，不同 partid = 1638，不同 place = 98，空 place = 366 → 证实 place 不是主键的一部分。
而原代码去重检查用的是 WHERE partid=@partid AND place=@place（误以为是 partid+place 联合主键）。源库 tbisto 中同一配件有多仓位多行，第一行插入成功后，第二行 partid 相同但 place 不同——去重检查通过（误判为不重复），INSERT 时触发 partid 单列主键冲突。

注意：docs/数据库字段说明.md（5.2 节）描述 part_stock 主键为 partid，与实际一致；但代码误用联合主键去重，与文档/实际不符。

修复方案（用户确认"合并求和"）
part_stock 主键仅 partid（一配件一行），源库同一配件可能多仓位多行。按 nno 分组合并：数量求和、取首个仓位 place。

修复内容
文件：src/QP11.Services/MigrationService.cs（第 541-617 行，MigrateStock() 方法）
修复：
读取源数据后按 nno 分组，kcamount 求和、posi/iprj/oprc 取首行；
去重检查改为仅按 partid（SELECT COUNT(1) FROM part_stock WHERE partid=@partid），partid 已存在则跳过；
INSERT 用分组后的合并值；
日志输出"迁移 N 条 (合并自 M 行)"，便于核对。
验证
执行 dotnet build src/QP11.Services/QP11.Services.csproj，结果：


Plain Text

1
2
已成功生成。
    0 个错误
（2 个 CS0618 警告为既有的 SqlConnection 过时提示，非本次引入）

---

## 2026-08-09 修复迁移页进度文字被"开始迁移"按钮遮挡

### 问题描述
数据迁移页面中，进度条后的文字（如"配件数据 (3/10)"）紧挨着"开始迁移"按钮，文字被按钮遮挡。

### 根因分析
`txtProgress`（Column 2）与 `btnMigrate`（Column 3）之间没有间距，Column 为 Auto 宽度，文字边缘紧贴按钮。

### 修复方案
给 `txtProgress` 添加右边距 `Margin="0,0,10,0"`，将文字与按钮隔开。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/MigrationWindow.xaml` | 第99-100行：`txtProgress` 添加 `Margin="0,0,10,0"` |

---

## 2026-08-09 修复供应商迁移只迁移了3条的问题

### 问题描述
源库 tbgugys 表有 80 条供应商记录，迁移后目标库 supplier_infor 只插入了 3 条。

### 根因分析
源表 `tbgugys.gyno` 是 `char(10)` 固定长度类型：
- SQL Server 会自动用空格填充到 10 位
- 大部分供应商的 `gyno` 实际就是空值（10 个空格）
- 代码中 `Trim()` 后变成空字符串，被 `string.IsNullOrEmpty(gyno)` 跳过
- 只有那 3 个有实际编号的供应商被迁移

### 修复方案
当 `gyno` 为空时，用 `id` 字段（int，NOT NULL）生成唯一标识作为 `sid`，格式为 `"S" + id`（如 `"S1"`、`"S2"`）。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | 第466-471行：移除 `if (string.IsNullOrEmpty(gyno)) continue;` 改为用 `"S" + row.id` 生成 sid |

### 验证结果
`dotnet build src/QP11.Services/QP11.Services.csproj`：**0 错误**（2个CS0618警告为既有的 SqlConnection 过时提示，非本次引入）

---

## 2026-08-09 修复供应商迁移数据与采购单据供应商不匹配

### 问题描述
供应商迁移完成后，进货历史中供应商全部显示空白，进货订单的供应商显示的是字母+数字的id（如`KH014`），无法关联到供应商名称。

### 根因分析
通过连接源库（frp-bar.com:47017）和目标库（192.168.1.86:1433）对比数据发现：

**`tbgugys`（供应商主表）和 `tbistoed`（入库明细）使用两套完全不同的 `gyno` 编号体系：**

| 数据源 | gyno 值 | 说明 |
|--------|---------|------|
| `tbgugys` | `0001`、`0002`、`0003`（3条），77条为空 | 旧编号体系，几乎废弃 |
| `tbistoed` | `KH01`、`KH014`、`KH02426`等（429个不同值） | 实际使用的编号体系 |
| 交集 | 仅 `0001` 一条 | 两套编号几乎无交集 |

供应商迁移从 `tbgugys` 取数据（sid=`0001`/`S4`/`S5`...），但采购迁移从 `tbistoed` 取数据，`bill_buy.supplier` 存的是 `KH014` 等值，在 `supplier_infor` 中找不到匹配记录。

此外，`tbistoed` 中有 5436 条 gyno 为空的记录（占 2.6%），这些记录的 `bill_buy.supplier` 直接为空字符串。

### 修复方案

**1. 重写 `MigrateSuppliers()`：**
- 从 `tbistoed` 提取去重的 `gyno`+`gyname` 组合作为供应商主数据源（这是采购单据实际引用的编号）
- 合并 `tbgugys` 中 gyno 非空但 `tbistoed` 中未出现的供应商
- 从 `tbgugys` 中按 gyno 或 gyname 匹配补充详细信息（地址、电话等）
- gyno 为空时尝试用 gyname 在 `tbgugys` 中查找匹配

**2. 修复 `MigratePurchases()` 中空供应商问题：**
- 当 `tbistoed.gyno` 为空但 `gyname` 非空时，用 `gyname` 在目标库 `supplier_infor` 中查找对应的 sid

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | 重写 `MigrateSuppliers()` 方法（第450-567行）：改为从 `tbistoed` 提取去重供应商+合并 `tbgugys` 详细信息 |
| `src/QP11.Services/MigrationService.cs` | 修改 `MigratePurchases()` 方法（第706-720行）：gyno 为空时用 gyname 查找供应商 sid |

### 验证结果
`dotnet build src/QP11.Services/QP11.Services.csproj`：**0 错误**（2个CS0618警告为既有的 SqlConnection 过时提示，非本次引入）

---

## 2026-08-09 Repository 批量改为异步打开数据库连接（CodeRule/Desktop/Jhdh/Logistics/PartBatch/PartImage）

### 背景
`DatabaseFactory.Create()` 已改为返回未打开连接。`BaseRepository` 已新增 `protected async Task<DbConnection> CreateConnectionAsync()` 方法（创建连接 + await OpenAsync）。各具体 Repository 继承自 BaseRepository，需要把对 `CreateConnection()` 的调用改为 `await CreateConnectionAsync()`。

### 修改规则
- 规则1：`using var db = CreateConnection();` → `using var db = await CreateConnectionAsync();`
- 规则2：`var db = transaction?.Connection ?? CreateConnection();` → `var db = transaction?.Connection ?? await CreateConnectionAsync();`
- 特殊：`CodeRuleRepository.cs` 中 if 块内的 `db = CreateConnection();` → `db = await CreateConnectionAsync();`
- 保留 `protected DbConnection CreateConnection() => DatabaseFactory.Create();` 方法定义不动
- 不修改方法签名、不添加 using 语句、不修改其他任何代码

### 修改文件

| 文件 | 修改处数 | 说明 |
|------|---------|------|
| `src/QP11.Data/Repositories/CodeRuleRepository.cs` | 6 | 5处规则1（using var）+ 1处特殊（if 块赋值 `db = CreateConnection()`） |
| `src/QP11.Data/Repositories/DesktopRepository.cs` | 3 | 3处规则1 |
| `src/QP11.Data/Repositories/JhdhRepository.cs` | 11 | 4处规则1 + 7处规则2（transaction?.Connection ?? 模式） |
| `src/QP11.Data/Repositories/LogisticsRepository.cs` | 5 | 5处规则1 |
| `src/QP11.Data/Repositories/PartBatchRepository.cs` | 5 | 5处规则1 |
| `src/QP11.Data/Repositories/PartImageRepository.cs` | 3 | 3处规则1 |
| **合计** | **33** | |

### 验证结果
对 `f:\qp11\src\QP11.Data\Repositories\` 目录执行 Grep `CreateConnection\(\)` 确认：6 个目标文件中除 `protected DbConnection CreateConnection() => DatabaseFactory.Create();` 方法定义行外，无遗漏的 `CreateConnection()` 调用。其余未在任务范围内的 Repository 文件未做改动。

---

## 2026-08-09 Repository 批量改为异步打开数据库连接

### 背景
`DatabaseFactory.Create()` 已改为返回未打开连接。`BaseRepository` 已新增 `protected async Task<DbConnection> CreateConnectionAsync()` 方法（创建连接 + await OpenAsync）。各具体 Repository 继承自 BaseRepository，需要把对 `CreateConnection()` 的调用改为 `await CreateConnectionAsync()`。

### 修改规则
- 规则1：`using var db = CreateConnection();` → `using var db = await CreateConnectionAsync();`
- 规则2：`var db = transaction?.Connection ?? CreateConnection();` → `var db = transaction?.Connection ?? await CreateConnectionAsync();`
- 保留 `protected DbConnection CreateConnection() => DatabaseFactory.Create();` 方法定义不动
- 不修改方法签名、不添加 using 语句、不修改其他代码

### 修改文件列表

| 文件 | 规则1处数 | 规则2处数 | 合计 |
|------|-----------|-----------|------|
| `src/QP11.Data/Repositories/AccountRepository.cs` | 4 | 2 | 6 |
| `src/QP11.Data/Repositories/ArrearageRepository.cs` | 8 | 4 | 12 |
| `src/QP11.Data/Repositories/BaosunRepository.cs` | 6 | 0 | 6 |
| `src/QP11.Data/Repositories/BuyRepository.cs` | 9 | 7 | 16 |
| `src/QP11.Data/Repositories/CarMarkRepository.cs` | 6 | 0 | 6 |
| `src/QP11.Data/Repositories/ClientRepository.cs` | 5 | 0 | 5 |
| **合计** | **38** | **13** | **51** |

### 验证结果
对 6 个文件逐一 Grep `CreateConnection()`，每个文件仅剩 1 行方法定义 `protected DbConnection CreateConnection() => DatabaseFactory.Create();`，无遗漏的同步调用。Grep `await CreateConnectionAsync()` 共 51 处，与修改数一致。

---

## 2026-08-09 修复软件新开页面和切换页面卡顿10秒（异步Open总览）

### 问题描述
软件新开页面和切换页面都要卡 10 秒左右，严重影响使用体验。

### 根因分析
`DatabaseFactory.Create()` 在创建连接后会**同步调用 `connection.Open()`** 打开数据库连接。由于数据库为远程 SQL Server 2000，每次同步 `Open()` 耗时 1～3 秒，而每次页面加载/切换会触发 3～5 次 `Open()` 调用（多个 Repository 各自创建连接），累计阻塞 UI 线程约 10 秒。

同步 `Open()` 直接运行在 UI 线程上，期间界面完全冻结无响应。

### 修复方案
采用**方案A：异步 Open（治本）**——将所有同步 `Open()` 改为异步 `OpenAsync()`，使数据库连接握手在后台线程执行，UI 线程不再阻塞。数据库版本为 SQL Server 2000，SqlClient 完全兼容，无需更换驱动；保留 OleDb 提供程序并确保连接池正常工作。

### 修改总览

#### 1. 核心基础设施层

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Infrastructure/DatabaseFactory.cs` | `Create()` 方法移除同步 `connection.Open()`，改为返回**未打开**连接；`TryOpenOleDb`/`TryOpenOdbc`/SqlClient 回退分支改为 Open() 验证后 Close()，返回未打开连接 |
| `src/QP11.Core/Interfaces/IDbConnectionFactory.cs` | 新增 `Task<DbConnection> CreateAsync()` 方法 |
| `src/QP11.Data/Infrastructure/DbConnectionFactory.cs` | 实现 `CreateAsync()`：调用 `DatabaseFactory.Create()` 后 `await OpenAsync()` |
| `src/QP11.Data/Infrastructure/UnitOfWork.cs` | `_connection` 类型由 `IDbConnection` 改为 `DbConnection`（支持 `OpenAsync()`）；`BeginTransactionAsync` 改为 `await _connection.OpenAsync()` |
| `src/QP11.Data/Repositories/BaseRepository.cs` | 新增 `protected async Task<DbConnection> CreateConnectionAsync()`（创建 + await OpenAsync）；`GetByIdAsync`/`GetAllAsync`/`InsertAsync`/`UpdateAsync`/`DeleteAsync`/`CountAsync` 改用 `await CreateConnectionAsync()` |

#### 2. Repository 层（异步打开连接）

继承自 `BaseRepository` 的具体 Repository 中，将 `CreateConnection()` 调用改为 `await CreateConnectionAsync()`；对未继承 `BaseRepository` 而直接实现接口的 Repository，新增同名 `CreateConnectionAsync()` 方法。

| 范围 | 文件 | 说明 |
|------|------|------|
| 新增方法 | `src/QP11.Data/Repositories/PartRepository.cs` | 新增 `CreateConnectionAsync()`；`GetStockByIdAsync` 等改用异步 |
| 新增方法 | `src/QP11.Data/Repositories/SellRepository.cs` | 新增 `CreateConnectionAsync()` |
| 新增方法 | 其余 24 个 Repository（如 `UserRepository.cs`、`BaosunRepository.cs` 等） | 新增 `CreateConnectionAsync()` |
| 改为 await | `CodeRuleRepository.cs`/`DesktopRepository.cs`/`JhdhRepository.cs`/`LogisticsRepository.cs`/`PartBatchRepository.cs`/`PartImageRepository.cs` | 详见上方"Repository 批量改为异步打开数据库连接（CodeRule/Desktop/Jhdh/Logistics/PartBatch/PartImage）"小节（33 处） |
| 改为 await | `AccountRepository.cs`/`ArrearageRepository.cs`/`BaosunRepository.cs`/`BuyRepository.cs`/`CarMarkRepository.cs`/`ClientRepository.cs` | 详见上方"Repository 批量改为异步打开数据库连接"小节（51 处） |

#### 3. ViewModel / View 层

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/SellControl.xaml.cs` | 改用 `await dbFactory.CreateAsync()`；修复 async void 方法 |
| `src/QP11.Wpf/Views/BuyEditDialog.xaml.cs` | 改用 `await dbFactory.CreateAsync()`；修复 async void 方法 |

#### 4. WebApi 控制器层

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.WebApi/Controllers/SellController.cs` | 改用 `await _dbFactory.CreateAsync()` |
| `src/QP11.WebApi/Controllers/PartsController.cs` | 改用 `await _dbFactory.CreateAsync()` |
| `src/QP11.WebApi/Controllers/ClientsController.cs` | 改用 `await _dbFactory.CreateAsync()` |

#### 5. 特例（保留同步）

| 文件 | 处理方式 | 原因 |
|------|---------|------|
| `src/QP11.Wpf/Views/BillPrintSettingsWindow.xaml.cs` | 保留同步 `dbFactory.Create()` + `db.Open()` | 打印预览非关键路径，改为 async 需牵动更大范围改动，权衡后保留同步 |

### 验证结果
全量编译两个主项目，结果如下：
- `dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj --no-incremental`：**0 错误**（2 个 CS0618 警告为既有的 `System.Data.SqlClient` 过时提示，非本次引入）
- `dotnet build f:\qp11\src\QP11.WebApi\QP11.WebApi.csproj --no-incremental`：**0 错误**（同上 2 个 CS0618 警告）

### 预期效果
页面加载/切换时，数据库连接握手在后台线程异步进行，UI 线程不再被 `Open()` 同步阻塞，页面切换由卡顿 ~10 秒降至接近瞬时响应。

---

## 2026-08-09 修复登录窗口连接测试报错（ExecuteScalar requires an open and available Connection）

### 问题描述
登录窗口打开时报错：`ExecuteScalar requires an open and available Connection. The connection's current state is closed`。

### 根因分析
异步 Open 重构（见上方"修复软件新开页面和切换页面卡顿10秒"）将 `DatabaseFactory.Create()` 改为返回**未打开**连接，但遗漏了 `DatabaseFactory.TestConnection()` 方法——该方法调用 `Create()` 后未 `Open()` 就直接 `cmd.ExecuteScalar()`，导致连接状态为 closed 时抛异常。

调用链：`LoginWindow.TestDbConnectionAsync()` → `_dbInfoService.TestConnection` → `DatabaseFactory.TestConnection()` → `Create()`（未打开）→ `ExecuteScalar()`（抛错）。

### 修复方案
在 `TestConnection()` 的 `ExecuteScalar()` 前补上 `db.Open()`。该方法为同步签名（`out string message`），且 `LoginWindow` 已用 `Task.Run` 包装调用，不会阻塞 UI 线程，故保持同步 `Open()`。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Infrastructure/DatabaseFactory.cs` | 第465-467行：`Create()` 后补 `db.Open()` 再 `ExecuteScalar()` |

### 验证结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj`：**0 错误**（2 个 CS0618 警告为既有的 `System.Data.SqlClient` 过时提示）

---

## 2026-08-09 添加页面切换/查询性能调试日志（定位卡顿与查不到配件）

### 背景
异步 Open 重构后用户反馈卡顿依旧，且销售开单出现"有库存的配件查不到"的新问题。需添加调试日志定位真正的瓶颈和查询异常，不能凭猜测下结论。

### 日志埋点位置

| 层级 | 文件 | 埋点内容 |
|------|------|---------|
| 连接层 | `src/QP11.Data/Infrastructure/DbConnectionFactory.cs` | `CreateAsync` 中 `OpenAsync` 耗时 |
| 连接层 | `src/QP11.Data/Repositories/BaseRepository.cs` | `CreateConnectionAsync` 中 `OpenAsync` 耗时（含仓储类名） |
| 连接层 | `src/QP11.Data/Repositories/PartRepository.cs` | `CreateConnectionAsync` 中 `OpenAsync` 耗时 |
| 查询层 | `src/QP11.Data/Repositories/PartRepository.cs` | `GetStockListAsync` / `GetStockListAdvancedAsync` 记录查询参数、返回行数、执行耗时 |
| ViewModel | `src/QP11.Wpf/ViewModels/SellViewModel.cs` | `LoadPartListAsync` 记录走简单/高级查询分支、拼音参数、废品仓过滤前后行数 |
| 页面层 | `src/QP11.Wpf/Views/MainWindow.xaml.cs` | `OpenTab` 区分新建/切换已存在Tab；`OpenFunctionTab` 记录创建控件耗时与总耗时 |
| 页面层 | `src/QP11.Wpf/Views/SellControl.xaml.cs` | `LoadDropdowns` 3个下拉并行查询耗时；`LoadPartList` 查询参数、返回行数、耗时 |

### 日志标签
- `[PERF]` — 性能耗时类日志
- `[QUERY]` — 数据库查询类日志（含参数与返回行数）

### 验证结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj`：**0 错误**（4 个警告：2 个 CS0618 既有 + 2 个 CS8603 新引入局部变量可能的 null 返回，不影响功能）

### 后续
运行软件后，按以下顺序查看日志定位问题：
1. 页面卡顿：看 `[PERF] MainWindow.OpenFunctionTab` 总耗时 → 拆分为"创建控件"vs"OpenTab"；再看 `[PERF] *.CreateConnectionAsync OpenAsync` 是否仍为瓶颈
2. 查不到配件：看 `[QUERY] PartRepository.GetStockListAdvancedAsync` 的参数值与返回行数；若返回0行则对比 SQL 条件与数据库实际数据

---

## 2026-08-09 Serilog 增加 Debug 输出（VS输出窗口看不到日志）

### 问题描述
添加性能调试日志后，在 VS 输出窗口看不到任何 `[PERF]`/`[QUERY]` 日志输出。

### 根因分析
`App.xaml.cs` 的 `LoggerConfiguration` 只配置了 `.WriteTo.File(...)` 写日志文件，未配置 Debug sink，因此 VS 输出窗口（Debug 窗口）收不到日志。同时未显式设置 `MinimumLevel`，默认为 `Information`，但为确保调试日志一定输出，显式指定 `MinimumLevel.Information()`。

### 修复方案
1. 引用 `Serilog.Sinks.Debug` v3.0.0 NuGet 包
2. 在 `LoggerConfiguration` 中添加 `.WriteTo.Debug(outputTemplate: "[{Level:u3}] {Message:lj}{NewLine}")`
3. 显式设置 `.MinimumLevel.Information()` 确保调试日志级别输出

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/QP11.Wpf.csproj` | 第15行：新增 `Serilog.Sinks.Debug` v3.0.0 包引用 |
| `src/QP11.Wpf/App.xaml.cs` | 第61-67行：`LoggerConfiguration` 增加 `.MinimumLevel.Information()` 和 `.WriteTo.Debug(...)` |

### 日志输出位置
- **VS 输出窗口**（调试时实时查看）：`[Level] 消息` 格式
- **日志文件**（程序目录 `logs/qp11-YYYYMMDD.log`）：`时间 [Level] 消息` 格式，含异常堆栈

### 验证结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj`：**0 错误**

---

## 2026-08-09 根据调试日志修复卡顿根因与查不到配件问题

### 调试日志分析结果

用户运行后日志显示：
```
[PERF] OpenAsync 耗时 0ms              ← 连接池已生效，连接不是瓶颈
[QUERY] GetStockListAsync keyword=null top=200 返回200行 耗时9486ms  ← 查询本身9.5秒！
[QUERY] GetStockListAdvancedAsync partNo=gm-8-048 返回0行 耗时18ms  ← 查不到配件
```

**结论**：之前的异步Open重构方向正确（OpenAsync 0ms），但真正瓶颈是 SQL 查询本身慢（9.5秒），不是连接握手。

### 问题1：卡顿根因——无条件TOP200查询慢

**根因**：`GetStockListAsync` 无条件 TOP 200 查询的 `ORDER BY part_data.partno ASC` 对全表排序，`partno` 列无索引时极慢。对比有条件查询（`LIKE '%gm%'`）仅19ms——先过滤到135行再排序，速度快。

**修复**：
1. SQL等价改写（不影响结果集）：
   - `LEFT JOIN` → `INNER JOIN`（WHERE条件已排除右表NULL，等价但优化器处理更好）
   - `(DEL IS NULL OR DEL = '0')` → `ISNULL(DEL, '0') = '0'`（避免OR，可利用索引）
2. `SellControl_Loaded` 中 `await LoadPartList()` 改为 `_ = LoadPartList()`（fire-and-forget），窗口立即显示不等待查询完成

### 问题2：查不到配件（gm-8-048 返回0行）

**对比分析**：
- **库存管理** `GetStockListAsync(keyword)`：keyword 匹配 `name`、`partno`、`name_py`、`carname`、`cartype`、`cartype_py`、`class`、`memo` **8个字段**
- **销售开单** `GetStockListAdvancedAsync(partNo)`：纯ASCII输入只匹配 `partno`、`name_py` **2个字段**

如果 `gm-8-048` 存在于 `name` 字段而非 `partno` 字段，库存管理能查到但销售开单查不到。

**诊断措施**：在 `GetStockListAsync` 和 `GetStockListAdvancedAsync` 中，当返回0行时打印完整SQL和参数值（`[QUERY-EMPTY]` 日志），用户可拿去SSMS直接执行验证。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Repositories/PartRepository.cs` | `GetStockListAsync`：LEFT JOIN→INNER JOIN、(DEL IS NULL OR DEL='0')→ISNULL(DEL,'0')='0'；返回0行时打印完整SQL+参数 |
| `src/QP11.Data/Repositories/PartRepository.cs` | `GetStockListAdvancedAsync`：同上SQL优化；返回0行时打印完整SQL+参数（含DynamicParameters全部值） |
| `src/QP11.Wpf/Views/SellControl.xaml.cs` | `SellControl_Loaded`：`await LoadPartList()` 改为 `_ = LoadPartList()`，窗口先显示不等待查询 |

### 待用户验证
1. **卡顿**：运行后看 `[QUERY] GetStockListAsync` 耗时是否从9.5秒降低。若仍慢，需在数据库给 `part_data.partno` 列加非聚集索引：
   ```sql
   CREATE INDEX IX_part_data_partno ON part_data(partno)
   ```
2. **查不到配件**：运行后搜 `gm-8-048`，看VS输出窗口的 `[QUERY-EMPTY]` 日志，把完整SQL拿去SSMS执行，确认是数据问题还是查询条件问题

### 验证结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj`：**0 错误**（4个警告均为既有的CS0618/CS8603）

---

## 2026-08-09 卡顿已修复（9.5秒→9毫秒）+ 查不到配件根因确认（数据迁移问题）

### 调试日志验证结果

用户运行后日志显示卡顿已彻底解决：
```
[QUERY] GetStockListAsync keyword=null top=200 返回200行 耗时11ms  ← 原9486ms，提升1000倍
[QUERY] GetStockListAsync keyword=null top=200 返回200行 耗时7ms   ← 稳定在7-11ms
```

**ISNULL 优化生效**：`(DEL IS NULL OR DEL='0')` → `ISNULL(DEL,'0')='0'` 避免了 OR 导致的全表扫描，查询从9.5秒降至9毫秒。

### 查不到配件根因确认

通过 PowerShell 直连数据库查询确认：
```
partno=GM-8-048 | name=半轴总成615MM | cartype=新君威2.0AT-R | DEL=空
排序规则: Chinese_PRC_CI_AS (大小写不敏感，小写gm能匹配大写GM)
part_data 表有记录: 1行
part_stock 表关联记录: 0行  ← 根因！
```

**根因**：`gm-8-048` 在 `part_data` 表存在，但 `part_stock` 表**没有对应库存记录**。查询以 `part_stock` 为主表 LEFT JOIN `part_data`，所以 `part_data` 有但 `part_stock` 没有的配件查不到。

### 处理决策

**查询逻辑保持不变**（`FROM part_stock LEFT JOIN part_data`）：库存查询应以库存表为主表，这是正确的业务逻辑。`part_stock` 缺记录是**数据迁移问题**，应修复迁移逻辑而非改查询。

曾尝试将主表调换为 `FROM part_data LEFT JOIN part_stock`（让没库存的配件也能查到），经用户指出这是数据迁移问题后已**还原**。当前查询保持原始主表 `part_stock`，仅保留 ISNULL 性能优化。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Repositories/PartRepository.cs` | `GetStockListAsync`/`GetStockListAdvancedAsync`：保留 `ISNULL(DEL,'0')='0'` 性能优化；主表还原为 `FROM part_stock LEFT JOIN part_data`（曾调换为 part_data 主表，已还原） |

### 待后续处理
`part_stock` 表缺记录是数据迁移问题，需排查 `MigrationService` 中 `part_stock` 表的迁移逻辑，确认为何部分配件库存记录未迁移。

### 验证结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj`：**0 错误**（2个警告为既有的CS0618）

---

## 2026-08-09 查询卡顿9秒根因定位（OleDb 读取多列数据慢）

### 问题描述
数据迁移修复后，销售开单查询再次卡顿9秒。日志显示 `GetStockListAsync 返回200行 耗时9366ms`，但 PowerShell 直连数据库用 SqlClient 跑同样 SQL 只要 19ms。

### 根因分析
编写独立 C# 测试程序，用 OleDb (SQLOLEDB) 连接测试同一 SQL：
```
OleDb ISNULL (只选partid 1列):     41ms
OleDb OR (只选partid 1列):         19ms
OleDb full cols ISNULL (选24列):   9317ms  ← 慢！9秒！
OleDb ISNULL 2nd (只选partid):     20ms
```

**根因**：SQL 本身没问题（只选 partid 只要 19ms），但**选完整24列数据时 OleDb 耗时 9317ms**。

OleDb (SQLOLEDB) 提供程序在读取多列数据（24列 × 200行 = 4800个值）时性能极差：
- SqlClient 读同样数据只要几十毫秒
- OleDb 读同样数据要 9 秒

这不是 SQL 写法问题（ISNULL vs OR 差异在只选 partid 时仅 41ms vs 19ms），不是连接问题（OpenAsync 0ms），纯粹是 OleDb provider 读取列数据的瓶颈。

### 历史背景
- 之前会话明确选择保留 OleDb provider，因为 SQL Server 2000 + .NET 8 下：
  - `System.Data.SqlClient` 在 .NET 8 上默认协商 TLS，与 SQL 2000 不兼容（prelogin 握手失败）
  - `Microsoft.Data.SqlClient` v7.0.2 已放弃 SQL 2000 支持（错误状态: 15）
  - OleDb (SQLOLEDB) 是 Windows 内置、兼容 SQL 2000 的唯一稳定方案

### 待用户决策
OleDb 读多列数据慢是 provider 级别瓶颈，无法通过 SQL 优化解决。可选方案：
1. **减少查询列数**：检查 UI 实际显示哪些列，只查需要的列（可能从24列减到10列以内）
2. **升级数据库**：从 SQL Server 2000 升级到 SQL Server 2008+，即可用 SqlClient
3. **分页查询**：从 TOP 200 改为分页（每页50条），减少单次返回数据量
4. **尝试 ODBC**：改用 ODBC `{SQL Server}` 驱动（Windows内置，兼容SQL2000），测试是否比 OleDb 快

### 未做修改
本次仅做诊断分析，未修改任何代码。SQL 写法保持 `FROM part_stock LEFT JOIN part_data` + `ISNULL(DEL,'0')='0'` 不变。

---

## 2026-08-09 修复查询卡顿9秒（SQL Server 2000 多列+排序执行计划低效）

### 问题描述
上一条记录诊断后，用户反馈查询仍卡9秒。日志：`GetStockListAsync 返回200行 耗时9366ms`。

### 根因分析（逐列对比测试）
编写独立测试程序，用新连接逐列累加测试，定位到：
- 只查 partid 1列：25ms
- 查14列：33ms
- 查15列（+memo）：9331ms
- 查18列（+isck+warning+part_th+part_gg）：9479ms

进一步测试发现：**不是某一列慢，而是多列+ORDER BY 组合触发 SQL Server 2000 生成低效执行计划**。直接 `SELECT 24列 ... ORDER BY partno` 要9秒，但用子查询方案只要22ms。

### 解决方案
**子查询方案**：先在子查询中 `SELECT TOP 200 partid, partno ... ORDER BY partno ASC`（只排序轻量列），外层再 JOIN part_data 和 part_stock 获取所有列（走主键索引，快）。

```sql
-- 优化前（9秒）：多列+ORDER BY 触发低效执行计划
SELECT TOP 200 [24列] FROM part_stock LEFT JOIN part_data ON ... ORDER BY part_data.partno ASC

-- 优化后（22ms）：子查询先取partid，外层JOIN取列
SELECT [24列] FROM (
    SELECT TOP 200 part_stock.partid, part_data.partno FROM part_stock
    LEFT JOIN part_data ON ... WHERE ... ORDER BY part_data.partno ASC
) AS t
INNER JOIN part_data ON part_data.partid = t.partid
INNER JOIN part_stock ON part_stock.partid = t.partid
```

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Repositories/PartRepository.cs` | `GetStockListAsync`：改用子查询方案，子查询取partid+排序，外层JOIN取24列；`GetStockListAdvancedAsync`：同样改用子查询方案，所有WHERE条件移入子查询 |

### 验证结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj`：**0 错误**（4个警告均为既有的CS0618/CS8603）

### 待用户验证
运行软件，打开销售开单，看 `[QUERY] GetStockListAsync` 耗时是否从9秒降到几十毫秒。

---

## 2026-08-09 修复数据迁移遗漏 part_stock 记录（kcamount<=0 被过滤）

### 问题描述
销售开单查不到 `gm-8-048` 等配件（库存管理能查到）。排查发现目标库 `part_data` 有 25900 条但 `part_stock` 仅 11420 条，14480 个配件缺库存记录。

### 根因分析
直连源库（frp-bar.com:47017 备用数据）查询确认：
```
gm-8-048 在源库 tbisto: kcamount=0.00, posi=A3, iprj=170.54, oprc=234.00
tbisto 统计: 总25541行, kcamount>0 的11458行, kcamount<=0或NULL 的14083行
```

`MigrateStock()` 的查询 SQL 为 `SELECT * FROM tbisto WHERE kcamount > 0`，**`WHERE kcamount > 0` 过滤掉了 14083 个库存为0或负的配件**。这些配件在 `part_data` 表被迁移（`MigrateParts` 用 `tbprnoty` 表，无此过滤），但 `part_stock` 表未迁移，导致目标库有配件信息无库存记录，销售开单查不到。

### 修复方案
1. 去掉 `WHERE kcamount > 0`，迁移全部 `tbisto` 记录（含 kcamount<=0）
2. `kcamount` 求和后用 `Math.Max(0, ...)` 保证不为负（库存不能为负数）
3. 已有的 `nno` 为空过滤、重复 partid 跳过逻辑保持不变

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | 第614-615行：`SELECT * FROM tbisto WHERE kcamount > 0` 改为 `SELECT * FROM tbisto`；第627行：`kcamount` 求和后加 `Math.Max(0, ...)` 保证非负 |

### 补迁移说明
已迁移过的库重复运行迁移时，已有 `part_stock` 记录会被跳过（第662行 `exist > 0` 跳过逻辑），**只补插缺失的记录**，不会覆盖现有库存数据。用户重新运行数据迁移即可补齐 14480 个缺失的 part_stock 记录。

### 验证结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj`：**0 错误**（2个警告为既有的CS0618）

---

## 2026-08-09 修复拼音修复模块（迁移数据全为问号 + 扫描漏检 + 修复后仍为问号）

### 问题描述
从第三方迁移的配件、车型数据拼音码（`name_py`/`cartype_py`）全部是 `?` 字符串。拼音修复模块存在三个问题：
1. 只能扫描出 100 多条没有拼音码的数据（实际应有 2 万多条问号数据）
2. 修复前显示 `?` 号（数据本身就是问号字符串）
3. 修复后仍显示 `?` 号（没有重新生成拼音）

### 数据库现状（part_data 表，共 25900 条未删除记录）
- `name_py` 含问号：**25894** 条（如 `name=半轴总成970MM` → `name_py=????MM`）
- `cartype_py` 含问号：**20324** 条
- 原扫描 SQL 只能扫到 3+182=185 条真正空字符串记录

### 根因分析（3 个 bug）

**Bug 1 - 数据来源：`MigrationService.GetChineseInitial` 编码比较错误**
`src/QP11.Services/MigrationService.cs` 原 `GetChineseInitial(char ch)` 方法用字符的 **Unicode 码点**（中文字符范围 `0x4e00-0x9fff`）直接与 **GB2312 编码范围**（`0xB0A1-0xD7F9`）比较，两者无交集，所有中文都走到 `return '?'`，导致迁移时每个中文都被写成 `?`。

**Bug 2 - 扫描漏检：`PartRepository.GetMissingPinyinAsync` WHERE 条件不全**
`src/QP11.Data/Repositories/PartRepository.cs` 原 WHERE 条件为 `name_py IS NULL OR name_py = ''`，只能扫到真正空字符串的 185 条记录，扫不到 25894+20324 条问号字符串记录。

**Bug 3 - 修复后仍为问号：`PinyinFixWindow.BtnScan_Click` 判断逻辑错误**
`src/QP11.Wpf/Views/PinyinFixWindow.xaml.cs` 原代码：
```csharp
NamePyNew = string.IsNullOrEmpty(r.NamePy) ? PinyinHelper.GetPinyinInitials(...) : r.NamePy
```
`r.NamePy` 是 `"????"` 时 `IsNullOrEmpty` 返回 false，直接复用原值，未重新生成拼音。

### 修复方案

**修复 1 - MigrationService.GetChineseInitial 用 GB2312 字节比较**
参考 `src/QP11.Wpf/Helpers/PinyinHelper.cs` 的正确实现，将 Unicode 字符转为 GB2312 字节再比较编码范围：
```csharp
private static readonly Encoding Gb2312 = Encoding.GetEncoding("GB2312");
private static char GetChineseInitial(char ch)
{
    var bytes = Gb2312.GetBytes(ch.ToString());
    if (bytes.Length < 2) return '?';
    int code = (bytes[0] << 8) + bytes[1];
    // ... 范围判断 A-Z
}
```
依赖宿主进程（QP11.Wpf）在 `App.xaml.cs#L53` 注册的 `CodePagesEncodingProvider`。同步修正了 s/t 字母边界（`0xC8F6-0xCBF0` 为 S，`0xCBF1-0xCDD9` 为 T，与 PinyinHelper 对齐）。

**修复 2 - PartRepository 扫描 SQL 加入问号判断**
WHERE 条件增加 `OR name_py LIKE '%?%'` / `OR cartype_py LIKE '%?%'`，并把 OR 子句用括号明确分组，避免 AND/OR 优先级歧义。

**修复 3 - PinyinFixWindow 新增 NeedsRegenerate 判断**
新增私有方法 `NeedsRegenerate(string? py)`：拼音为空 **或包含 `?`** 时返回 true，触发重新生成。`NamePyNew`/`CartypePyNew` 判断改用 `NeedsRegenerate`；统计逻辑 `nameMissing`/`cartypeMissing` 同步用同一判断。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | 顶部加 `using System.Text;`；类内加 `Gb2312` 静态字段；`GetChineseInitial` 改用 GB2312 字节比较，修正 s/t 边界 |
| `src/QP11.Data/Repositories/PartRepository.cs` | `GetMissingPinyinAsync` SQL 加 `LIKE '%?%'` 条件，括号分组 |
| `src/QP11.Wpf/Views/PinyinFixWindow.xaml.cs` | 新增 `NeedsRegenerate` 方法；`NamePyNew`/`CartypePyNew` 判断改用 `NeedsRegenerate`；统计逻辑同步 |

### 验证

**编译验证**：QP11.Services / QP11.Data / QP11.Wpf 三个项目均 0 错误（仅有原有警告和 WebApi.dll 被占用警告）。

**SQL 验证新扫描条件**：新 WHERE 能扫到 **25900 条**记录（vs 原 185 条），其中 `name_py` 需修复 25897 条，`cartype_py` 需修复 20528 条。

### 后续
1. 重启软件（当前 QP11.WebApi.dll 被占用，需关闭运行中的软件后再启动）
2. 打开「拼音修复」窗口，点「扫描缺失」应显示 25900 条，名称/车型拼音(修复)列显示正确的拼音首字母
3. 点「一键修复」批量更新所有问号数据为正确拼音
4. 修复后重新扫描应显示「所有配件拼音码完整，无需修复」

> **注**：本次修复的扫描 SQL（`LIKE '%?%'`）和 `NeedsRegenerate` 判断已被下方「W164W166 数字丢失」修复改为全量校正逻辑，不再适用。

---

## 2026-08-09 修复拼音修复模块（车型 W164W166 修复后变 WW，数字丢失）

### 问题描述
用户反馈：车型为 `W164W166`，修复后的拼音码变成了 `WW`（数字 164/166 被丢弃）。

### 数据库现状确认
查询 `cartype LIKE '%W164%' OR cartype LIKE '%W166%'` 共 104 条，典型数据：
- `cartype=[W164]` → `cartype_py=[W]`（数字 164 丢失）
- `cartype=[W166 (2014-)]` → `cartype_py=[W]`（数字 166 丢失）
- `cartype=[W164 普通]` → `cartype_py=[w164pt]`（正常，手动改过）

统计全表（25900 条未删除记录）：
- `cartype` 含数字但 `cartype_py` 不含数字且不含问号：**5157 条**
- `name` 含数字但 `name_py` 不含数字且不含问号：**1 条**

### 根因（2 个 bug）

**Bug 1 - 数据来源：`MigrationService.GetPinyinInitial` 不处理数字**
`src/QP11.Services/MigrationService.cs` 原 `GetPinyinInitial` 只处理中文（`0x4e00-0x9fff`）和字母（`char.IsLetter`），**没有数字分支**，导致 `W164` → `W`、`W166` → `W`。而 `PinyinHelper.GetPinyinInitials` 有数字分支（`c >= '0' && c <= '9'`），两个实现不一致。

**Bug 2 - 修复模块判断不全：`NeedsRegenerate` 只判断空和问号**
前一次修复引入的 `NeedsRegenerate(string? py)` 判断逻辑为「为空或包含 `?`」。`"W"` 不为空也不含问号，被当作有效值不重新生成，所以修复后仍是 `"W"`。

### 修复方案（全量校正，用户确认）

**修复 1 - MigrationService.GetPinyinInitial 加数字分支**
与 `PinyinHelper.GetPinyinInitials` 对齐，增加 `else if (char.IsDigit(c)) result += c;`，迁移时 `W164` → `W164`、`W166` → `W166`。

**修复 2 - 拼音修复模块改为全量校正逻辑**
- `PartRepository.GetMissingPinyinAsync`：SQL 改为返回所有未删除且有 name/cartype 的记录（去掉拼音字段判断），交由 C# 端判断
- `PinyinFixWindow.BtnScan_Click`：对每条记录重新生成拼音，与原值比较（`StringComparison.OrdinalIgnoreCase` 忽略大小写），不一致即纳入修复列表
  - `????` vs 重新生成 `bczx` → 不一致 → 修复
  - `W` vs 重新生成 `w164` → 不一致 → 修复
  - `bcw164` vs 重新生成 `bcw164` → 一致 → 不修复
- 移除 `NeedsRegenerate` 方法，统计文案改为「名称拼音不一致 N 条，车型拼音不一致 N 条」
- `NamePyNew`/`CartypePyNew`：需修复时用重新生成的值，不需修复时保留原值（避免 `UpdatePinyinAsync` 覆盖正常字段）

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | `GetPinyinInitial` 增加 `else if (char.IsDigit(c)) result += c;` 数字分支 |
| `src/QP11.Data/Repositories/PartRepository.cs` | `GetMissingPinyinAsync` SQL 改为返回所有未删除且有 name/cartype 的记录，C# 端做全量校正判断 |
| `src/QP11.Wpf/Views/PinyinFixWindow.xaml.cs` | `BtnScan_Click` 改为全量校正逻辑（重新生成+比较）；移除 `NeedsRegenerate`；统计文案改为「不一致」 |

### 验证
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj`：**0 错误**。

### 后续
1. 重启软件
2. 打开「拼音修复」窗口点「扫描缺失」，应显示约 31000 条（问号 25894 + 数字丢失 5157 + 其他不一致）
3. W164/W166 相关记录的「车型拼音(修复)」列应显示 `w164`/`w166`（含数字）
4. 点「一键修复」批量更新
5. 修复后重新扫描应显示「所有配件拼音码完整，无需修复」

---

## 2026-08-09 修复销售编辑窗口输入完整拼音首字母查不到客户（shpw查不到上海鹏伟）

### 问题描述
销售编辑窗口（SellEditDialog）的客户查询输入框中，输入"shpw"查不到"上海鹏伟"的销售历史，但输入"shp"和"pw"都能查到。

### 根因分析
通过数据库查询发现 `client_infor` 表中存在脏数据：cid=KH08786，**name="shpw"**（客户名直接是拼音字母而非中文），name_py="SHPW"，该客户没有任何销售记录。而真正的"上海鹏伟"是 cid=KH02（有2385条销售记录）。

`SellEditDialog.ResolveClientNames` 方法采用**精确匹配优先且短路**的逻辑：
1. 输入"shpw" → 第一步精确匹配（`c.Name == input`）命中脏数据 KH08786 → 直接返回 `["shpw"]` → **跳过拼音匹配** → SQL 用 `name IN ('shpw')` 查询销售历史，因 KH08786 无销售记录，结果为空
2. 输入"shp" → 精确匹配无结果 → 进入拼音 Contains 匹配 → "shpw".Contains("shp")=true → 返回"上海鹏伟"等 → 有数据
3. 输入"pw" → 精确匹配无结果 → 拼音 Contains 匹配 → "shpw".Contains("pw")=true → 返回"上海鹏伟"等 → 有数据

### 修复方案
用户选择"修改代码逻辑"（不删除脏数据）。修改 `ResolveClientNames` 方法，**取消精确匹配短路**，合并所有匹配结果，并将拼音匹配放在精确匹配之前（用户输入拼音时意图是搜索拼音对应客户）。同步修改 `GetSelectedClientCode` 复用 `ResolveClientNames` 逻辑，消除相同的短路问题。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Wpf/Views/SellEditDialog.xaml.cs` | `ResolveClientNames`：取消三步短路返回，改为合并所有匹配结果（拼音匹配 → 精确匹配 → 名称包含匹配），拼音匹配优先排序；`GetSelectedClientCode`：改为复用 `ResolveClientNames` 结果，取第一个匹配客户名对应的Cid |

### 修复后行为
- 输入"shpw" → 拼音匹配命中"上海鹏伟"等 + 精确匹配命中"shpw" → 返回 `["上海鹏伟", "上海鹏伟南路军歆...", "shpw"]` → SQL 用 IN 查询 → 销售历史有数据
- `GetSelectedClientName` 返回"上海鹏伟"，`GetSelectedClientCode` 返回 KH02（正确客户）

### 验证结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj`：**0 错误**（既有 CS0618/CS8603 警告，非本次引入）

### 待用户验证
重启软件，打开销售编辑窗口，在客户查询输入框输入"shpw"，确认销售历史能显示"上海鹏伟"的记录。

---

## 2026-08-09 修复 GetStockListAsync 子查询 ORDER BY 缺少 TOP 报错

### 问题描述
运行时报错：
```
System.Data.OleDb.OleDbException (0x80040E14)
除非同时指定了 TOP，否则 ORDER BY 子句在视图、内嵌函数、派生表和子查询中无效。
```
堆栈定位到 `PartRepository.GetStockListAsync` 的子查询。

### 根因分析
前一次修复（见「查询卡顿9秒」小节）为 `GetStockListAsync` / `GetStockListAdvancedAsync` 引入子查询方案：
```sql
FROM (SELECT {topClause} part_stock.partid, part_data.partno FROM part_stock
      LEFT JOIN part_data ON ...
      WHERE ...
      ORDER BY part_data.partno ASC) AS t
```
SQL Server 2000 要求：**派生表（子查询）中使用 ORDER BY 必须同时指定 TOP**。

- `GetStockListAdvancedAsync` 的子查询写死 `SELECT TOP 999999999 ...`，正确。
- `GetStockListAsync` 的 `topClause` 取值规则为 `top > 0 ? $"TOP {top}" : ""`，当 `top=0`（默认值）时为空字符串，子查询变成 `SELECT  part_stock.partid ... ORDER BY part_data.partno ASC`，**缺少 TOP**，触发报错。

### 触发场景
`GetStockListAsync` 的调用方中，以下两处使用默认 `top=0`，必然触发报错：

| 文件 | 调用 |
|------|------|
| `src/QP11.Wpf/ViewModels/BaosunViewModel.cs:51` | `GetStockListAsync(keyword)`（报损单） |
| `src/QP11.Wpf/Views/InventoryWindow.xaml.cs:33` | `GetStockListAsync(keyword)`（库存管理） |

其余调用方（`SellViewModel`、`PartSelectorWindow`、`PartsController` 等）显式传 `top=200`，子查询带 `TOP 200`，不受影响。
`GetStockTool.cs` 传入的 `top` 取决于上层参数，理论上也可能触发。

### 修复方案
将 `topClause` 在 `top<=0` 时设为 `TOP 999999999`，与 `GetStockListAdvancedAsync` 保持一致：
```csharp
var topClause = top > 0 ? $"TOP {top}" : "TOP 999999999";
```

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Repositories/PartRepository.cs` | `GetStockListAsync` 第201行：`topClause` 的空字符串默认值改为 `"TOP 999999999"`；补充注释说明 SQL Server 2000 子查询 ORDER BY 必须配合 TOP |

### 验证结果
`dotnet build f:\qp11\src\QP11.Wpf\QP11.Wpf.csproj --no-incremental`：**0 错误**（4 个警告均为既有的 CS0618/CS8603，非本次引入）

### 待用户验证
重启软件，打开「库存管理」或「报损单」页面（这两处走 `top=0` 分支），确认不再报 `除非同时指定了 TOP` 错误，且配件列表正常加载。

---

## 2026-08-09 修复数据迁移后销售/退货未区分的问题

### 问题描述
本地项目数据库通过 `flag` 和 `type` 字段区分销售和退货（`flag=2` 表示退货，`flag=1` 表示正常），但第三方迁移过来的数据没有区分，所有单据的 `flag` 和 `type` 都写死为 1，导致退货单无法被识别。

### 根因分析
源库（第三方软件）通过**明细数量正负**区分销售和退货（整单明细数量全为负数即为退货单），没有独立的退货类型字段。而原迁移代码中 `bill_buy`/`detail_buy`/`bill_sell`/`detail_sell` 的 INSERT 语句把 `flag` 和 `type` 写死为 1，未根据明细数量正负设置。

参考依据：
- `src/QP11.Core/Constants/BusinessConstants.cs` 中 `BillFlag` 枚举 `Returned=2`
- `src/QP11.Services/SellService.cs` 和 `BuyService.cs` 中本地软件创建退货单时设置 `flag=2`

### 修复方案
在 `MigratePurchases` 和 `MigrateSales` 方法中，按源库单据分组后判断该单所有明细数量是否全为负数：
- 全负 → 退货单：`flag=2`, `type=2`
- 否则 → 正常单：`flag=1`, `type=1`

将原 INSERT 语句中写死的 `1` 改为参数化 `@flag`/`@type`，值为 `billFlag`/`billType`。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | `MigratePurchases`（第719-721行）：添加 `isReturn`/`billFlag`/`billType` 判断（jkamount 全负为退货）；`bill_buy` INSERT（第755-756行）：flag/type 改为参数；`detail_buy` INSERT（第804行）：type 改为参数 |
| `src/QP11.Services/MigrationService.cs` | `MigrateSales`（第865-867行）：添加 `isReturn`/`billFlag`/`billType` 判断（ckamount 全负为退货）；`bill_sell` INSERT（第891-892行）：flag/type 改为参数；`detail_sell` INSERT（第921-942行）：flag/type 由写死的 `1, 1` 改为 `@flag, @type` 参数 |

### 关键代码
```csharp
// 退货单识别：源库无退货类型字段，通过明细数量全为负数标识整单退货。
// 退货单 flag=2(BillFlag.Returned), type=2(退货)；正常单 flag=1, type=1。
var isReturn = group.All(r => ((long?)(double?)r.jkamount ?? 0) < 0);  // 采购用 jkamount，销售用 ckamount
var billFlag = isReturn ? 2 : 1;
var billType = isReturn ? 2 : 1;
```

### 验证结果
`dotnet build f:\qp11\src\QP11.Services\QP11.Services.csproj --no-incremental`：**0 错误**（4 个警告均为既有的 CS0618/CS8603，非本次引入）

### 待用户验证
重新运行数据迁移后，查询目标库确认退货单已正确标记：
```sql
-- 采购退货单
SELECT * FROM bill_buy WHERE flag=2
-- 销售退货单
SELECT * FROM bill_sell WHERE flag=2
```

---

## 2026-08-09 收缩数据库文件释放空间

### 操作描述
清空业务数据后，数据库文件仍占用大量空间（数据文件 685.50 MB，日志文件 1616.69 MB，共 ~2.25 GB），需收缩释放空间。

### 操作内容
- 执行 `BACKUP LOG qipei WITH TRUNCATE_ONLY` 截断事务日志
- 执行 `DBCC SHRINKFILE (qipei, 20)` 收缩数据文件到 20 MB
- 执行 `DBCC SHRINKFILE (qipei_Log, 10)` 收缩日志文件到 10 MB

### 收缩前后对比

| 文件 | 收缩前 | 收缩后 | 实际已用 |
|------|--------|--------|----------|
| `qipei.mdf`（数据文件） | 685.50 MB | 20.00 MB | 7.00 MB |
| `qipei_Log.ldf`（日志文件） | 1616.69 MB | 10.18 MB | 2.46 MB |
| **总计** | **2302.19 MB** | **30.18 MB** | 9.46 MB |

### 执行方式
- 通过 Python pyodbc 直连数据库 `192.168.1.86,1433`（ODBC `{SQL Server}` 驱动，兼容 SQL Server 2000）执行 DBCC 命令
- 文件大小查询使用 `sysfiles`（SQL Server 2000 兼容，非 `sys.database_files`）

### 备注
此次收缩前已清空业务数据（保留 `user_infor` 员工账号、`groups` 用户组、`mnu` 菜单、`desktop` 桌面配置、`friends` 通讯录、`business_type` 业务类型、`CLASSES` 系统配置、`serialnumber*` 已重置为0、`pbcat*` PowerBuilder 系统表等），清空操作由用户手动完成。

---

## 2026-08-10 修复供应商迁移丢失245条gyno为空且tbgugys无法匹配的记录

### 问题描述
数据迁移日志显示供应商"迁移 496 条, 跳过 32 条 (共 773 条)"，但实际有 245 条供应商记录被静默跳过（未计入 skipped 计数）。这些记录在 `tbistoed` 中有采购单据引用，但因 `gyno` 为空且 `gyname` 无法在 `tbgugys` 中匹配，被 `continue` 跳过且未执行 `skipped++`。

结果：`supplier_infor` 表缺少这 245 条供应商，采购迁移时这些单据的 `bill_buy.supplier` 为空。

### 根因分析
`MigrateSuppliers()` 中处理 `gyno` 为空的三种情况：

| 情况 | 原代码处理 | 问题 |
|------|-----------|------|
| gyno 为空 + gyname 在 tbgugys 中有匹配 | 用 tbgugys 的 gyno 或 "S"+id 生成 sid | 正常 |
| gyno 为空 + gyname 在 tbgugys 中无匹配 + gyname 非空 | `continue`（跳过，未 skipped++） | **丢失供应商** |
| gyno 为空 + gyname 也为空 | `continue`（跳过，未 skipped++） | 无法迁移 |

通过脚本 `Tools/SchemaQuery/check_skipped.py` 连接源库分析，确认 245 条 gyno 为空且 gyname 无法匹配的记录中，1 条 gyname 也为空，其余 244 条有 gyname 但未插入 `supplier_infor`。

### 修复方案
在 `MigrateSuppliers()` 中，当 `gyno` 为空且 `gyname` 无法在 `tbgugys` 中匹配时：

1. **gyname 非空**：先检查目标库 `supplier_infor` 是否已有同名供应商（避免重复插入），若已有则 `skipped++`；否则生成唯一 sid `"G" + 自增序号`（如 G1、G2...），正常插入 `supplier_infor`
2. **gyname 也为空**：`skipped++` 并跳过

这样采购迁移时（`MigratePurchases` 第723-731行），gyno 为空时用 gyname 查 `supplier_infor.name` 即可找到对应的 G 编号 sid。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Services/MigrationService.cs` | 第497行：新增 `int genCounter = 1;` 计数器 |
| `src/QP11.Services/MigrationService.cs` | 第505-536行：将原 `else { continue; }` 拆分为 `else if (gyname非空)` 分支（查重+生成G编号sid）和 `else` 分支（skipped++） |

### 验证结果
`dotnet build src/QP11.Services/QP11.Services.csproj`：**0 错误**（4个警告为既有的 CS0618/CS8603，非本次引入）

## 2026-08-10 打包 exe 安装程序 v2.2.3

### 操作内容
将项目打包为 exe 安装程序，包含近期所有代码修改（OpenAsync 异步化、连接状态修复、MigrationService flag/type 与供应商迁移修复等）。

### 版本号变更
- `src/QP11.Wpf/QP11.Wpf.csproj`：`<Version>` 2.2.1 → 2.2.3
- `QP11_Setup.iss`：`#define MyAppVersion` 2.2.2 → 2.2.3

### 打包流程
1. **dotnet publish**：`dotnet publish src\QP11.Wpf\QP11.Wpf.csproj -c Release -o Publish --self-contained true -r win-x64`
   - 自包含发布（内置 .NET 8 运行时，目标机器无需预装环境）
   - 输出目录：`f:\qp11\Publish\`
   - 结果：成功，0 错误（仅既有警告）
2. **Inno Setup 编译**：`ISCC.exe QP11_Setup.iss`（使用根目录脚本）
   - 编译耗时：50.6 秒
   - 压缩方式：lzma2/ultra64 + SolidCompression

### 产物
- 安装包路径：`f:\qp11\InstallPackage\QP11Setup_v2.2.3.exe`
- 大小：75.89 MB
- 生成时间：2026-08-10 08:45

### 注意事项
- 安装脚本排除 `appsettings.json`、`printsettings.json`、`Data\*`，重装时不覆盖用户配置和业务数据
- 卸载时询问是否删除 `Data` 和 `logs` 目录，默认保留用户业务数据

## 2026-08-10 修复配件报损页面报错"对象名 'bill_baosun' 无效"

### 问题描述
配件报损页面（BaosunControl）打开时报错：
```
System.Data.OleDb.OleDbException: 对象名 'bill_baosun' 无效。
```

### 根因分析
通过查询数据库和参考原始 PB 项目 DataWindow 分析文档（`restore-ui-fidelity/spec.md` 第416行），确认：

> **原始 PB 系统中报损功能使用 `bill_sell` 表，通过 `flag=3` 标识报损记录。**
> `d_bill_baosun_list` 的 SQL 为：`SELECT * FROM bill_sell WHERE flag=3`

当前 WPF 代码错误地使用了不存在的 `bill_baosun` 和 `detail_baosun` 表名，而配套数据库（`qipei`）中实际使用的是 `bill_sell`（flag=3）和 `detail_sell` 表。

### 修复方案

**1. `BillBaosun.cs` 实体映射修正：**
- `[Table("bill_baosun")]` → `[Table("bill_sell")]`
- `[Table("detail_baosun")]` → `[Table("detail_sell")]`
- `DetailBaosun.Inprice` 的 `[Column("inprice")]` → `[Column("price")]`（detail_sell 表无 inprice 列，进价存在 price 列）
- `DetailBaosun.Intotal` 的 `[Column("intotal")]` → `[Column("stotal")]`（detail_sell 表无 intotal 列，小计存在 stotal 列）

**2. `BaosunRepository.cs` SQL 修正：**
- 所有查询表名从 `bill_baosun`/`detail_baosun` 改为 `bill_sell`/`detail_sell`
- 查询时添加 `flag=3` 过滤条件（报损记录标识）
- INSERT 时 `flag` 和 `type` 硬编码为 3（报损类型）
- INSERT 明细时字段名 `inprice`→`price`，`intotal`→`stotal`

### 修改文件列表

| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Core/Entities/BillBaosun.cs` | 表映射改为 bill_sell/detail_sell，Column 映射 inprice→price、intotal→stotal |
| `src/QP11.Data/Repositories/BaosunRepository.cs` | SQL 表名改为 bill_sell/detail_sell，查询加 flag=3，INSERT 硬编码 flag=3, type=3 |

### 编译结果
- QP11.Core：0 错误，0 警告
- QP11.Data：0 错误，2 警告（既有）
- QP11.Wpf：0 错误，6 警告（既有）

## 2026-08-10 报损管理页面查询改造（实时搜索+防抖+车型筛选）

### 改动内容

**1. BaosunControl.xaml（查询条件区改造）**
- Mode A（开单模式）：查询条件区新增"车型"文本框，移除"查询"按钮
- Mode B（查询模式）：移除"查询"按钮
- 搜索框绑定 `TextChanged` 事件，日期选择器绑定 `SelectedDateChanged` 事件

**2. BaosunControl.xaml.cs（防抖逻辑）**
- 新增 `_debounceCts` 和 `_billDebounceCts`（`CancellationTokenSource`）
- 新增 `OnSearchTextChanged` 方法：文本变化时取消上次防抖定时器，300ms 后重新查询配件列表
- 新增 `OnBillDateChanged` 方法：日期变化时取消上次防抖定时器，300ms 后重新查询单据列表
- 新增 `DebounceSearchPartsAsync` / `DebounceSearchBillsAsync` 防抖方法
- 新增 `LoadBills` 方法（原 `BtnSearchBills_Click` 逻辑拆分）
- `LoadPartList` 改为从三个独立文本框读取参数，调用多参数 `LoadPartListAsync`
- 移除 `BtnSearchParts_Click` 和 `TxtPartQuery_KeyDown` 事件处理

**3. BaosunViewModel.cs**
- `LoadPartListAsync` 签名改为 `(string? partNo, string? partName, string? cartype)`
- 所有参数为空时回退到 `GetStockListAsync`，否则调用 `GetStockListAdvancedAsync`

### 编译结果
QP11.Wpf：0 错误，6 警告（既有）

## 2026-08-10 修复报损查询模式经手人显示数字ID的问题

### 问题描述
报损出库页面的查询模式下，单据列表的"经手人"列显示的是数字工号，而不是对应的姓名。

### 根因分析
`BaosunRepository.GetListAsync()` 的 SQL 使用 `SELECT * FROM bill_sell` 直接查询，`worker` 字段返回的是 `bill_sell` 表中存储的工号（workid），没有关联 `work_infor` 表转换为人名。

### 修复方案
将 `GetListAsync()` 的 SQL 改为显式列选择 + LEFT JOIN `work_infor`：
- 用 `ISNULL(wi.name, b.worker) AS worker` 将经手人工号转为姓名
- 如果 `work_infor` 中找不到对应记录，回退显示工号
- 使用 `b.` 表别名前缀确保列名无歧义

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/QP11.Data/Repositories/BaosunRepository.cs` | `GetListAsync` 的 SQL 从 `SELECT * FROM bill_sell` 改为显式列选择 + LEFT JOIN `work_infor` |

### 编译结果
QP11.Data：0 错误，2 警告（既有，PartRepository.cs）

---

## 2026-08-15 工作台移除库存预警和应收款，新增今日配件销售排行

### 需求
桌面导航工作台：
1. 移除「库存预警」KPI 卡片及其列表
2. 移除「应收款」KPI 卡片
3. 新增「今日配件销售排行」区域（按销量降序，显示实时库存）

### 操作内容
1. **仓储层**：`ISellRepository` 新增 `GetTodayPartsRankingAsync` 接口，`SellRepository` 实现——SQL 按 `detail_sell` 按配件分组 SUM 今日销量，`LEFT JOIN part_stock` 取实时库存，`TOP 10` 降序
2. **XAML 布局**：
   - KPI 卡片行：`UniformGrid Columns="4"` → `Columns="2"`，移除库存预警和应收款两张卡片
   - 右侧列表：从「库存预警（前 10 条）」DataGrid 改为「今日配件销售排行」DataGrid（列：排名/编号/名称/销量/金额/库存）
3. **代码后置**：
   - 移除 `_arrearageRepo` 字段和依赖注入（`DesktopControl` 构造函数、`MainWindow.xaml.cs` 2 处创建调用）
   - `LoadDashboardAsync` 移除 `alertTask`/`receivableTask`/`payableTask` 查询，新增 `rankingTask`
   - 排行数据在内存中加上 `Rank` 序号（SQL Server 2000 不支持 `ROW_NUMBER()`）
   - 移除 `DgStockAlerts_MouseDoubleClick` 事件处理

### 修改文件
- [ISellRepository.cs](file:///f:/qp11/src/QP11.Core/Interfaces/ISellRepository.cs#L26-L27) — 新增 `GetTodayPartsRankingAsync` 接口
- [SellRepository.cs](file:///f:/qp11/src/QP11.Data/Repositories/SellRepository.cs#L213-L234) — 新增 `GetTodayPartsRankingAsync` 实现
- [DesktopControl.xaml](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml#L138-L203) — KPI 卡片从 4 张减为 2 张，右侧列表改为配件销售排行
- [DesktopControl.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml.cs) — 移除 `_arrearageRepo`、更新 `LoadDashboardAsync`、移除库存预警事件
- [MainWindow.xaml.cs](file:///f:/qp11/src/QP11.Wpf/Views/MainWindow.xaml.cs#L83-L87) — 2 处 `DesktopControl` 构造移除 `IArrearageRepository` 参数

### 编译验证
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj -c Debug`：0 错误，0 警告

---

## 2026-08-15 桌面导航工作台今日销售单据客户编码改为客户名称

### 需求
桌面导航工作台「今日销售单据（最近 10 笔）」列表中将「客户编码」列改为显示「客户名称」。

### 操作内容
1. **BillSell 实体**新增 `ClientName` 属性（`[NotMapped]`，仅查询时填充，不持久化到 `bill_sell` 表）
2. **SellRepository.GetListAsync** SQL 改为 LEFT JOIN `client_infor` 并查询 `client_infor.name AS ClientName`，所有表名/字段名均加前缀以消除 JOIN 后的歧义
3. **DesktopControl.xaml** DataGrid 列标题由「客户编码」改为「客户名称」，绑定由 `{Binding Client}` 改为 `{Binding ClientName}`，宽度由 `80` 改为 `*`（名称长度可变）

### 修改文件
- [BillSell.cs](file:///f:/qp11/src/QP11.Core/Entities/BillSell.cs#L19-L21) — 新增 `ClientName` 属性
- [SellRepository.cs](file:///f:/qp11/src/QP11.Data/Repositories/SellRepository.cs#L46-L54) — GetListAsync SQL 增加 LEFT JOIN 查询客户名称
- [DesktopControl.xaml](file:///f:/qp11/src/QP11.Wpf/Views/DesktopControl.xaml#L205) — 列标题和绑定修改

### 编译验证
`dotnet build src/QP11.Wpf/QP11.Wpf.csproj -c Debug`：0 错误，0 警告

---

## 2026-08-15 打包 v2.2.9 安装程序

### 操作内容
基于 v2.2.8 之后新增的「工作台改造：今日配件销售排行」功能及此前多次 bug 修复（客户编码改名称、排行增加车型等），重新打包生成 v2.2.9 安装程序。

### 版本变更
- 版本号：v2.2.8 → v2.2.9
- 同步更新位置：
  - [QP11_Setup.iss](file:///f:/qp11/QP11_Setup.iss#L7) 第 7 行 `#define MyAppVersion "2.2.9"`
  - [QP11.Wpf.csproj](file:///f:/qp11/src/QP11.Wpf/QP11.Wpf.csproj#L28) 第 28 行 `<Version>2.2.9</Version>`
- 注：`UpdateService.GetCurrentVersion()` 通过反射读取程序集版本，无需硬编码改动

### 打包步骤
1. 清理旧 `F:\qp11\Publish\` 目录
2. `dotnet publish F:\qp11\src\QP11.Wpf\QP11.Wpf.csproj -c Release -o F:\qp11\Publish`
   - 自包含发布（SelfContained=true，RuntimeIdentifier=win-x64）
   - 编译输出：QP11.Wpf.exe FileVersion=2.2.9.0
   - 编译警告：2 个 `SqlConnection` 过时警告（既有，与本打包无关）
   - robocopy 退出码 1（MSB3073 警告）= wwwroot 复制成功，属正常
3. `ISCC.exe F:\qp11\QP11_Setup.iss` 编译安装包（64.5 秒）

### 输出结果
- 安装包路径：`F:\qp11\InstallPackage\QP11Setup_v2.2.9.exe`
- Inno Setup 编译警告（3 个，均为既有问题，未引入）：
  - OnlyBelowVersion 6.1 的 quicklaunchicon 任务在新系统不处理（Vista/2008 已不支持）
  - PrivilegesRequired=admin 与 per-user 区域（userappdata）共存提示
  - [Code] 段 `ResultCode` 变量未使用（卸载询问删除 Data/logs 时未取返回值）
