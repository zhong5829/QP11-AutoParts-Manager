<Window x:Class="QP11.Wpf.Views.BillPrintSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="单据打印设置" Width="1100" Height="700"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize" MinWidth="900" MinHeight="600">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧：设置面板 -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,8,0">
            <StackPanel>
                <!-- 单据类型 -->
                <GroupBox Header="单据类型" Margin="0,0,0,6" Padding="6">
                    <ComboBox x:Name="cboBillType" SelectionChanged="BillType_Changed" Margin="4">
                        <ComboBoxItem Content="销售单" IsSelected="True"/>
                        <ComboBoxItem Content="采购单"/>
                        <ComboBoxItem Content="退货单"/>
                    </ComboBox>
                </GroupBox>

                <!-- 打印格式 -->
                <GroupBox Header="打印格式" Margin="0,0,0,6" Padding="6">
                    <StackPanel>
                        <RadioButton x:Name="rbStyle1" Content="标准格式" IsChecked="True" Margin="4" Click="Setting_Changed"/>
                        <RadioButton x:Name="rbStyle2" Content="票据格式" Margin="4" Click="Setting_Changed"/>
                        <RadioButton x:Name="rbStyle3" Content="抬头格式" Margin="4" Click="Setting_Changed"/>
                    </StackPanel>
                </GroupBox>

                <!-- 打印高度 -->
                <GroupBox Header="打印高度" Margin="0,0,0,6" Padding="6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="70"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="高度:" VerticalAlignment="Center" Margin="4,0"/>
                        <TextBox Grid.Column="1" x:Name="txtHeight" Text="85" Margin="4,0" VerticalContentAlignment="Center" TextChanged="Height_Changed"/>
                        <TextBlock Grid.Column="2" Text="mm (0~999)" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </Grid>
                </GroupBox>

                <!-- 广告文字（抬头下方，加粗加大显示） -->
                <GroupBox Header="抬头下方广告文字（支持 {Phone} {Address} 占位符）" Margin="0,0,0,6" Padding="6">
                    <TextBox x:Name="txtAdText"
                             AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             MinHeight="50" MaxHeight="100"
                             TextChanged="AdText_Changed"/>
                </GroupBox>

                <!-- 页面边距 -->
                <GroupBox Header="页面边距 (mm)" Margin="0,0,0,6" Padding="6">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="上:" VerticalAlignment="Center" Margin="4,2"/>
                        <TextBox Grid.Column="1" x:Name="txtMarginTop" Text="5" Margin="2" VerticalContentAlignment="Center" TextChanged="Margin_Changed"/>
                        <TextBlock Grid.Column="2" Text="下:" VerticalAlignment="Center" Margin="4,2"/>
                        <TextBox Grid.Column="3" x:Name="txtMarginBottom" Text="5" Margin="2" VerticalContentAlignment="Center" TextChanged="Margin_Changed"/>

                        <TextBlock Grid.Row="1" Text="左:" VerticalAlignment="Center" Margin="4,2"/>
                        <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtMarginLeft" Text="8" Margin="2" VerticalContentAlignment="Center" TextChanged="Margin_Changed"/>
                        <TextBlock Grid.Row="1" Grid.Column="2" Text="右:" VerticalAlignment="Center" Margin="4,2"/>
                        <TextBox Grid.Row="1" Grid.Column="3" x:Name="txtMarginRight" Text="8" Margin="2" VerticalContentAlignment="Center" TextChanged="Margin_Changed"/>
                    </Grid>
                </GroupBox>

                <!-- 打印项目列表 -->
                <GroupBox Header="打印项目" Margin="0,0,0,6" Padding="6">
                    <StackPanel>
                        <DataGrid x:Name="dgColumns" AutoGenerateColumns="False" CanUserAddRows="False"
                                  HeadersVisibility="Column" SelectionMode="Single" RowHeight="26"
                                  BorderThickness="1" Margin="0,0,0,4"
                                  ItemsSource="{Binding}">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="显示" Width="38">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding Visible, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="列标题" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Header, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="宽度" Width="52">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Width, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent" TextAlignment="Right"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="排序" Width="48">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Content="▲" FontSize="9" Padding="3,0" Tag="up" Click="BtnMoveColumn_Click"/>
                                                <Button Content="▼" FontSize="9" Padding="3,0" Tag="down" Click="BtnMoveColumn_Click"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="添加列" Width="55" Margin="2,0" Click="BtnAddColumn_Click"/>
                            <Button Content="删除列" Width="55" Margin="2,0" Click="BtnDeleteColumn_Click"/>
                            <Button Content="恢复默认" Width="60" Margin="8,0,2,0" Click="BtnResetColumns_Click"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <!-- 特别声明 -->
                <GroupBox Header="特别声明" Margin="0,0,0,6" Padding="6">
                    <TextBox x:Name="txtDeclaration"
                             AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             MinHeight="60" MaxHeight="120"
                             TextChanged="Declaration_Changed"/>
                </GroupBox>

                <!-- 底部按钮 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
                    <Button Content="保存" Width="70" Margin="4,0" Click="BtnSave_Click" Padding="0,4"/>
                    <Button Content="取消" Width="70" Margin="4,0" Click="BtnCancel_Click" Padding="0,4"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- 右侧：实时预览 -->
        <GroupBox Grid.Column="1" Header="实时预览">
            <FlowDocumentReader x:Name="docPreview" ViewingMode="Scroll"/>
        </GroupBox>
    </Grid>
</Window>
