<UserControl x:Class="QP11.Wpf.Views.LabelPreviewEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
            <TextBlock Text="选区:" VerticalAlignment="Center" Margin="4,0,4,0"/>
            <TextBlock x:Name="txtSel" Text="未选中" VerticalAlignment="Center" MinWidth="70" Foreground="#4B3FE3"/>
            <Button Content="字号+" Width="56" Height="24" Margin="4,0" ToolTip="选中元素字号+2px（条码为高度+2mm）" Click="BtnFontUp_Click"/>
            <Button Content="字号-" Width="56" Height="24" Margin="4,0" ToolTip="选中元素字号-2px（条码为高度-2mm）" Click="BtnFontDown_Click"/>
            <Button Content="显隐" Width="56" Height="24" Margin="4,0" ToolTip="显示/隐藏选中元素" Click="BtnToggle_Click"/>
            <TextBlock Text="拖动调整位置 · 双击修改文字（仅本次打印）" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Gray" FontSize="11"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#999999" BorderThickness="1" Background="White">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                          HorizontalContentAlignment="Left" VerticalContentAlignment="Top">
                <Canvas x:Name="editCanvas" Background="White" MinWidth="200" MinHeight="120"
                        HorizontalAlignment="Left" VerticalAlignment="Top"
                        MouseMove="EditCanvas_MouseMove" MouseLeftButtonUp="EditCanvas_MouseUp"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>