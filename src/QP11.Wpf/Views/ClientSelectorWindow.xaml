<Window x:Class="QP11.Wpf.Views.ClientSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择客户" Height="500" Width="700"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="搜索(名称/编号/电话):" VerticalAlignment="Center"/>
            <TextBox x:Name="txtSearch" Width="250" Margin="5,0" TextChanged="TxtSearch_TextChanged" TabIndex="1"/>
            <Button Content="查询" Width="60" Click="BtnSearch_Click" TabIndex="2"/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="dgClients" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" MouseDoubleClick="DgClients_MouseDoubleClick" TabIndex="3">
            <DataGrid.Columns>
                <DataGridTextColumn Header="编号" Width="80" Binding="{Binding Cid}"/>
                <DataGridTextColumn Header="名称" Width="150" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="电话" Width="120" Binding="{Binding Tel}"/>
                <DataGridTextColumn Header="等级" Width="50" Binding="{Binding Level}"/>
                <DataGridTextColumn Header="信用额度" Width="80" Binding="{Binding Credit, StringFormat=c2}"/>
                <DataGridTextColumn Header="欠款" Width="80" Binding="{Binding SellUse, StringFormat=c2}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,0">
            <TextBlock x:Name="txtArrearInfo" Text="" Foreground="Red" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="确定选择" Width="80" Height="28" Margin="20,0,0,0" Click="BtnConfirm_Click" IsDefault="True" TabIndex="4"/>
            <Button Content="取消" Width="80" Height="28" Margin="5,0" IsCancel="True" TabIndex="5"/>
        </StackPanel>
    </Grid>
</Window>
