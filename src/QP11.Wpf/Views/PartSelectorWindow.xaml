<Window x:Class="QP11.Wpf.Views.PartSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:QP11.Wpf.Helpers"
        Title="配件选择" Height="550" Width="800"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Window.Resources>
        <helpers:SingleLineConverter x:Key="SingleLineConverter"/>
        <Style x:Key="SingleLineCell" TargetType="TextBlock">
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 搜索条件区 -->
        <Grid Grid.Row="0" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="编码:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Grid.Column="1" x:Name="txtPartNo" Margin="2" TextChanged="TxtSearch_TextChanged" PreviewKeyDown="QueryTextBox_PreviewKeyDown"/>
            <TextBlock Grid.Column="2" Text="名称:" VerticalAlignment="Center" Margin="8,0,4,0"/>
            <TextBox Grid.Column="3" x:Name="txtPartName" Margin="2" TextChanged="TxtSearch_TextChanged" PreviewKeyDown="QueryTextBox_PreviewKeyDown"/>
            <TextBlock Grid.Column="4" Text="车型:" VerticalAlignment="Center" Margin="8,0,4,0"/>
            <TextBox Grid.Column="5" x:Name="txtCartype" Margin="2" TextChanged="TxtSearch_TextChanged" PreviewKeyDown="QueryTextBox_PreviewKeyDown"/>

        </Grid>

        <!-- 配件列表 -->
        <DataGrid Grid.Row="2" x:Name="dgParts" AutoGenerateColumns="False"
                  IsReadOnly="False" CanUserAddRows="False" SelectionMode="Extended"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  MouseDoubleClick="DgParts_MouseDoubleClick"
                  PreviewKeyDown="DgParts_PreviewKeyDown"
                  ContextMenuOpening="DgParts_ContextMenuOpening"
                  CellEditEnding="DgParts_CellEditEnding"
                  PreparingCellForEdit="DgParts_PreparingCellForEdit">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                            <Setter Property="Foreground" Value="Red"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="件号" Width="100" Binding="{Binding Partno, Converter={StaticResource SingleLineConverter}}" ElementStyle="{StaticResource SingleLineCell}" IsReadOnly="True"/>
                <DataGridTextColumn Header="名称" Width="180" Binding="{Binding Name, Converter={StaticResource SingleLineConverter}}" ElementStyle="{StaticResource SingleLineCell}" IsReadOnly="True"/>
                <DataGridTextColumn Header="适用车型" Width="150" Binding="{Binding Cartype, Converter={StaticResource SingleLineConverter}}" ElementStyle="{StaticResource SingleLineCell}" IsReadOnly="True"/>
                <DataGridTextColumn Header="单位" Width="60" Binding="{Binding Unit, Converter={StaticResource SingleLineConverter}}" ElementStyle="{StaticResource SingleLineCell}" IsReadOnly="True"/>
                <DataGridTextColumn Header="仓位" Width="80" Binding="{Binding Place, Converter={StaticResource SingleLineConverter}}" ElementStyle="{StaticResource SingleLineCell}" IsReadOnly="True"/>
                <DataGridTextColumn Header="零售价" Width="80" Binding="{Binding Lsprice, StringFormat=N2}" ElementStyle="{StaticResource SingleLineCell}" IsReadOnly="True"/>
                <DataGridTextColumn Header="批发价" Width="80" Binding="{Binding Pfprice, StringFormat=N2}" ElementStyle="{StaticResource SingleLineCell}" IsReadOnly="True"/>
                <DataGridTextColumn Header="进货价" Width="80" Binding="{Binding Inprice, StringFormat=N2}" ElementStyle="{StaticResource SingleLineCell}" IsReadOnly="True"/>
                <DataGridTextColumn Header="库存" Width="55" Binding="{Binding StockAmount}" IsReadOnly="True"/>
                <DataGridTextColumn Header="预警" Width="55" Binding="{Binding StockWarning}"/>
                <DataGridTextColumn Header="备注" Width="*" Binding="{Binding Memo, Converter={StaticResource SingleLineConverter}}" ElementStyle="{StaticResource SingleLineCell}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button Content="确定选择" Width="80" Height="28" Margin="5,0" Click="BtnConfirm_Click" IsDefault="True"/>
            <Button Content="取消" Width="80" Height="28" Margin="5,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
