<UserControl x:Class="QP11.Wpf.Views.AgnesAssistantTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:QP11.Wpf.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="500"
             Background="#FAFAFA">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:AgnesMessageItem}">
            <Border Margin="6,3" Padding="10,6" CornerRadius="8" MaxWidth="420"
                    HorizontalAlignment="Left">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#FFFFFF"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsUser}" Value="True">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Background" Value="#DCEBFF"/>
                                <Setter Property="BorderBrush" Value="#B8D8FF"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsTool}" Value="True">
                                <Setter Property="Background" Value="#FFF4E0"/>
                                <Setter Property="BorderBrush" Value="#F0D080"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsSystem}" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="BorderBrush" Value="#DDDDDD"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="{Binding Text}" TextWrapping="Wrap"
                               Foreground="{Binding RoleBrush}" FontSize="13"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer x:Name="msgScroll" Grid.Row="0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Padding="8,8,8,4">
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="inputBox"
                         Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         MinHeight="52" MaxHeight="140"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="14" Padding="6"
                         KeyDown="InputBox_KeyDown"/>
                <StackPanel Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Bottom">
                    <Button Content="发送" Command="{Binding SendCommand}"
                            Padding="20,8" Margin="0,0,0,4" FontWeight="Bold"/>
                    <TextBlock Text="Enter 发送 / Shift+Enter 换行"
                               FontSize="10" Foreground="#999" Margin="0,2"/>
                </StackPanel>
            </Grid>
        </Border>

        <StatusBar Grid.Row="2" Background="#F0F0F0">
            <StatusBarItem>
                <Button Command="{Binding ToggleOnlineCommand}" Padding="8,2">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Content" Value="● 在线"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsOnline}" Value="False">
                                    <Setter Property="Content" Value="● 离线"/>
                                    <Setter Property="Foreground" Value="#CC0000"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <Button Content="清空历史" Command="{Binding ClearCommand}" Padding="8,2"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
