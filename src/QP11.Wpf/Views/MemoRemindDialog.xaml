<Window x:Class="QP11.Wpf.Views.MemoRemindDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="备忘录提醒" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    <Border BorderBrush="#E6A817" BorderThickness="2" Background="#FFF8E1" Padding="16">
        <StackPanel>
            <TextBlock Text="备忘录提醒" FontSize="15" FontWeight="Bold" Foreground="#8A5A00" Margin="0,0,0,8"/>
            <TextBlock Text="以下备忘已到提醒时间：" Foreground="#5C4100" Margin="0,0,0,6"/>
            <ItemsControl x:Name="listItems" Margin="6,0,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock TextWrapping="Wrap" Margin="0,2" Foreground="#5C4100">
                            <Run Text="• " FontWeight="Bold"/>
                            <Run Text="{Binding Content}"/>
                            <Run Text="　"/>
                            <Run Text="{Binding RemindText}" Foreground="#9C6E00" FontSize="11"/>
                        </TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,14,0,0">
                <Button Content="知道了" Width="90" Height="28" IsDefault="True" Click="BtnOk_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>